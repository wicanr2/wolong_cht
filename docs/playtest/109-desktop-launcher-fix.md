# 109 — 桌面選章與兩段確認修正

**狀態：兩項修正已通過 AppImage 正常操作驗證；Windows／macOS 封包檢查通過，原生 GUI 未驗。**

- 日期：2026-09-07；使用者限定 AppImage、Windows、macOS，Android 不在範圍。
- 規格：[155](../spec/155-desktop-launcher-input.md)；修正前證據：[108](108-appimage-dosgolem-input-audit.md)。
- 修正版：`v.1.0.1-20260907`，本機交付根目錄 `dist-all/v.1.0.1-20260907/`。
- 原版：dosv `KI.EXE`，SHA-256 `fffeba985231cda4d636e93d10f598470b1f691d00275e4aa38e285893d43868`；
  dosgolem 提交 `55067dbb030510ef56723885fff7b617e31d0338`，640×400 畫布座標，無新增反組譯。
- 新版封包、內部程式與原始碼的雜湊分別保存於交付根目錄的 `manifest.json`、
  `build-inputs.json`、`SHA256SUMS.txt`；原始碼仍為未提交工作樹，因此以逐檔雜湊識別實際建置內容。

## 修正結果

| 問題 | 修正 | 原封包正常操作驗證 |
|---|---|---|
| 點第一章日期進第二章 | 啟動選章與讀檔使用共用日期矩形，不再使用舊殼層列高 | `(300,152/200/248/296)` 分別進四章；`(220,128)` 不再選章 |
| 點一次直接進君主卡 | 清單初次無反白；第一次反白，第二次確認 | 第一章曹操兩次點擊後才進卡；第四章另測選第二列後點第三列仍確認第二列 |
| 取消層級缺失 | 反白時右鍵只解除反白；瀏覽時右鍵回劇本；卡片右鍵回未反白清單 | 三個分支正常 |
| 反白字仍為黑色 | 使用現有清單的反白字色 `chrome.Highlight` | 第一章反白清單內 **0 / 67,584 px** |

原版側本輪補驗的逐步截圖與日誌在 `workplace/desktop-launcher-fix-20260907/`。
AppImage 側的正常操作程式、逐步座標／時間與截圖在交付根目錄 `verification/`：

- `probe.py`、`input-trace.json`：使用 X11 真實滑鼠事件，不用直啟或代替玩家設定劇本。
- `factions-0.png` 至 `factions-3.png`：四章逐一選取。
- `faction-first.png`、`faction-deselected.png`、`lord-second.png`、`card-back.png`：兩段與取消。
- `first-chapter-selected.png`：與 dosgolem `list-first.png` 的清單矩形 **0 px**。
- `main.png`：正常選第一章、曹操並進入遊戲，與原版開局 **0 / 256,000 px**。
- `ignored-title-diff.txt`：點名稱欄之前與之後 **0 px**，沒有誤選。
- `list-initial-diff.txt`：初始清單剩 **95 px**，位置為原版滑鼠游標；不記成完整 0 px。

## 桌面封包

入口 [tools/release_desktop.sh](../../tools/release_desktop.sh) 限桌面，沿用既有 Go、
osxcross 與 AppImage 映像；重用原有素材過濾及 AppDir 組裝，沒有執行含 Android 的全平台管線。
版號接受 `^v\.[0-9]+\.[0-9]+\.[0-9]+-[0-9]{8}$`，輸出存在即停止，未覆蓋歷史包或 tag。
候選包完整包含本機原版資料，僅留 `full/`，不可公開散布；`release/`、`promo/` 說明本輪沒有新增公開包或影片。

| 封包 | 驗證 |
|---|---|
| Linux amd64 AppImage | 自解後執行本包 AppRun，無外掛資料旗標；正常滑鼠操作通過，runtime 二進位雜湊與本次建置相同 |
| Windows amd64 ZIP | ZIP 解碼、逐檔 checksum、原版資料／字型／音樂輸入雜湊、PE x86-64 與原始建置二進位雜湊均相符 |
| macOS 雙架構 tar.gz | 逐檔 checksum、共用素材、Mach-O x86-64／arm64 與建置二進位雜湊均相符 |

檢查程式：[tools/verify_desktop_packages.py](../../tools/verify_desktop_packages.py)，
結果 `verification/package-check.json`。Windows／macOS 尚無本輪原生機器操作證據，不能以交叉建置宣稱實機驗收。
Android 原始碼與封包未改動；完整 Go 測試仍會載入共用模組，不代表 Android 發行驗收。

## 額外確認頁評估

**已證實（v.1.0.1）：** 當時 `launcherGameConfirm` 只顯示劇本與君主，再呼叫 `startWorld`；
沒有存檔覆寫檢查。`startWorld` 不寫存檔；本輪用既有 SAVE.DAT 的副本正常開局，
退出驗證程序後 `cmp` 與 SHA-256 確認原副本未變，記錄在 `verification/runtime.sha256`。

**建議：** 刪除君主卡後這一頁，君主卡「確定」直接進遊戲。保留首頁語言選擇、
滾輪、鍵盤與卡片右鍵返回；玩家仍有確認機會，也少一次重複點擊。
保留頁面的收益僅是額外看一次摘要，代價是每次開局多一步。
**當時尚待使用者定案，因此 v.1.0.1 保留額外確認頁**，不能宣稱完整啟動流程已與原版相同。

## 檢查與限制

冷跑 Go vet／全套 Go 測試通過，日期中心／邊界、空列、兩段確認與取消回歸測試通過。
文件索引、過期斷言與發行檢查按專案閘門執行；新增文件造成的索引及統計過期已回填。
本輪容器均 `--rm`、無網路、以 UID/GID 1000:1000 執行；收尾檢查本輪容器無殘留。
未 commit、push、公開發布或改動 Android。音訊使用容器 null 裝置，不宣稱本輪驗過聽感。

## 後續：移除重複確認頁（v.1.0.2-20260907）

使用者明確同意「移除確認頁」後，規格 155 增補契約經 READY 再實作。
君主卡確定直接傳回目前劇本與勢力的開局要求，刪除額外頁面及其重複選取欄位；
建立失敗保留君主卡、原選取與錯誤提示。卡片取消與自定軍師沿用原路徑。

本次交付為 `dist-all/v.1.0.2-20260907/`，保留前版未覆蓋。
原版由相同 dosgolem 提交重新執行，收據在
`workplace/desktop-launcher-confirm-20260907/`，包含原始點擊腳本、日誌及截圖。
AppImage `verification/probe.py` 已刪除原本額外的 `(320,204)` 開始點擊；
`post-lord.png` 即為主地圖，`main.png` 與本輪 dosgolem **0 / 256,000 px**。
勢力反白矩形仍 **0 / 67,584 px**，四章點擊、右鍵分層返回及既有存檔不變均通過。

冷跑完整 `tools/check.sh` 通過；正常流程及非第一列勢力開局、資料失敗提示測試通過。
三平台重新建置，逐包內容、架構、雜湊與 AppImage 執行身分通過；
Windows／macOS 原生 GUI 仍未驗，Android 未改動。驗收收據在交付目錄 `verification/`。

## 2026-09-08 新版 dosgolem 複驗與讀檔修正

**目前狀態：開局／讀檔退出修正已驗證；完整操作體驗尚未完成驗收。**
主線已合併原 wolong-oracle，使用乾淨提交
`fd746405cf0ef9e535121e215b935b0bf59cd1de` 重建 dosgolem，未開修改分支。
證據根目錄 `workplace/dosgolem-main-20260908/`，原版仍為本文件所列 KI.EXE。
本輪不載入舊 dosgolem 執行檔，也未用 DOSBox 圖片替代。

| 項目 | 本輪結果 |
|---|---|
| 新舊 dosgolem 開局 | 全畫面 0 px；同一選單腳本到達系統選單的時刻由 7 時變 8 時，指令數不可當成相同虛擬時刻 |
| v.1.0.2 正常開局、勢力反白、系統原有六列 | 分別 0／0／0 px；系統選單內原版再跑一千萬道指令時鐘不變，AppImage 等兩秒畫面不變 |
| v.1.0.2 首頁讀檔 | 發現已載入 World 卻殘留 LOAD DATA，擋住系統按鈕；原版直接回地圖 |
| v.1.0.3 修正 | 成功離開啟動殼層同時清除其選槽狀態；錯誤路徑仍保留提示 |
| v.1.0.3 新遊戲 | 四章日期、反白／確認／分層返回重跑；第一章開局 0 / 256,000 px，反白 0 / 67,584 px |
| v.1.0.3 首頁讀檔 | 正常點 LOAD DATA、第一槽日期後直接回地圖；再開系統、儲存、右鍵取消成功 |
| 讀檔後畫面 | 四區 0 px，map 71 px，全部限於 `(320,224,16,16)` 游標所在格；未列全畫面 PASS |
| 儲存視窗 | 矩形 95 px，全部限於 `(376,160,14,14)` 原版箭頭位置；未列完整 0 px |
| 無效名稱欄 | 點擊前後全畫面 0 px；本輪取消儲存後 SAVE.DAT 副本未變 |

讀檔使用同一份 `workplace/dosgolem/root-noclouds/SAVE.DAT`，這是既有受控存檔，
從正常首頁載入；未用直啟或注入世界狀態。修正前截圖在上述證據根的 `load-app/`，
新版截圖與點擊軌跡在 `dist-all/v.1.0.3-20260908/verification/load/`；
開局回歸在同目錄 `normal/`，差分數字在 `*-diff.txt`。
原版取消儲存後出現 SAVE DATA 背景殘影，本輪不將它當成 remake 應複製的 UI 契約，
仍待輸入／重繪時序交叉檢查。

三平台重新建置，AppImage 正常操作與封包身分、Windows／macOS 內容及架構查核通過。
完整 `tools/check.sh` 通過；無 Android 改動、無 commit／push。本輪容器皆已退出並自動刪除。

### 後續驗收仍缺

- 真正儲存後退出重啟讀檔；本輪只驗證載入既有存檔及取消儲存，不能替代寫入生命週期。
- 正常進戰場後的戰術點選／命令／取消、拖曳與邊緣捲動節奏。
- 原生 Windows／macOS GUI、實體滑鼠延遲與聽感；容器音訊仍使用 null 裝置。
- 隱藏亂數與時鐘同狀態尚未涵蓋；不以局部像素相同宣稱全遊戲或即時速度一致。

## 存檔生命週期與日期點擊修正（v.1.0.4-20260908）

2026-09-08 從同一受控 SAVE.DAT 的第一槽正常讀入，開系統資料儲存，
點第二槽日期。dosgolem 已寫入第二槽；v.1.0.3 AppImage 只選槽，需額外按頁尾確定。
後者的新程序再點第二槽仍是空槽，確認為操作差異。規格 155 增補經 READY 後，
桌面滑鼠日期點擊改成直接執行；讀取空槽忽略。鍵盤及觸控選槽分派保留。

原版以公開 `SetScratch` API 將資料寫入隔離目錄，沒有修改執行器核心或原版記憶體。
工具 `tools/dosgolem_save_replay.go` 在 dosgolem fd74640 module 中建置，
`-root /orig -scratch /scratch -out /out` 完成正常讀第一槽、存第二槽；
另開新程序帶 `-reload` 讀第二槽。原版素材掛載唯讀。
證據在 `workplace/save-lifecycle-20260908/`，包含原版寫入 trace、存檔與重新載入截圖，
以及 v.1.0.3 失敗重播。原版儲存後留有槽窗顯示，重製則回系統並提示已儲存，
未要求玩家再確認一次。這是回饋呈現差異，不宣稱儲存後全畫面一致。

v.1.0.4 AppImage 用同一座標、無頁尾確定步驟，成功存第二槽並於新程序讀回：

| 檢查 | dosgolem | AppImage |
|---|---|---|
| 四槽各自變動 byte 數 | 0／6240／0／0 | 0／6438／0／0 |
| 第二槽名稱 | 第一章曹操／荀彧 | 同左 |
| 新程序讀第二槽 | 196 年 4 月 16 日地圖 | 同左；四區 0 px、map 71 px 游標格殘差 |
| 重啟讀取改寫 SAVE.DAT | 未觀察寫入（writes.json 空） | 前後 SHA-256 相同 |
| 原生檔 | 不適用 | SAVE-slot2.wlsave 已產生；尚未以行軍路線驗證重啟完整恢復 |

兩端未對齊隱藏時鐘與亂數，不要求 byte 完全相同，也不能把存檔差異數當成規則差異。
檢查工具 `tools/verify_save_lifecycle.py` 驗證大小、只有第二槽改寫、槽名、重啟後雜湊；
正常滑鼠與程序重啟由封包 `verification/save/`、`reload/` 的腳本、日誌和截圖證明。
原版 format 的日欄位初次檢查腳本誤讀成 u16，併入相鄰欄位；收據已訂正為低位元組，
判定日期另以原版／AppImage 實際顯示交叉驗證，未修改存檔 bytes。

新遊戲與先前流程重跑，開局仍 0 px。桌面單擊存檔測試實際檢查兩種格式寫入與原生讀回；
另驗空槽不觸發讀取。完整檢查、三平台封包身分與架構查核均需以本批收據為準。
尚待正常戰術、捲動及原生平台試玩；另需補驗首頁重啟對原生行軍路線／游標的恢復，
目前 startWorld 與遊戲內 readSave 的資料入口不同，不能以本輪靜止局面的讀回排除此缺口。

## 首頁原生存檔恢復（v.1.0.5-20260908）

已用正式 applyLauncherResult → startWorld 路徑重現：第二槽原生檔內
CityCursor=91、Routes 含兩段，v.1.0.4 首頁載入後為 0／空。
遊戲內 readSave 會優先讀原生檔，但首頁 startWorld 只用 LoadScenario。
這是既有規格 20 的入口不對稱，並非新加原版規則。

規格 155 增補 READY 後，兩者共用 loadSavedWorld；不存在原生檔才 fallback，
存在但損壞須報錯。新遊戲仍只取劇本，既有 Player 合法性與成功後清除選槽仍保留。
`TestLauncherLoadRestoresNativeRuntime` 使用獨立 writer／reader 及完整首頁分派，
修正前失敗、修正後路線與游標完整恢復；再破壞原生檔，證明原世界與選槽提示保留。
這是窄狀態測試，不冒稱玩家已正常編軍、下行軍令後重啟的完整證據。
失敗／通過記錄在 `workplace/native-restart-20260908/`。

v.1.0.5 AppImage 正常滑鼠從首頁載入原版存檔、存第二槽、結束程序、重啟讀第二槽通過。
另開新程序讀取損壞原生檔副本：留在 LOAD DATA 並顯示第二槽原生檔問題，
沒有靜默進入地圖；收據為封包 verification/corrupt/loaded.png 與操作軌跡。
此圖也顯示長錯誤細節會超出右邊界，短的原因可讀，長文排版仍需補修。

原版格式重啟生命周期再次檢查，只有第二槽變動，重啟讀取前後 SAVE.DAT 雜湊相同。
新遊戲全畫面對 dosgolem fd74640 仍 0 px。原版存檔原生擴充不存在，
因此原生狀態契約由規格與重製測試驗證，不能稱為 DOS 同狀態比對。
三平台重新建置；Windows／macOS 仍只查核封包與架構，未原生 GUI 試玩。

下一步優先正常戰術與地圖操作；行軍命令後原生重啟仍需正常玩家流程補強。
原生檔損壞提示長文裁切已列待修，Android 仍不在本輪範圍。

## 系統取消與編成確認訂正（v.1.0.6-20260908）

先前流程腳本執行成功，不等於每張返回圖都已回到預期層級。追加勘誤：
v.1.0.5 在系統選單外 (300,200) 右鍵仍停在系統，後續編成操作被阻擋；
不能沿用前文「右鍵取消」的概括宣稱。原版 fd74640 的 system-cancel.png 已回地圖。
舊圖與腳本保留，新證據見 `workplace/player-march-20260908/app/`。

按原版正常流程從 root-saveb 第一槽編成夏侯淵，另發現成軍對話不接受左鍵，
只有鍵盤能關閉。使用者已授權修正明顯缺陷；規格 155 增補經 READY 後實作。
v.1.0.6 的正常 AppImage 重播證明：右鍵先關系統、左鍵可關成軍對話，
能回到地圖並自然遇上許昌防守戰。編成視窗對原版差 79 / 46,080 px，
尚未逐點歸因，不能逕稱全部都是游標。

損壞原生檔提示換行後落在畫布內兩行，保留失敗時讀檔頁；正常存第二槽與新程序
讀回回歸通過，新遊戲開局仍 0 px。三平台封包、內部雜湊與架構檢查通過，
Windows／macOS 原生操作仍未驗。收據在 v.1.0.6 的 verification/。

### 戰術驗收方法訂正

自然遭遇的兩邊攻方不同，不能當成同狀態戰鬥；固定介面六區 0 px 只證明該批圖
的固定版面一致。首次戰術選隊、突擊與撤退都沒有有效回饋，暫不列通過。
等待 10、20 秒及延長滑鼠按住至 150 ms 的重播仍不足以排除開場等待。

只加診斷日誌的暫時編譯覆蓋（未改正式原始碼、不是正式封包收據）確認：
點擊座標正確，但軟體繪圖環境約 1.2–2.1 次更新／秒，戰鬥只到第 7–8 格，
OpeningActive 仍為 true。日誌與覆蓋來源在
`workplace/player-march-20260908/battle-diagnostic/`。
最初診斷碼未防範首頁尚無 world 而崩潰，已補條件後乾淨重跑；這是診斷工具缺陷，
不能登記成正式 AppImage 崩潰。

原版的 sub_19FA0 是開場等待之前的入口，不能用「已到此入口」代表能下令。
依既有 re/11，改等待開場之後的 sub_1A426 腳本分派；只觀測執行位址，
不強制跳入、不改記憶體或跳過繪圖。AppImage 另以正常系統選單調整戰術速度重跑。
完成前仍不得宣稱戰術操作一致或實體手感通過。

## 戰術喊話與命令修正驗收（v.1.0.7-20260908）

原版等待 sub_1A426 後，在喊話仍顯示時選隊已出現選取框；直接點突擊立即換命令旗。
v.1.0.6 卻先把左鍵吃成關喊話，證明多了一次確認。經規格 155 增補 READY，
advanceBattleTalkInput 不再截走左鍵，保留 Enter／Space 及開場／單挑輸入閘。

正式 AppImage 的 verification/battle-ready/ 從首頁正常讀檔、編軍、調速、自然遭遇，
偵測到喊話外框才下令。軟體繪圖環境需將單次按住延長為 2 秒，避免輸入取樣漏掉；
每次只有一次按下與放開，腳本與單調時間軌跡都保留。結果如下：

| 動作 | 可見結果 | 對原版量測 |
|---|---|---|
| 喊話仍在時點左翼 | 喊話保留，左翼選取框出現 | 選取框頂邊 0 px |
| 點突擊一次 | 僅左翼改突擊，選取框清除 | 單隊結果目視確認，規格 33 的選取語意 |
| 再點突擊 | 無選隊時六隊都改突擊 | 六張命令圖示各 0 px |
| 點退卻一次 | 六隊都改白旗 | 六張命令圖示各 0 px |

這份收據確認選隊／下令不再多吃一次點擊；未涵蓋戰後返回，也不證明實體短按延遲。
原版攻方呂布與 AppImage 的自然攻方不同，背景、喊話變體、亂數與戰況不得當成同狀態。

新遊戲全畫面 0 px、系統右鍵回地圖、編成對話左鍵、錯誤訊息換行再次目視通過。
正常存第二槽及程序重啟讀回通過；兩邊各自只有第二槽變動，重啟讀檔不改寫 SAVE.DAT。
最初檢查誤將 root-saveb 與原版 root-noclouds 的存檔混比；已以 root-noclouds
乾淨重跑正式 AppImage 存／讀流程。這是驗證輸入選錯，沒有修改原版收據或遊戲資料來湊結果。
Windows／macOS 封包內容、雜湊與架構通過，原生操作未驗。完整檢查與封包身分見本版 verification/。

下一步仍為正常行軍後重啟、地圖捲動與戰後返回；本文件不是全遊戲體驗一致的完成宣告。
