# 53 — 實機：戰場的 `▶▶` 按下去會怎樣

**狀態：PASS（行為 confirmed）。按一下戰場整片變成均勻底紋、側欄與小地圖照動；再按一下戰場回來。**

- 日期：2026-08-29
- 原版側：`WOLONG_DOSV_SEED_SAVE=SAVE-FIELD.DAT tools/dosv_capture.sh parity-ffwd "…;wait:16;shot:b0;click:560,461;wait:2;shot:ff1;wait:4;shot:ff2;click:560,461;wait:2;shot:ff3;…"`
  （`▶▶` 熱區 (496,376) 128×16 → 遊戲 (560,384) → 視窗 y×1.2 ＝ 461）
- 輸出：`workplace/promo-live/parity-ffwd/{b0,ff1,ff2,ff3,ff4}.png`

## 1. 看到什麼

| 幀 | 內容 |
|---|---|
| `b0` | 野戰開場：曹操 對 呂布，兩個對白框，戰場地形 |
| `ff1`（按下 2 秒）| **戰場區整片換成藍底綠線的菱形底紋**，對白框仍在，小地圖上部隊點在動，`▶▶` 那格描成黃框 |
| `ff2`（+4 秒）| 同上，小地圖的點又移了——**戰鬥在背景跑** |
| `ff3`（再按一下 +2 秒）| 戰場回來，對白已換成「突擊！一不也不能後退！」，位置與時間都往前走了 |

結論與機器碼一致（[`../spec/102`](../spec/102-battle-fast-forward.md) §1）：
`▶▶` ＝ 跳過戰場重畫的快轉。

## 2. 底紋

`ff1` 戰場區的底紋是 32 px 週期的菱形格（藍底、綠線），**不是**視窗底的龍紋。
它是 `sub_1DC9D` 從 `es:0x2080`（戰場資料之後）讀到的位元組畫成圖塊的結果，
那一段記憶體是什麼沒解（`spec/102` §5）。remake 鋪龍紋，標為差異。

## 3. 按下框

`b0` 與 `ff1` 的 `▶▶` 格差 313 px：開著時外圈描成色 12（黃），
關掉後（`ff3`）描回色 10（紅）——與 handler `0x1C24E`／`0x1C277` 的 `ah` 對上。
