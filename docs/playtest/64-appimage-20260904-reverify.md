# 64 — 2026-09-04 的 AppImage 複驗：四個關卡對原版 0 px

**狀態：通過。** 把這兩天的四批規則改動（[`../spec/127`](../spec/127-captured-sovereign-becomes-retainer.md)–[`130`](../spec/130-freelance-random-join.md)）
打成 AppImage 之後再驗一次。**勢力清單本體、君主卡、指令列三個 rect 對原版各 0 px**，
另外三張與 9/2 那一批 remake 產物**整張 640×400 逐像素相同**。
⚠ 六個關卡裡的**進言上框與編成面板沒驗到**——這條操作序列走不到那兩個畫面（§4）。

- 日期：2026-09-04
- remake 側：`dist-all/packages/wolong-remake-linux-amd64-20260904.AppImage`
  SHA-256 `a852a2d9bf38d8412ae26d7e47eb725031a2bf934936d276652c24c7c11f7408`

  ⭐ **判斷「驗的是哪一顆」要看包裡的執行檔**：解開之後
  `usr/bin/wlgame` 的 SHA-256 是
  `e28c510ffcc29a09f0d0ebf1203b4ba0590b23a8fa48906652292b7affb199f2`。
  AppImage 外層每次重打都會變（squashfs 帶時間戳），執行檔不會
  （[`57`](57-appimage-20260902-reverify.md)）。
  ⚠ 這兩個雜湊要**分開兩段寫**——`stale_scan` 的雜湊層是逐行配對
  「檔名 … SHA-256 …」，兩個雜湊擠在同一段會讓它把執行檔的那一個
  配到 AppImage 的檔名上（本輪初稿就被擋下來一次）
- 原版側：`docs/images/lubu-orig-*.png`，沿用 [`56`](56-lubu-flow-parity.md) 的那一批
- 打包：`tools/release_appimage.sh 20260904`（只重打 Linux amd64）

## 1. 包裝層：五個符號，正負對照都乾淨

這兩天的改動**沒有新增任何玩家看得到的字串**，所以 [`57`](57-appimage-20260902-reverify.md) §1.1
那招（找新字串）用不了。改用**這兩天新增的 Go 函式名**——它們在符號表裡：

| 符號 | 20260904 | 20260902（負對照）|
|---|---:|---:|
| `randomJoin` | 2 | **0** |
| `fewestGeneralsFaction` | 2 | **0** |
| `postBattleMorale` | 1 | **0** |
| `applySquadLeaderGone` | 2 | **0** |
| `demoteCapturedSovereign` | 1 | **0** |

⭐ **五個負對照全部是真的零**，所以這一組有鑑別力——不像 `57` §1.1 警告的
「兩包都有，看起來都對」那種自我確認。

## 2. 行為層

### 2.1 對原版

| 關卡 | rect | [`57`](57-appimage-20260902-reverify.md) | 本輪 | 判定 |
|---|---|---:|---:|---|
| 勢力清單本體 | `152,104,368,160` | 0 / 58,880 | **0 / 58,880** | PASS |
| 君主卡 | `160,112,240,192` | 0 / 46,080 | **0 / 46,080** | PASS |
| 指令列 | `0,32,432,32` | 0 / 13,824 | **0 / 13,824** | PASS |

### 2.2 與 9/2 那一批 remake 產物互比（整張 640×400）

| 關卡 | 差異 |
|---|---:|
| 勢力清單 | **0 / 256,000** |
| 君主卡 | **0 / 256,000** |
| 行軍目標 | **0 / 256,000** |

### 2.3 可重現性（正對照）

同一顆 AppImage、同一條時間軸**跑兩次**，六張截圖**全部 0 px**。
⭐ 這一步是必要的：沒有它就分不出「畫面變了」與「即時制每次跑到的時間點不同」。

## 3. ⚠ 兩個量測坑

### 3.1 拿了被推翻的舊截圖當基準

第一次比對用 `workplace/promo-live/flow-remake-05/`（9/1 18:15），
勢力清單差 **20.93%**，看起來像大回歸。實際上那一批是
[`56`](56-lubu-flow-parity.md) §4.1 **修正之前**的截圖——黑底、沒有「－－－」、
數字欄左偏，五項全在 `CONTEXT.md` 的推翻表裡。

⭐ **`workplace/` 底下的舊擷取沒有「已被推翻」的標記**，
而檔名（`flow-remake-05`）不帶日期也不帶狀態。
版控裡的 `docs/images/lubu-remake-*-fixed.png` 才是修正後的那一版——
**檔名帶 `-fixed` 是唯一的線索**。

### 3.2 指令列那一張現在要滑鼠

`key:p` 這條時間軸拍不到指令列開著的畫面——**鍵盤捷徑 `1`–`4` 在 2026-09-02
被拿掉了**（使用者裁定「臥龍傳是滑鼠遊戲」，[`../spec/13`](../spec/13-main-window-toggles.md) §3）。
補拍要用 `click:352,16`（熱區 1 的中心，矩形 `(336,0,32,32)`，[`../re/47`](../re/47-main-screen-window-registry.md) §2）。

## 4. 未解

| 項目 | 現況 | 下手點 |
|---|---|---|
| 進言上框 `0,80,256,80` | **沒驗到**。這條時間軸的 `key:p` 開的是進言五項選單，不是說服理由那一頁 | 要走到「說服 → 選理由」，序列見 [`56`](56-lubu-flow-parity.md) §2.2 的後半 |
| 編成面板 `144,112,240,192` | **沒驗到**，同上——編成是另一條流程 | 同上 |
| 軍團選單 | [`57`](57-appimage-20260902-reverify.md) §3 記著原版那張是另一個局面（4/20、讀了存檔），本輪沒重試 | 用 [`60`](60-corps-menu-parity.md) 的原版素材，那一張是同狀態的 |
