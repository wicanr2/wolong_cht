# 63 — 說明書 11 章逐章盤點：哪些對過原版、用什麼方式對的

**狀態：盤點，不是量測。** 回答一個先前沒有被系統性問過的問題——
**說明書上的功能是不是都對拍過**。

答案分兩層：

1. **沒有。** 沒有任何一份文件是按說明書章節組織驗收的，這一份是第一次。
2. ⭐ **而且「都對拍」本來就不是正確的目標**——說明書 11 章裡只有第 2–6 章
   講畫面，第 7–11 章講的是**規則**（外交判定、災害觸發、戰術攻略），
   規則沒有畫面可以逐像素比，只能用機器碼或實跑對照驗。
   把它們算成「沒對拍」會低估，算成「對拍過」會說謊。

- 日期：2026-09-04
- 說明書判讀：[`../reference/01`](../reference/01-jp-manual.md)（38 頁，
  有實質機制的頁都讀完，剩 p.6 啟動操作與 p.36–38 附錄）
- 證據等級四種，定義見 §1

## 1. 四種證據等級

| 等級 | 意思 |
|---|---|
| **逐像素** | 原版截圖與 remake 截圖同狀態逐像素比過，有 `docs/playtest/` 的數字 |
| **實跑對照** | 兩邊各跑一次比行為（長跑事件、時間序列），不是比像素 |
| **機器碼** | 規則從反組譯讀出來並實作，`docs/spec/` 是 CONFORMED，但**沒有跟原版比過行為** |
| **未驗** | 只有說明書的敘述，remake 照它做或還沒做 |

⭐ **「機器碼 ＋ 程式碼真的做了」就算 CONFORMED**（使用者裁定 2026-09-04，
起因是第 9 章災害）。判準是**逐項查程式碼**，不是查文件的狀態行——
要看到擲骰、接線、傷害量、測試四樣都在，缺一樣就不算。
實機觀察仍然有價值，但**不是升級的必要條件**。

⚠ **機器碼等級不等於對過原版。** 它保證「照著原版的程式碼寫」，
不保證「跑起來一樣」——這兩件事之間隔著接線、時序與呼叫端，
而這個專案這兩天連續抓到三件都是接線出錯
（`docs/spec/128`、`129`、`playtest/62`）。

## 2. 逐章

| 章 | 內容 | 最高證據 | 在哪 |
|---|---|---|---|
| **1** 目的與終了條件 | 勝負判定、Game Over | ✅ **CONFORMED** | `internal/state/outcome.go` 的三個離開碼（`Victory`／`DefeatTrustZero`／`DefeatFactionEliminated`）＋ [`../spec/30`](../spec/30-victory.md)、[`../re/59`](../re/59-game-over-exit-codes.md)。結局播放 [`../spec/67`](../spec/67-ending-playback.md)。⚠ 結局畫面**沒有與原版並排比過** |
| **2** 開新遊戲／續玩／軍師作成 | 劇本選擇、勢力清單、君主卡、軍師命名 | **逐像素** | [`56`](56-lubu-flow-parity.md)：君主卡 0 px、勢力清單本體 0 px、指令列 0 px。軍師命名視窗（[`../spec/104`](../spec/104-advisor-naming-window.md)）有截圖，**沒有逐像素比** |
| **3** 主視窗與命令視窗 | 主畫面、四個視窗、九個指令、一覽表、說服 | **逐像素** | [`37`](37-main-screen-parity.md) 五區 0 px、[`38`](38-window-parity.md)、[`39`](39-system-window-parity.md)、[`42`](42-window-parity.md) 四張清單 ＋ 財政 ＋ 數值輸入器 0 px、[`60`](60-corps-menu-parity.md)／[`61`](61-city-personnel-menu-parity.md) 三張彈出選單 0 px。⚠ **進言那一張確定拍不到**（[`42`](42-window-parity.md) §5）|
| **4** 戰術基本系統 | 1:10 換算、勝負條件、命令、陣形、地圖移動 | **逐像素** | [`43`](43-field-battle-parity.md) 野戰七區 0 px、[`40`](40-tactical-parity.md) 攻城、[`62`](62-parity-retest-20260903.md) 最新一輪 |
| **5** 戰略參數 | 自勢力／財政／武將／據點／軍團的欄位 | **逐像素** | [`42`](42-window-parity.md)：四張清單與財政視窗逐欄一致 |
| **6** 戰術參數 | 側欄七格的顯示資訊 | **逐像素** | [`40`](40-tactical-parity.md)：側欄七區裡六區 0 px |
| **7** 外交 | 外交全般、外交官任命、停戰、協力 | ✅ **CONFORMED** | `internal/rules/diplomacy`（`Friendship` 的和平位元 ＋ 值、六階 `Level`、`DisplayIndex`、`Diplomat.Tick`，**12 支測試**）＋ `state/events.go` 的 `QueuePlayerCeasefire`（事件 6）／`QueuePlayerCooperation`（事件 7）與事件 3 的完成端。[`../mechanics/50`](../mechanics/50-diplomacy.md)。⚠ 行為**沒有與原版對照過** |
| **8** 內政 | 內政全般、內政官任命 | ✅ **CONFORMED** | `internal/rules/governor`（`Tick`，6 支測試）＋ `internal/rules/economy`（月結、稅率、生產力複利，**35 支測試**）。[`../mechanics/10`](../mechanics/10-strategy.md)、[`../mechanics/40`](../mechanics/40-economy.md) |
| **9** 災害 | 暴風雨／火災／暴動、被害量 | ✅ **CONFORMED** | 2026-09-04 逐項查過程式碼（§2.1）：三種災害的擲骰、月結接線、傷害量、呈現、12 支測試全在，連原版「靠海」判定的低位元組 bug 都照抄。[`../spec/81`](../spec/81-disaster-quantities.md) |
| **10** 戰略攻略 | 停戰連招、協力準備期、預備兵、武術＋統率、行軍費用 | ✅ **CONFORMED** | 10.1／10.2 併在第 7 章；10.4 預備兵 ＝ `state/corps.go` `distributeReserves` ＋ `strategy.go` `autoFormCorps`；10.6 行軍費用 ＝ `combat.Upkeep` ＋ `corps.go` `upkeepHour`。⭐ **10.5「武術と統率の合計が同じなら強さは同じ」查證後不成立**——那是評價的性質，戰力同和時武力偏重的一方較高（[`../mechanics/30`](../mechanics/30-combat.md) §4.2）|
| **11** 戰術攻略 | 突擊時機、陣形有利不利、擊破狀態、中央突破、追擊、回り込み、誘餌、從內側開門 | ✅ **CONFORMED** | 見 §2.2——基礎機制全在（疲勞度、有利／不利、19 個腳本 opcode、守方突擊拆城壁），**108 支測試**。⚠ 這一章多半是**玩家打法**不是系統功能，「有沒有做」問的是它依賴的機制在不在 |

### 2.1 第 9 章災害：逐項查程式碼的結果

| 項目 | 位置 |
|---|---|
| 火災／暴動的擲骰 | `internal/rules/economy/disaster.go` `RollCityDisaster`——火災看防災值、暴動看上昇值，兩道閘各自 `rng & 0xFF < disasterGate` 再 `rng & 0x3F >= 門檻` |
| 暴風雨 | 同檔 `RollStorm` ＋ `StormArea`（以中心據點為準的 11 × 11 格）|
| **接線** | `internal/state/state.go` 月結 ③：逐據點擲 `RollCityDisaster`、擲 `RollStorm`，再寫進事件佇列（事件 11 ／ `0x010C` ／ `0x020C`）|
| 傷害量 | `internal/state/events.go` 的 `applyQueuedStormMarker`／`applyQueuedDisasterMarker` → `applyCityDisasterEffect`（[`../spec/81`](../spec/81-disaster-quantities.md) §3）|
| 呈現 | `cmd/wlgame/main.go` `drawStormArea` |
| 世界指紋 | `stormArea` 進 `Fingerprint()`，所以它是可重現狀態的一部分 |
| 測試 | 12 支（`TestDisasterGate`／`TestFireTakesPrecedence`／`TestRiotOnly`／`TestStormArea`／`TestStormFirstGate`／`TestDisasterMarkerAppliesRawPersistentEffects` …）全 PASS |

⭐ **原版的 bug 也照抄了**：「靠海」判定比的是 X 座標的**低位元組**
（`cmp byte ptr [bx+848h], 0C0h`），結果 `X mod 256 ≥ 192` 才算靠海——
最東邊 49 個據點反而被判成內陸。`disaster.go` 的 `coastalThreshold`
把成因寫在註解裡，並指出「要改成真正的東半部只需換一行」。

> ⚠ **查證時踩到一個量測坑，記在這裡。** 第一次查呼叫端用
> `grep … | head -12`，輸出剛好被截在第 12 行，於是看不到
> `state.go:1039` 那個真正的呼叫點——差一步就要寫成「接線沒接」。
> **用 `head` 截斷之後不能下「沒有」的結論**，那與
> `~/diagnosis-notes` 的「查詢回空要先做正對照」是同一件事。

### 2.2 第 11 章：問的是「它依賴的機制在不在」

11.1–11.9 多半是**玩家打法**（中央突破、回り込み、誘餌），不是可以獨立
實作的系統功能。所以逐項查的是它們依賴的機制：

| 說明書 | 依賴的機制 | remake |
|---|---|---|
| **疲勞度**（6.1 定義、11.1／11.5 用到）| 兵回到陣形位置時歸最小、移動每幀 −1、攻擊時有上限 | ✅ `tactical.Soldier.Stamina`：`StaminaFull = 128`、`StaminaFighting`、`StaminaBackToForm`，移動端 `s.Stamina--`（`sub_1ADC8`）|
| **11.2 陣形的有利／不利** | 三階（有利／普通／不利），差距 ≤ 8 算普通 | ✅ `computeAdvantage`（`sub_1ADC8` 尾段）＋ `advantageBias = 7`／`evenBand` |
| **11.1 突擊／攻擊的時機** | 突擊、攻擊、陣形三個命令與接觸判定 | ✅ `Charge`／`Attack`／`Form` ＋ `attackCollision` |
| **11.4／11.6／11.7** 中央突破・回り込み・誘餌 | 16 種陣形變更 ＋ 陣形線移動 | ✅ `Formations`（16 種 × 48 組座標）＋ [`../spec/37`](../spec/37-tactical-player-controls.md)（兩個熱區的矩形與算式，CONFORMED）|
| **11.5 追擊**（「敵人會因疲勞後退，趁它重整前壓上」）| ⭐ **這是腳本的行為，不是硬寫的規則** | ✅ `script.go` 把 `BATTLE.DAT` 的**19 個 opcode 全部實作**（查詢有利／不利、雙方命令最小值、剩餘兵力、城壁耐久…）。remake 跑的是原版同一份腳本資料 |
| **11.8 從內側開門** | 守方下突擊令 ⇒ 場上完好的城壁一次全毀且不可還原 | ✅ `battle.go` 的 `Sortied` ＋ `Order` 的側別閘（`sub_1B7CB`，只挑實體類型 1，門不在裡面）|
| **11.3 擊破狀態** | 「推進到對方畫面邊緣，新兵一出陣就挨打」 | ⚠ 這是**現象不是機制**——它是補兵位置 ＋ 攻擊判定的自然結果，沒有對應的獨立程式碼 |

## 3. ⭐ 結論：畫面對得很細，規則多半只到機器碼

| 層 | 章 | 狀態 |
|---|---|---|
| **畫面** | 2–6 | 大量逐像素對拍，多數 0 px |
| **規則** | 1、7、8、9、10、11 | ✅ **全部 CONFORMED**（2026-09-04 逐章查過程式碼）|

⭐ **逐章查完的結論與查之前的印象不一樣。** 前一版把第 1、7、8、10、11 章
都標成「只到機器碼等級、沒有一項做過與原版的行為對照」——
**那個判斷是從文件的狀態行推的**，逐項查程式碼之後，
六章的規則本體、接線與測試都在（規則層六個模組共 208 支測試）。

⚠ **但這是「有沒有做」的查核，不是「做對了沒」。** 這兩天抓到的三件
（`spec/128`、`129`、`playtest/62`）**全部都是「有做但接線或算式錯」**，
而它們在這種查核下都會被判成「有」。要回答「做對了沒」只有兩條路：
逐行對機器碼，或與原版做行為對照。

唯一的例外是 [`45`](45-ai-longrun-comparison.md)（AI 長程對照）：
原版半年的三大擴張事件與 remake 方向幅度一致，
呂布／曹操終值逐城相同——**那是目前唯一一次規則層的實跑對照**，
而它只涵蓋第 10 章的一小塊（AI 的侵攻決策）。

## 4. 未解

| 項目 | 現況 | 下手點 |
|---|---|---|
| 「有做」與「做對了」的差距 | 這一份只證明前者。後者要逐行對機器碼或與原版做行為對照——而規則層的行為對照目前只有 [`45`](45-ai-longrun-comparison.md) 一次 | 再跑一輪 AI 長程對照，或針對單一機制設計同狀態實驗 |
| 第 11.3「擊破狀態」 | 是現象不是機制，沒有獨立程式碼。**沒有驗過 remake 會不會產生同樣的現象** | 要設計一個「把對方壓到邊緣」的局面，看新兵出陣是否立刻挨打 |
| 三種災害的實機觀察 | 仍然沒有，但**依裁定不擋 CONFORMED**。要做的話：災害是機率觸發的，長跑等它或找一個災害剛發生的存檔，`spec/81` 的數值可以當預期值 |
| 第 7 章外交的行為對照 | 停戰／協力的成立與否沒有跟原版比過 | 同狀態存檔 ＋ 兩邊各下同一個提案，比結果 |
| 第 11 章的戰術攻略規則 | 追擊／回り込み／誘餌／疲勞度沒有實跑證據 | 這幾條是**玩家操作技巧**，要設計特定局面才觀察得到 |
| 第 1 章結局播放 | 有 remake 截圖，沒有與原版並排 | 原版要打到結局，或用存檔跳到最後一場 |
| 軍師命名視窗的逐像素 | 有截圖沒有比 | 原版側 `tap` 到那一頁，照 [`54`](54-menu-second-row-tap.md) |
