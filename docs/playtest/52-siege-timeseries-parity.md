# 52 — 攻城的時間序列對拍：原版七張影格 vs remake 的規則層

**狀態：攻城計時器對得上；攻方怎麼進城還沒對上。**
⭐ 大將體力兩邊都線性下降、六段之間沒有系統性漂移。
⛔ 原版全程**沒有打城壁**（門強度條七張都不在），第六張就已經與守方交手；
remake 的登城部隊全部撞在城壁上，一路把耐久磨到 0 才進得去。
順著這條線讀機器碼修掉了**三處**（[`../spec/97`](../spec/97-climb-when-both-axes-blocked.md)、
[`98`](../spec/98-climb-into-a-gate-hits-it.md)），**但這條 fixture 的行為沒有變**——
成因不在那三處，見 §6。

- 日期：2026-08-27
- 規格：[`../spec/91`](../spec/91-tactical-parity.md)（分區與同狀態）、
  [`../spec/36`](../spec/36-ground-planes-and-climbing.md) §1.4（登城的四個條件）
- 原版側：`workplace/promo-live/parity-battle4/` 的 `t1`–`t16`
- fixture：**同一次擷取存下來的 `SAVE-L.DAT`**（196年5月10日）

## 1. ⭐ 這一組的價值：存檔與影格出自同一次擷取

[`51`](51-siege-deadlock.md) 用的是 `probe-march/e10.png`，而那一次擷取
**沒有存檔**——只能拿另一天（5月10日）的存檔去對 5月20日的畫面，
於是三個計量條的**基準值全部未知**：城壁耐久是 `(城兵數 + 50) × 10`
（[`../re/11`](../re/11-tactical-battle.md) §5.11），而城兵數十天之內會變；
攻方體力的初值是軍團士氣，也會變。**差異的數字要配基準值才有語意。**

`parity-battle4` 沒有這個問題。它的時間線是：

```
… savefile:SAVE-L.DAT; rclick…; wait:6; wait:6;
shot:t1; wait:6; shot:t2; … wait:6; shot:t16
```

**存檔先落地，然後每隔一個 `wait:6` 拍一張。** 基準值就在手上：

| | 值 |
|---|---|
| `SAVE-L.DAT` | 196年5月10日 |
| 攻方 軍團 35 呂布 | 兵 600、士氣 200 |
| 守方 軍團 39 夏侯惇 | 兵 600、士氣 200 |
| 許昌 城兵 | 116 → **城壁耐久 1,660** |

`t1` 的側欄寫著「許昌 作戰／曹操 對呂布」「呂布 軍」「夏侯惇軍」——
與存檔對得上。

## 2. 原版的七張影格（`t8` 之後戰鬥已結束，數值凍住）

四條計量條的**像素長度**（算式見 [`../spec/31`](../spec/31-tactical-sidebar.md) §2.3，
上限 124），門強度條見 [`../spec/32`](../spec/32-gate-strength-bar.md)：

| 影格 | 攻方兵力 | 攻方體力 | 守方兵力 | 守方體力 | 門強度 |
|---|---:|---:|---:|---:|---:|
| t1 | 124 | 100 | 124 | 124 | **0** |
| t2 | 124 | 96 | 124 | 124 | **0** |
| t3 | 123 | 91 | 124 | 124 | **0** |
| t4 | 123 | 88 | 124 | 124 | **0** |
| t5 | 123 | 84 | 124 | 124 | **0** |
| t6 | 122 | 79 | **106** | **85** | **0** |
| t7 | 118 | 75 | **105** | **37** | **0** |

⭐ **門強度條七張全是 0**——不是量錯：把 `t6` 的上緣裁出來看，
那一整條是地形，沒有視窗。**原版的攻方從頭到尾沒有打城壁。**

⭐ **t6 起兩軍在交手**：守方大將的體力兩段之內從 124 掉到 37，
雙方兵力同時開始掉。

## 3. remake 的同一條 fixture

`-siege-corps 35,39`（規則層直接跑，不經畫面）：

| frame | 攻方兵力 | 攻方體力 | 守方兵力 | 守方體力 | 門強度 | 城壁 |
|---:|---:|---:|---:|---:|---:|---:|
| 250 | 124 | 124 | 124 | 124 | **102** | 1,639 |
| 500 | 124 | 112 | 124 | 124 | 72 | 1,164 |
| 750 | 124 | 93 | 124 | 124 | 42 | 683 |
| 1000 | 124 | 75 | 124 | 124 | 10 | 163 |
| 1100 | 124 | 67 | 124 | 124 | 0 | **0** |
| 1116 | 124 | 66 | **0** | **0** | 0 | 0 |

## 4. 對得上的：攻城計時器

用攻方體力當時鐘（它由 `sub_1AE56` 每 10 幀扣 1，是兩邊都確定的機制）：

| | 體力條 100 → 75 |
|---|---|
| 原版 | t1 → t7，六個 `wait:6` |
| remake | 約第 662 → 1,000 幀，**338 幀** |

⇒ **一個 `wait:6` ≈ 56 個 remake 幀**，六段之間沒有系統性漂移。
斜率對得上——**攻城計時器這一條是通的**。

## 5. ⛔ 對不上的：攻方怎麼進城

把同樣那 338 幀擺在一起看：

| | 原版（t1→t7）| remake（f662→f1000）|
|---|---|---|
| 門強度條 | **一次都沒出現** | **一路顯示**（54 → 10）|
| 城壁 | 沒被打 | 877 → 163（**掉了 714**）|
| 兩軍接觸 | **t6 就交手**（守方體力 124→37）| **一次都沒有** |
| 攻方兵力 | 124 → 118 | 124（沒掉）|

**腳本沒有問題。** 把呂布在據點 82 攻城段的腳本攤開，第 98–102 個 byte 是：

```
 98: orderBy 參數2 運算元3    ⭐ 兵種 2 → 命令 3（城壁／登城）
100: orderBy 參數3 運算元3    ⭐ 兵種 3 → 命令 3
102: orderBy 參數1 運算元2    兵種 1 → 命令 2（突擊）
```

remake 也照著跑了——第 250 幀起攻方的命令分布是
`{2: 8, 3: 32, 7: 8}`，**32 個兵確實拿到了登城命令**。

⭐ **問題在登城的目標格。** 原版 `loc_1AB39` 把命令 3 的目標設成
**(索引第二欄, 自己的 Y)**（[`../re/11`](../re/11-tactical-battle.md) §5.8i），
而這張圖翻轉後那個 X 是 **27**——量過那一欄：

```
x=27 y=32   低平面地面層 (無)   高平面 (無)   門格=false
```

**那一格沒有地面，它就是城壁本體。** 於是 32 個登城兵走到 x=28 就撞上去，
`tryMove` 把每一次撞擊算成一點耐久（`hitStructure`），門強度條因此一直亮著，
城壁被磨到 0——**remake 是把城牆敲穿進去的，原版不是。**

而這張圖的門在 **x = 31（y 20／41）與 x = 34（y 6／54）**，
四格都不在 x = 27 那一欄。

## 6. 讀了機器碼之後：修掉三處，而 fixture 沒有變

`tools/ida.sh script dosv tools/ida_range.py` dump 了移動分派與登城那幾支，
對出三個 remake 與原版不一樣的地方，都改掉了：

| # | 原版 | remake（修前）| 出處 |
|---|---|---|---|
| 1 | 純 Z 移動的觸發只要「X 與 Y 這一幀都沒走成」 | 還要求 X、Y **已經等於目標** | `sub_1AF69` 的 `jb` 鏈 |
| 2 | 命令 3 的目標 Z 取**高平面**那一格的地面層（牆頂）| 取腳下的地面層 | `loc_1AB39` 的 `bh \|= 10h` |
| 3 | 爬不上去時若上一層有實體 → 走碰撞處理**打它** | 直接放棄 | `sub_1B0D3` 的 `and al, al / jnz loc_1B533` |

⚠ **三處都修好之後，這條 fixture 的每一個數字都沒有動。** 成因在別處：

| 這張圖的門 | 位置 |
|---|---|
| 未破的門（`0xF0`–`0xF7`，攻方那一側）| **4 格**：(34,6)、(31,20)、(31,41)、(34,54) |
| 已破／可爬（`0xF8`+，城內）| 8 格：x ＝ 24 或 26，y 4–56 |

而**登城部隊的 Y 全部落在 28–35**（`form 4`／`5`／`6` 這三個陣形的 Y 帶），
命令 3 的目標又是 **(登城點 X, 自己的 Y)**——Y 不會變。
**所以沒有一個兵走得到門格**，而 x ＝ 27 那一欄在 y 29–31 正好是城壁，
於是撞上去、把它磨穿。

⭐ **那三處是真的對錯了，只是不是這一條的成因。**

## 7. 未解

| 項目 | 現況 | 下手點 |
|---|---|---|
| ⭐ **登城部隊走不到門格** | 門在 y 6／20／41／54，而部隊的 y 帶是 28–35（§6）。命令 3 不改 y，所以在這張圖上永遠碰不到門 | 三個方向：(a) 陣形是不是選錯了——腳本下的是 `form 4`／`5`／`6`，而 y 帶由陣形表決定；(b) `sub_1CAEB` 寫進立即值的是**未翻轉**的索引第二欄（`0001CB0F` 的 `mov al, es:[bx+1]`），而 remake 對它套了 `RotateGateX`——翻轉的圖上兩者差 `63 − x`；(c) 繞路（`loc_1BD46`）會不會把兵帶到門那一列 |
| 索引第二欄到底是什麼 | `../re/11` §4.5 稱它「城門附近的 X」，§5.8i 定案為「命令 3 的目標 X」。但這張圖上它落在城壁本體、離最近的門有 3–7 格 | 掃 186 張攻城圖：第二欄那一欄的圖塊值是什麼、與門的距離分布 |
| 守方為什麼在 t6 掉那麼快 | 原版守方大將體力兩段內 124 → 37 | 要先讓攻方以原版的方式進城，才比得了 |
