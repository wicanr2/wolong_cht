# 112 — 戰後結果頁預設關閉與限時摘要

**狀態：v.1.0.12-20260908 AppImage 正常操作通過；Windows／macOS 封包查核通過，原生 GUI 未驗。**

- 日期：2026-09-08。
- 範圍：AppImage／Windows／macOS 桌面程式，Android 不變。
- 使用者決定：戰後結果頁改成選項、預設關閉，開啟可設定秒數後自動關閉。
- 規格：[89 §7](../spec/89-siege-damage-report-toggle.md)。

## 原版與重製差異

原版正常退卻後直接回到大地圖，沒有結果確認頁。
證據在 `workplace/battle-return-20260908/original/timeline.txt`、oracle.log 與截圖；
dosgolem fd746405cf0ef9e535121e215b935b0bf59cd1de，dosv KI.EXE
SHA-256 fffeba985231cda4d636e93d10f598470b1f691d00275e4aa38e285893d43868。
這是動態正常操作證據，不新增反組譯位址或規則推論。
原版與本輪 remake 自然遭遇的攻方可能不同，因此只比較返回操作，不宣稱完整戰況同狀態。

## 設定與計時

系統選單第九列「戰後結果」依序為關、3 秒、5 秒、10 秒、15 秒、30 秒。
預設關閉，戰鬥完成後自動結算；開啟時從第一次繪製結果頁開始計時，
到期自動返回，也能用新的點擊、Enter 或 Space 提前關閉。
計時採實際單調時間，不採戰術 tick、遊戲日期或顯示更新數。
原版六列的位置保持不變，新列和外框均留在 640×400 畫布內。
設定沿用既有工作階段設定，不新增偏好檔或更改存檔；原有損害報告只控制額外一行。

## 候選程式驗證

`workplace/battle-return-20260908/options/three-seconds/` 從正常讀檔、系統設定、編軍，
自然遭遇後退卻。`post-009.png` 到 `post-012.png` 顯示結果頁，`post-013.png`
已回大地圖；戰後取樣階段沒有任何鍵鼠輸入。
依截圖單調時間，顯示時長落在約 2.40–3.95 秒的觀測區間，包含設定的 3 秒。
精確的到期邊界另由固定時間參數的測試驗證；不能用截圖間隔宣稱毫秒級實跑精度。

桌面測試涵蓋：預設關閉立即通過、首次顯示前不接受舊輸入、到期邊界、提前關閉、
重繪不重設倒數、下一場重設，以及原版六列位置與新增第九列邊界。
初次預設關閉實跑未走到戰鬥即被中止，保存在 `options/off/interrupted-startup/`；
此組不作通過證據，另以每步進度與命令逾時重新驗證。

## 自評更新

README 自評改為明列八個評估軸及權重，加權 84.7 四捨五入為 85／100。
新增桌面操作軸；規則仍為 83，資料、文字、逆向及音訊未因 UI 工作加分。
畫面 97 保留非整格 1 像素殘差，平台 60 保留 Windows／macOS 原生驗收缺口。
新權重不同於舊總分，不能把分差當成完成率；歷史自評已保存至 WORKLIST。


## 正式交付收據

`dist-all/v.1.0.12-20260908/verification/` 直接執行新 AppImage，沒有替換程式：

- `off/`：預設關閉，45 張戰後取樣皆無結果頁，無任何追加輸入即返回大地圖。
- `three-seconds/`：第 10–12 張取樣顯示結果頁，第 13 張已返回，戰後無追加輸入。
  截圖時間界定約 2.15–4.41 秒，包含設定的 3 秒；實跑不宣稱毫秒級精度。
- `normal-input-check.json`、各案例輸入紀錄與時間收據保存驗收範圍；
  `package-check.json` 查核 AppImage 實跑程式及 Windows／macOS 架構與內含檔案雜湊。
- `source-check.json` 確認 347 個建置來源與工作樹一致；最終完整檢查保存於交付驗證目錄。

提前點擊與連續兩場重設由單元測試覆蓋；本次正常 GUI 抽樣專注兩條使用者要求的
無確認路徑，不以單元測試冒充其他實機路徑。
