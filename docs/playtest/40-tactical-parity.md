# 40 — 戰場的第一次逐區對拍：側欄兩區 PASS，戰場翻轉補上

**狀態：部分通過。** 側欄的陣形列與 `▶▶` 列**逐像素相同**，
標題／將旗／指令面板從整片不同降到 2–6%（剩下的是雙方名字與計量條這種狀態差）。
⭐ **戰場區從 87.8% 降到 46.1%**：成因是玩家守城時原版把戰場**轉 180 度**，
接上之後（§4）小地圖同步從 41.5% 降到 13.4%。
剩下的是雙方部隊不同，加上鏡頭差**一個等角格**（§4.1）。

- 日期：2026-08-17
- 規格：[`../spec/91`](../spec/91-tactical-parity.md)（分區）、
  [`../spec/31`](../spec/31-tactical-sidebar.md) §1.1（側欄外框）
- 原版側：`workplace/promo-live/probe-march/e10.png`
  （松崗 DOS/V，196年5月20日，許昌攻防戰：張遼軍攻、夏侯惇軍守）
- remake 側：`tools/parity_shot.sh out.png -direct -scenario 0 -player 0
  -seed 7 -open-siege -siege-node 82 -siege-defend -shot-frames 1`
  （`-siege-defend` 讓玩家站守方，與原版這一張同一邊）

## 1. ⭐ 怎麼讓原版開出一場戰場

先前一直沒對過戰場，卡在「怎麼讓兩邊開同一張」。實際做起來比想像簡單：

**編成一支軍團，然後等 AI 來打。**

```
編成(#3) → 選一位武將 → 確定 → 回主畫面 → 等
```

第 45 天（4月6日編成、5月20日開戰）荀彧報「張遼的兵馬，向許昌進攻過來了！！」，
下一張截圖就是戰場。**不必下行軍指示**——那條路要點選單的第二列，
而滑鼠腳本目前點不到（§1.2）。

戰場是**玩家首都許昌**（據點 82），所以 remake 側用 `-siege-node 82` 就對得上；
攻城戰的戰場編號就是據點編號（`internal/rules/battlefield`）。

### 1.1 指令列要先把「命令」視窗打開

開局四個視窗全關，**指令列本身就是那四個視窗之一**——沒開的時候
主畫面下面整片都是地圖，點 y=43 那一列什麼都不會發生。
順序是 `click:0,0`（鏡頭歸零）→ `click:37,0;press`（開命令視窗）→ 才點得到指令。

指令列第 n 顆的 x ＝ `24 + 48n`（[`../re/22`](../re/22-strategy-command-tree.md) §3），
在地圖模式下**主機座標 ＝ 遊戲座標 ÷ 9.6**，所以「編成」(#3, 遊戲 x 188) 送
`click:20,5`。

### 1.2 ⚠ 一次按下會被兩層各吃一次

在指令列上送 `click;press`，「軍團」選單**開了，而且同一瞬間被選走第一列**。
INT 33 `AX=5` 回的 BX 是**當下的按鍵狀態**不是計數，而 `clickat` 把鍵按住
0.12 秒——剛開起來的選單再 poll 一次就看到「按著」，於是拿當時的游標位置選列，
框外的游標被 `sub_193E9` 夾到列 0。

清單視窗沒有這個問題（框外的點被忽略），所以「編成 → 選武將 → 確定」整條走得通。
**目前沒有可靠的辦法點到選單的第二列以後**；要走那條路得先解決這件事。

## 2. 結果

| 區 | 修前 | 修後 | 判定 | 差在哪 |
|---|---:|---:|---|---|
| `field` | 87.82% | **46.08%** | FAIL | 轉 180 度接上了（§4）；剩鏡頭差一格 ＋ 部隊位置 |
| `bottom` | 12.83% | 12.83% | FAIL | 六格的部隊內容（狀態）|
| `sb-title` | 100.00% | **2.16%** | FAIL | 「對呂布」vs「對袁術」——雙方是誰（狀態）|
| `sb-enemy` | 32.48% | **4.82%** | FAIL | 主將名 ＋ 兩條計量條的長度（狀態）|
| `sb-minimap` | 61.48% | **13.44%** | FAIL | 同 `field`：轉了之後只剩部隊點 |
| `sb-self` | 48.27% | **6.33%** | FAIL | 同 `sb-enemy` |
| `sb-formation` | 20.00% | **0.00%** | **PASS** | — |
| `sb-command` | 23.59% | **3.59%** | FAIL | remake 多畫了一個選取框 |
| `sb-arrow` | 46.67% | **0.00%** | **PASS** | — |

## 3. 改出來的三件事

| 項目 | 原版 | remake（修前）|
|---|---|---|
| 側欄的兩根柱子與四條橫帶 | `ICONGRF` 段 1 的四塊圖，一塊一塊貼（[`../spec/31`](../spec/31-tactical-sidebar.md) §1.1）| 沒畫，兩側是視窗底色 |
| 標題格的底 | **色 0（黑）**，`sub_1F1A3` 先填再寫字 | 視窗底色（深藍）|
| 標題文字與兩條計量條的顏色 | 調色盤索引 9／11／12 | 手抄的 RGB，整組偏藍 |

四塊外框的位移各差 `0x40`／`0x40`／`0x80`，最後一塊結束在 `0x3F00`
＝段 1 的長度——`TestBattleFrameTilesFillSegment` 把這個算術檢查釘住了。

## 4. ⭐ 戰場區的成因：玩家守城要把戰場轉 180 度

把 remake 的小地圖轉 180 度再比，相同像素從 **48.1% 跳到 83.0%**
（`workplace/scratch/rot.py`）。這不是巧合——原版早就寫著：

> `sub_14ED7`（攻城遭遇）在**玩家守城**那一支 `or cs:byte_10D35, 0C0h`，
> bit 7 是攻守對調、**bit 6 是戰場翻轉**，而載入器 `sub_1CAEB` 看的是同一個位元
> （[`../re/58`](../re/58-bgm-scene-mapping.md) §4）。

這一張原版截圖裡玩家是守方（張遼攻、夏侯惇守），所以戰場翻轉；
remake 的 `-open-siege` fixture 玩家是攻方，不翻轉。

接上之後（[`../spec/56`](../spec/56-battlefield-rotation.md)）用
`-siege-defend` 讓兩邊都站守方：`field` **87.8% → 46.1%**、
`sb-minimap` **41.5% → 13.4%**。攻方那一張的截圖與改動前**逐像素相同**，
所以這個改動沒有動到不該轉的那一半。

### 4.1 剩下的是鏡頭差一格

`tools/parity_shift.py` 對 `field` 區掃 ±40 px：最佳位移是 **(−16, −8)**，
差異 42.3% → **21.0%**。16×8 正好是**一個等角格**的步距。

同一支對 `sb-minimap` 掃 ±6 px：最佳位移 (−1, +1) 只從 15.3% 降到 14.2%，
**等於沒有位移**。⭐ 小地圖畫的是整張戰場、與鏡頭無關，
所以「小地圖不用平移、戰場要平移一格」把兩個候選分開了：
**翻轉的中心是對的，差的是鏡頭**。

## 5. 未解

| 項目 | 現況 | 下手點 |
|---|---|---|
| 鏡頭差一個等角格 | 量到 (−16, −8)，而初始鏡頭是 `sub_199F3` 的 `word_1D328=0x24`／`word_1D32A=0x0E`（§4.1）| 那個差在世界座標上是半格，湊不出來——要讀原版守城時的鏡頭初值是不是另一組 |
| 同一場戰鬥 | 這一輪只對「同一張戰場」，雙方的軍團組成不同 | 要對就得讓 remake 從原版存檔載入同一個局面 |
| `sb-command` 的選取框 | remake 畫了、原版那一刻沒畫 | 確認原版是「有選中才畫」還是「從不畫」|
