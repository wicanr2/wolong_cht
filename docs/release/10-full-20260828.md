# 10 — 2026-08-28 四平台完整版（戰術規則層修正後重打）

**狀態：已交付並驗過。⛔ 內含原版資產，不可外流。**
`dist-all` 是一致的 `wolong-remake-20260828` 批次——六個產物全部重建，
三支含 remake 實跑畫面的推廣片也全部重錄重剪。

- 日期：2026-08-28
- 工具：`tools/release_all.sh 20260828` ＋ `WOLONG_BUNDLE_DATA=1 tools/android_build.sh`
  ＋ `tools/release_smoke.sh 20260828`
  ＋ `WOLONG_BUNDLE_DATA=1 tools/py.sh tools/release_all_fs.py refresh`
- 前一批：[`09`](09-full-20260827.md)（`20260827`），**被本批取代**

## 1. 這一批比 `20260827` 多了什麼

全部是**戰術規則層與呈現層的缺陷修正**，每一項都有機器碼出處與正對照測試：

| 改動 | 規格 |
|---|---|
| 城壁一撞就垮：原版的「一撞歸零」比的是**背對城**，兩個分支；remake 把面向寫死成 West，於是攻方朝城走就被判成背對 | [`../spec/93`](../spec/93-siege-wall-instant-break-facing.md) |
| 攻城戰打不完：退卻的兵每一幀被清掉繞路，鎖死在半路 | [`../spec/94`](../spec/94-retreat-path-not-cleared-every-frame.md) |
| 守方一兵未損：開場擺兵的高度用錯了表 | [`../spec/95`](../spec/95-spawn-height-uses-ground-plane.md) |
| 守陣只活一幀：回陣的那一步把命令降級成「就位」 | [`../spec/96`](../spec/96-guard-command-not-downgraded.md) |
| 兩軸都被擋住時不嘗試登城；命令 3 的目標高度讀錯平面 | [`../spec/97`](../spec/97-climb-when-both-axes-blocked.md) |
| 爬進未破的門時不打它 | [`../spec/98`](../spec/98-climb-into-a-gate-hits-it.md) |
| 門強度那一塊是**視窗**，先前少畫了一圈外框 | [`../spec/32`](../spec/32-gate-strength-bar.md) §2.2 |
| 大地圖的軍團：朝向的 Go 零值不是「停住」；軍團站在首都中心格時首都疊圖沒讓位 | [`../spec/74`](../spec/74-corps-on-world-map.md) §4.05／§4.1 |
| 驗收模式關掉的改成**音訊輸出**而不是音訊**狀態**——先前 `-shot` 會讓系統選單的「音效」那一格印成「未接入」 | [`../spec/29`](../spec/29-audio.md) §5.1 |

## 2. 產物

| 檔 | 大小 (B) | SHA-256 |
|---|---:|---|
| `wolong-remake-android-debug-20260828.apk` | 52,131,178 | `b03966bb3c4a370024b244b2b0a62f34ee965e7244f11c6dc830db806ed7822e` |
| `wolong-remake-linux-amd64-20260828.AppImage` | 30,188,736 | `609a3b0343acc16c97ee49993ca6c7d26c9a6b5a143a3cde142052b7c7fdf6de` |
| `wolong-remake-linux-amd64-20260828.tar.gz` | 29,795,750 | `74296f0edcb0b2d0e6daa24c6bd3fd722d6c9981d8c5b15f38d5eefdc18b5169` |
| `wolong-remake-linux-arm64-tools-20260828.tar.gz` | 2,161,081 | `eecce85a2ac7e589bfc649f5f2ee4c6a71e36be973d4ef13bf3576634fd5e353` |
| `wolong-remake-macos-universal-20260828.tar.gz` | 38,574,204 | `36104a08287223083903d186418d6313cf6d04652e14d0bdbe51319500fddef8` |
| `wolong-remake-windows-amd64-20260828.tar.gz` | 29,750,688 | `cfc2c6d0e3e459ae0a750dc2c213fa45fdd6e4fdc9e6f1e07b46b5965e517e09` |

⭐ **這一批在同一天打了三次**：第一次的包裡沒有授權條款（§6），第二次補上；
第三次（`f393ee9` 之後）是為了 **APK 的「關於」頁**（[`../spec/99`](../spec/99-about-page-license.md)）
與**譯稿校訂**（簡體 23 則「著→着」、英文 UI 詞 4 條，[`../spec/84`](../spec/84-multilanguage.md) §6）——
語系包 `go:embed` 進執行檔，所以六個產物全部重建。上表是第三次的數字；**APK 之後又為了手機版的字放大 2 倍（`7a67800`，[`../spec/100`](../spec/100-phone-text-scale.md)）與 Scale2x 去鋸齒（`e9f1704`，[`../spec/101`](../spec/101-phone-glyph-scale2x.md)）各單獨重打一次**，桌面四包不受影響。

## 3. 驗收

| 項目 | 結果 |
|---|---|
| `tools/check.sh`（打包前 ＋ 打包後）| ✅ EXIT=0 |
| **每個遊戲包的 ogg 數** | ✅ Linux tar／Windows tar／macOS tar／APK **各 32**；`linux-arm64-tools` 是工具包，0 是對的 |
| AppImage 啟動 ＋ 大地圖 | ✅ `verification/appimage-smoke-20260828.png` |
| AppImage 結局過場 | ✅ `verification/appimage-ending-20260828.png` |
| Linux tar 的 `wlgame` | ✅ `verification/linux-tar-smoke-20260828.png` |
| 不帶任何資料旗標 | ✅ `verification/bundled-nodflags-20260828.png` |
| 四平台的檔頭與 ABI | ✅ `verification/BUILD-ABI.md`：四個 `wlgame` 的 SHA-256 互不相同，ELF／PE32+／Mach-O x86-64／Mach-O arm64 各就各位 |
| 雜湊 | ✅ `sha256sum -c dist-all/SHA256SUMS.txt` 22 列全數相符 |
| **授權條款進了包** | ✅ 四個 `tar.gz` 各一份 `LICENSE`；AppImage 在 `usr/share/doc/wolong-remake/LICENSE`；發行根目錄 `dist-all/LICENSE`。✅ **APK 走遊戲內「關於」頁**（§6）|
| Windows／macOS 原生 GUI | ❌ 仍未實機驗收，只有交叉建置檔頭 |
| Android 模擬器 smoke | ❌ **這一批沒跑**：`wolong-android-emulator:20260820` 已不在機器上，重建要重下 13.7 GB 的 system image（[`09`](09-full-20260827.md) §4），而當下機器負載是 14 核 load 90。APK 本身有內嵌檢查（`assets/gamedata/*` 106 個檔）|

## 4. 這一批重建了兩顆映像

`tools/release_all.sh` 與 `tools/android_build.sh` 的預設標籤都帶日期，
而那兩顆映像**不在機器上**——`20260827` 那一批的 macOS 映像被清掉了。

```sh
docker build -t wolong-osxcross-go:20260828 -f docker/osxcross-go/Dockerfile docker/osxcross-go
docker build --network host -t wolong-go-android:20260828 docker/android
```

兩顆都因為層快取而很快建完（`osxcross-go` 幾乎只是 COPY）。
`docker/osxcross-go/Dockerfile` 的抬頭現在留著**從這一份建過的標籤清單**——
發行紀錄會引用當批的那一顆，沒有那份清單的話 `tools/stale_scan.py`
會把舊紀錄裡的標籤判成過期。

## 5. 推廣片：三支重錄重剪

⚠ **程式改了就要重錄動態段，不能只重剪**（[`../promo/README.md`](../promo/README.md)）。
這一批動到戰術規則層，攻城與野戰兩段的畫面因此都變了。

| 片 | 這一批 | SHA-256 |
|---|---|---|
| `wolong-remake-trailer.mp4` | ✅ 七段動態素材全部重錄（大地圖／野戰／攻城 ＋ 四語系），72.000 秒；第三次重打時再重錄一次（語系包變了）| `58fe759a…` |
| `wolong-remake-dosv-realmachine.mp4` | ✅ remake 側六段重錄（另補 `newgame`／`form`／`march` 三段），72.000 秒；第三次重打時再重錄一次 | `28b864df…` |
| `wolong-remake-android.mp4` | ✅ 1,200 幀重錄重剪，48.468 秒；第三次重打與**字放大 2 倍**（[`../spec/100`](../spec/100-phone-text-scale.md)）與 Scale2x（[`../spec/101`](../spec/101-phone-glyph-scale2x.md)）後各再重錄一次 | `fe7f4d3b…` |
| `wolong-remake-classic-revival.mp4` | ⏸ 未重出 | `ca474a01…` |
| `wolong-remake-yt-comparison.mp4` | ⏸ 未重出 | `3efe64fc…` |

後兩支的兩側素材**都是保存下來的代表幀**（原版取自使用者提供的 YouTube 錄影，
remake 取自 `docs/images/` 裡已當成 fixture 的截圖），腳本吃的是那些檔案——
重跑一次會得到同一支影片。要讓它們反映新版畫面，得先重拍 remake 側的代表幀，
而那些圖的 SHA-256 被別的文件當成 fixture 身分引用著
（[`../promo/README.md`](../promo/README.md) 的「靜態圖一律用 `promo-*.png`」那一條）。
**這是刻意不動，不是漏掉。**

### 5.1 順手補上兩支腳本

先前有兩批推廣片素材**沒有留下產生它們的命令**：主預告的五張靜態圖，
與四段語系畫面。重拍要先重新猜一次旗標，而猜錯的症狀是「圖看起來很像」。

| 腳本 | 補了什麼 |
|---|---|
| `tools/promo_stills.sh`（新增）| 五張靜態圖的旗標寫死在腳本裡。**驗過**：用它重拍一次，五張與版控裡的舊圖**逐位元組相同** |
| `tools/promo_live_capture.sh` | 四段語系 ＋ 實機對照片要的 `newgame`／`form`／`march` 三段一併納入；「不重複張數」的下限改成逐段設定 |

⭐ **下限要逐段設定的理由是量出來的**：語言段會動的只有時鐘，
而「130 張裡有幾張不一樣」還跟**當下的機器負載**有關——同一條命令
在閒置的機器上量到 26 張，在 load 90 的機器上量到 17 張。
`newgame`／`form`／`march` 三段更極端：**不重複張數就是 1，而那是對的**——
編成與行軍指示都是非常駐視窗，開著的時候原版的時鐘停住
（[`../spec/13`](../spec/13-main-window-toggles.md)），畫面連日期都不會變。

## 6. 授權條款（使用者裁定 2026-08-28）

程式碼與文件採**專有授權**：非商業用途免費（含修改與再散布），
商業用途要先洽談（`wicanr2@gmail.com`）。全文在儲存庫根目錄的 `LICENSE`，
決策與適用範圍寫在 `~/.claude/rulebook/85-retro-remake-licensing.md`。

⭐ **條款要跟著包走，這件事害這一批重打了一次。** 拿到 `tar.gz` 的人看不到
儲存庫——包裡沒有那一份，收到的人就不知道自己被授權了什麼，而條款自己
（第 3 條 a）要求散布時保留全文。`tools/release_all_fs.py` 現在會把 `LICENSE`
複製進每一個平台包、AppImage 的 `usr/share/doc/` 與發行根目錄，
`packaging/release/` 的兩份說明範本也各加了一段摘要。

✅ **APK 的條款在遊戲內「關於」頁**（[`../spec/99`](../spec/99-about-page-license.md)，
第三次重打起）。gradle 的 assets 塞得進去但沒有人看得到，所以摘要（授權種類、
可以做什麼、商業洽談與聯絡信箱、不涵蓋原版素材、全文出處）顯示在系統面板的
第四頁；驗法是把 `libgojni.so` 解出來 `grep -a wicanr2@gmail.com`
（⚠ `unzip -p … | grep -c` 對二進位串流會靜默算成 0）。

## 7. 未解

| 項目 | 現況 |
|---|---|
| Windows／macOS 原生 GUI 實機驗收 | ⛔ 沒有那兩個平台的機器。交叉建置的產物只驗了檔頭 |
| Android 實機驗收 ＋ release signing | ⛔ 沒有裝置；目前是 debug 簽章 |
| Android 模擬器 smoke | 這一批跳過（§3）。要跑得先重建 13.7 GB 的模擬器映像 |
| 沒有音效裝置時遊戲會掛 | 完整版會自己找到音檔並開音訊，而 Ebiten 沒有可查詢的音訊 API（[`../spec/75`](../spec/75-bundled-audio.md) §5）|
