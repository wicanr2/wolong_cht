# 08 — 2026-08-26 四平台完整版（四語系批次）

**狀態：已被 `09`（`20260827` 批次）取代。⛔ 內含原版資產，不可外流。**
當時的 `dist-all` 是一致的 `wolong-remake-20260826` 批次——六個產物全部重建。

- 日期：2026-08-26
- 工具：`tools/release_all.sh 20260826` ＋ `WOLONG_BUNDLE_DATA=1 tools/android_build.sh`
  ＋ `tools/release_smoke.sh 20260826` ＋ `WOLONG_BUNDLED=1 tools/android_smoke.sh`
  ＋ `WOLONG_BUNDLE_DATA=1 tools/py.sh tools/release_all_fs.py refresh`
- 前一批：[`07`](07-full-20260824.md)（`20260824`），**被本批取代**

⚠ **「全平台重建」是兩支腳本**：`release_all.sh` 不會重建 APK
（Android 是另一條管線，`android_apk_name` 用檔案 mtime 取日期）。
只跑前者的話五個產物是新的、APK 還是舊的，**而 manifest 看起來完全正常**。

## 1. 這一批比 `20260824` 多了什麼

| 改動 | 規格 |
|---|---|
| **三處顯示修正**：TALK 對話框改黑底、小兵不再只畫一半、月結框六秒消失 | [`../spec/88`](../spec/88-display-polish-parity.md) |
| 戰後的損害報告改成可關的選項（**預設關**）；結果畫面也認滑鼠 | [`../spec/89`](../spec/89-siege-damage-report-toggle.md) |
| **四語系可玩**：繁體中文／简体中文／日本語／English | [`../spec/84`](../spec/84-multilanguage.md) |
| **執行期切換語言**：F9 循環、啟動殼層的 `LANGUAGE` 一列、手機系統面板第三頁 | [`../spec/86`](../spec/86-runtime-language-switch.md) |
| 半形語系的清單欄界——姓名欄不再把 `夏侯淵`／`夏侯惇` 裁成同一個 `XIAHOU` | [`../spec/85`](../spec/85-latin-list-layout.md) |
| 半形語系的其餘畫面：詞表的片語層、戰場標題與將旗版位、系統選單的值 | [`../spec/87`](../spec/87-latin-screen-layout.md) |
| **TALK 變數 key 給錯**：行軍抬頭永遠走 fallback，而 fallback 與正解一字不差，繁中看不出來 | 同上 §6 |
| 開局的戰略評估（孫策攻劉繇分歧的成因）| [`../spec/83`](../spec/83-initial-strategy-pass.md) |
| 災害數值全鏈、應戰挑選計分、信賴度帳本與初值 `0xFF` | [`../spec/81`](../spec/81-disaster-quantities.md)、[`../spec/82`](../spec/82-defender-selection.md)、[`../playtest/44`](../playtest/44-trust-init-oracle.md) |
| 單挑本體；視窗五張、野戰、攻城、據點情報卡的逐像素對拍 | [`../playtest/40`](../playtest/40-tactical-parity.md) |

語系包（`translations/*.json`）**`go:embed` 進執行檔**，
所以四個平台的包裡都不必另外附語系檔，Android 端也零設定。

## 2. 產物（同日第二次建置）

⚠ **同一個 `20260826` 批次重打過一次。** 第一次的包建在
[`../spec/88`](../spec/88-display-polish-parity.md) 那三處顯示修正**之前**，
裡面的對話框還是藍底、戰場的兵還是半截。批次日期沿用同一天，
所以**只有雜湊分得出新舊**——下表是第二次的。

| 檔 | 大小 (B) | SHA-256 |
|---|---:|---|
| `wolong-remake-android-debug-20260826.apk` | 26,772,292 | `849f45031bc38d61f7d7d9a1f5d401b85638418926d4acdf75c18d6e5b39ead2` |
| `wolong-remake-linux-amd64-20260826.AppImage` | 30,184,640 | `706f5290e88cc938955f1347e4b37a429d3c1950fe7d543fa1afd15023edbd6c` |
| `wolong-remake-linux-amd64-20260826.tar.gz` | 29,783,995 | `202560c8d3f6d874093163d48b1d9192f3eb8c4d97b12050bd1674f43552a790` |
| `wolong-remake-linux-arm64-tools-20260826.tar.gz` | 2,157,470 | `2cf4aeb0591c9304a4dbfbc92e772d646568841c33fa159ee4b10f4f2e78ce17` |
| `wolong-remake-macos-universal-20260826.tar.gz` | 38,562,057 | `389345ade28755d2b1622bc67d272fbabe78258d636ff0485b2a7b3899ec62e3` |
| `wolong-remake-windows-amd64-20260826.tar.gz` | 29,745,502 | `6065e84c67748be96ab471601bd2b80abc188495d2e2aefa1267d5e82864f2e1` |

> **教訓寫成規則**：**批次日期只有一天的解析度，同一天重打就分不出新舊。**
> 要靠雜湊。發行紀錄裡的雜湊因此不是裝飾——它是「這一包到底是哪一份程式」
> 的唯一答案。

兩支推廣片同時換新：主預告 `f17e242e…`（72 秒，**配樂改用原版的曲子**）、
實機對照片 `1128867f…`（八段裡六段並排）。見
[`../promo/README.md`](../promo/README.md)。

## 3. 驗收

| 項目 | 結果 |
|---|---|
| `tools/check.sh`（打包前）| ✅ EXIT=0 |
| AppImage 啟動 ＋ 大地圖 | ✅ `verification/appimage-smoke-20260826.png` |
| AppImage 結局過場 | ✅ `verification/appimage-ending-20260826.png` |
| Linux tar 的 `wlgame` | ✅ `verification/linux-tar-smoke-20260826.png` |
| 不帶任何資料旗標 | ✅ `verification/bundled-nodflags-20260826.png`，沒有「載不到字型」警告 |
| **包裡的語系**（`-lang zh-hans`／`ja`／`en`）| ✅ `verification/appimage-lang-*-20260826.png`。**AppImage 旁邊沒有 `translations/`**，畫面照樣是那個語言 ⇒ 內嵌生效；日文的 `拠点`／`予備兵` 也畫得出來 ⇒ 包裡的字型鏈含 JIS |
| `.so` 的 16 KB 對齊 | ✅ 由 `android_build.sh` 內建檢查擋（`0x4000`）|
| **Android 模擬器 smoke**（內嵌版）| ✅ 安裝後直接進 MainActivity、APK 自己解出 **69** 個原版檔（第一次建置驗的；第二次只改呈現層，指紋涵蓋的規則層沒動）|
| ↳ 指紋與桌面比對 | ✅ frame 1／60／120 ＝ `dc4773497b12206e`／`563d356df606873a`／`645434f9e3bd7991`，桌面同幀逐字相同 |
| **包裡的對話框**（第二次建置）| ✅ AppImage 跑 `-open-message`，框內 13,243 px 全黑、沒有一個 `(0,32,97)` ⇒ `spec/88` 的修正真的在包裡 |
| 雜湊 | ✅ `sha256sum -c dist-all/SHA256SUMS.txt` 24 列全數相符 |
| Windows／macOS 原生 GUI | ❌ 仍未實機驗收，只有交叉建置檔頭（`verification/BUILD-ABI.md`）|

⚠ **指紋這一項只證明核心跑得動且決定性成立。** 前 120 幀不進戰術、不結算
月份，這一批改的語系與版面**本來就都在那個範圍外**。指紋與上一批不同是
規則改動（開局戰略評估、災害數值、應戰挑選）造成的，不是平台差異——
判準是「Android 與桌面**同一顆執行檔版本**算出同一個值」。

## 4. 這一輪踩到的：推廣片被舊檔蓋掉

新剪的 72 秒預告片寫進 `dist-all/promo/`，**打包時被 8/20 的舊片覆蓋**。
成因是 `release_all_fs.py` 的 `promo_source()` **先找 `dist/promo/`**，
找到才輪到 `dist-all/promo/`；而 `dist/promo/` 裡還躺著舊版。

`stage` → `promote` 之後 `dist-all/promo/` 被 staging 整個換掉，
**manifest、`SHA256SUMS.txt`、檔名全部正常**，只有片長與內容是舊的。
發現它靠的是「我記得剛才那個雜湊」，不是任何一道檢查。

**處置**：推廣片一律產到 `dist/promo/`（那是來源），再讓打包流程複製過去；
[`../promo/README.md`](../promo/README.md) 的重跑命令已經改成寫 `dist/promo/`。

> **教訓寫成規則**：**「有兩個來源目錄、其中一個優先」的設計，會在
> 舊檔還在的時候安靜地贏。** 只要輸出檔名不帶版本，覆蓋就沒有任何痕跡。
> 產出物要嘛只有一個來源，要嘛檔名帶批次日期。

## 5. 未解

| 項目 | 現況 |
|---|---|
| 推廣片檔名不帶批次日期 | §4 的根因還在。加日期會動到 `PROMO_FILES` 與 `dist-all/README.md` 的說明，這一批沒動 |
| 沒有音效裝置的真實玩家 | ⛔ 仍會掛（[`05`](05-full-20260823.md) §5）。要收掉得讓 `wlgame` 有一個明確關音訊的旗標——`-audio ""` 現在是「自動找」不是「關」 |
| 啟動殼層的自動驗收 | 給 `-shot` 會走白名單直接進遊戲跳過殼層，不給就會開音訊而容器裡沒有音效裝置（[`06`](06-appimage-20260824.md) §4.1）。語言那一頁的畫面證據是**原始碼建置**的截圖（[`../playtest/46`](../playtest/46-runtime-language-switch.md)），不是包裡的執行檔 |
| Android 的語言切換沒在模擬器上點過 | 面板與 `syncLanguage` 有單測，畫面是桌面 Xvfb 跑 `cmd/wlandroid` 拍的；smoke 期間不可送觸控（送了指紋就不能比），所以要另開一輪非指紋的驗收 |
| Windows／macOS 實機 | 沒有硬體 |
| Android 實機驗收與正式簽章 | 沒有裝置；keystore 保管未決，出的仍是 debug 簽章 |
