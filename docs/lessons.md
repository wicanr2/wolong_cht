# 教訓：按觸發點索引

**狀態：由 `tools/lessons.py render` 產生，不要手改。**
權威是 [`lessons.json`](lessons.json)；`tools/ida.sh`／`tools/dosgolem.sh`
開頭會把相關的那幾條印出來。

- 日期：2026-09-10
- 規則：`~/.claude/rulebook/61-worklist-as-data-with-verify.md`（同一個做法用在教訓上）
- 相關：[`CONTEXT.md`](../CONTEXT.md) §6（事件台帳）、`CLAUDE.md` §7（歷史清單）

<!-- lessons:begin 由 tools/lessons.py render 產生，不要手改 -->

共 **16 條**。權威是 [`lessons.json`](lessons.json)，這一份由 `tools/py.sh tools/lessons.py render` 產生。

⭐ **按「什麼時候要想起這一條」索引**——教訓要防的動作發生在任務中間，不是開始時；按事件時間排的清單那時查不到。

| 什麼時候想起 | 這一條 | 犯過 | 防線 |
|---|---|---:|---|
| 反組譯時看到一整段 `db`，而前面有 `mov cs:xxx, reg` | [IDA 把整段解成資料時，先問是不是自我修改碼](#self-modifying-code-reads-as-data) | 2 | remind |
| 要引用文件裡的數字、或 `workplace/` 底下存下來的產物，當成「改之前是多少」 | [拿舊產物當基線＝拿舊程式碼當基線](#baseline-from-stale-artifact) | 3 | remind |
| 要下「這個規則是這樣」的結論，而手上只有一組原版樣本 | [一組樣本分不開兩種讀法，就不算驗證](#one-sample-cannot-separate) | 3 | remind |
| 同狀態對拍之前，宣稱兩邊起點對齊 | [「兩邊對齊了」要對每一種狀態各問一次](#alignment-per-state) | 1 | remind |
| 要用一個 remake 有、而原版沒有對應物的欄位當判準 | [remake 自己加的欄位最危險](#remake-only-field) | 3 | remind |
| 處理原版的顏色屬性 byte | [屬性 byte 的兩個半位元組是「背景︰前景」](#attribute-nibble-order) | 2 | remind |
| 寫下一條新規則或新教訓的那一刻 | [規則沒有觸發時機，等於不存在](#rule-without-trigger) | 3 | tool |
| 用 grep 或 pgrep 判斷「某個東西還在不在」 | [grep／pgrep 會匹配到查詢自己](#query-matches-itself) | 2 | test |
| 對拍時要把某個原版欄位標成「畫面用」「導出值」「remake 走自己那一套」而放進「不必比」那一格 | [宣告一個欄位「remake 不必建模」等於為它關掉所有檢查](#not-modelled-turns-off-checks) | 1 | remind |
| 寫掃描條件去問「有沒有人做某件事」，而答案是 0 處 | [過濾器自己有洞：清除端的立即值是補數](#filter-has-a-hole) | 2 | tool |
| 修好一個規則錯之後，本來綠的測試紅了 | [測試可能正是靠那個 bug 才綠的](#tests-green-on-the-bug) | 1 | rule |
| 手上沒有某個值，於是拿一條式子從別的值反解出來，然後拿它去解釋現象 | [倒推出來的數字是假說，不是觀測](#derived-number-as-observation) | 1 | tool |
| 拿「這一拍取了幾次亂數」當同步的判準，或看到某個逐拍指標全綠就宣告那一段沒問題 | [取數次數相同，不代表取數序列相同](#count-matches-sequence-differs) | 3 | tool |
| 追一支函式追到它設了旗標／回了 CF／寫了欄位，就下「所以會發生 X」的結論 | [「這個值被算出來」不等於「有人會看它」](#flag-set-without-consumer) | 3 | tool |
| 準備下「這裡是天花板／解不出來／需要決策」的結論，或準備開一項新工作 | [要說「卡住了」之前，先查自己的 docs/](#answer-already-in-own-docs) | 1 | tool |
| 改了規則層卻發現「比對結果一模一樣」，或夾具在某個階段之後才做一件會改狀態的事 | [夾具自己吃掉一筆，指標不會變紅](#silent-fixture-drop) | 1 | remind（❌ 不見了） |

### IDA 把整段解成資料時，先問是不是自我修改碼

<a id="self-modifying-code-reads-as-data"></a>**什麼時候想起**：反組譯時看到一整段 `db`，而前面有 `mov cs:xxx, reg`

**要做的**：先當自我修改碼——那幾行寫的是**後面幾行的運算元**。交叉解碼那段 bytes 再下結論；「解不出指令」在讀的時候長得像「這段不重要」。

| 日期 | 犯在哪 | 收據 |
|---|---|---|
| 2026-09-10 | `loc_1491B` 的退卻廣度優先：把兩個**終止條件**讀成「起點」，連錯兩種讀法，`commit` 並 `push` 之後才被第二組實測推翻 | `docs/playtest/119` §34.5 |
| 2026-08 | `loc_1A065` 的每輪更新、`loc_10701` 把屬性 patch 進 inline 立即值 | `docs/re/28` §3 |

**防線**：`remind` — 規則在 `CLAUDE.md` §7 第 28 條；`tools/ida.sh` 每次跑會把它印出來。⚠ 光有規則擋不住——2026-09-10 那次規則就在載入的 `CLAUDE.md` 裡，讀碼時一次都沒被想起來。

### 拿舊產物當基線＝拿舊程式碼當基線

<a id="baseline-from-stale-artifact"></a>**什麼時候想起**：要引用文件裡的數字、或 `workplace/` 底下存下來的產物，當成「改之前是多少」

**要做的**：**先在當前 commit 重跑一次基線**。存下來的序列檔、`.DAT`、截圖都不會告訴你它是哪一版跑的。

| 日期 | 犯在哪 | 收據 |
|---|---|---|
| 2026-09-10 | 文件寫 229，HEAD 重跑是 369——差點把一個改善（369 → 332）讀成退步（229 → 332） | `docs/playtest/119` §33.1 |
| 2026-09-10 | `remake-ck200.DAT` 對原版 0 個 byte，同一條命令列在 HEAD 重跑是 4 個 | 同上 |
| 2026-08 | `tools/go.sh` 沒把 `GOOS` 傳進 docker，三個平台建出一模一樣的執行檔，每次都 exit 0 | `CLAUDE.md` §7 第 21 條 |

**防線**：`remind` — `tools/dosgolem.sh` 每次跑會印。⚠ 只有規則擋不住——這一類的產物看起來永遠是對的。

### 一組樣本分不開兩種讀法，就不算驗證

<a id="one-sample-cannot-separate"></a>**什麼時候想起**：要下「這個規則是這樣」的結論，而手上只有一組原版樣本

**要做的**：先問：**別的讀法在這一組上會給出不同答案嗎？** 會，才算驗證；不會，那一組只是「相容」。至少要兩組能互相分開的。

| 日期 | 犯在哪 | 收據 |
|---|---|---|
| 2026-09-10 | 退卻起點：第一組（軍團 1）「離首都近的端點」與「固定取 B 端」剛好同解，當成通過並 push；第二組（軍團 39）才把兩者分開 | `docs/spec/46` §5.2 |
| 2026-08 | 拓樸一樣不等於路一樣：254 條邊裡 253 條兩邊都有，其中 109 條路徑長度差 > 2 格 | `CLAUDE.md` §7 第 16 條 |
| 2026-08 | 把城池圖形當成橋加進去，連通性真的從 27 群收成 1 群——結果對不代表理由對 | `CLAUDE.md` §7 第 18 條 |

**防線**：`remind` — `tools/ida.sh` 與 `tools/dosgolem.sh` 都會印。

### 「兩邊對齊了」要對每一種狀態各問一次

<a id="alignment-per-state"></a>**什麼時候想起**：同狀態對拍之前，宣稱兩邊起點對齊

**要做的**：游標、取數**個數**、亂數**值**是三件事。個數序列對值完全不敏感，所以那道自檢永遠不會為了值開口——要直接問值。

| 日期 | 犯在哪 | 收據 |
|---|---|---|
| 2026-09-10 | `-skip 27` 讓 remake 拿快照的亂數狀態多跑 27 拍，亂數流岔開；游標與個數都「對齊」了，值沒有。代價是把真正的第一個分歧藏了 288 拍 | `docs/playtest/119` §34 |

**防線**：`remind` — `tools/dosgolem.sh` 會印。問亂數值的方法在 `docs/playtest/119` §34.1。

### remake 自己加的欄位最危險

<a id="remake-only-field"></a>**什麼時候想起**：要用一個 remake 有、而原版沒有對應物的欄位當判準

**要做的**：先問原版用什麼判準。多出來的欄位讓每個判斷都能寫成 `X == Y` 而**看起來合理、測試也綠**——原版根本沒有那個概念。

| 日期 | 犯在哪 | 收據 |
|---|---|---|
| 2026-09-10 | `Corps.Node` 被當成「站在哪裡」用了三處：退卻的「站在自家據點上」、退卻起點、據點失守的調頭名單。原版分別用 `+0x0E < 600h`、邊的兩端、**座標** | `CONTEXT.md` §6 |
| 2026-09-10 | 又五處：四個 `c.Node == capital`（留守補兵、Stage 9 轉補兵、補兵、解散）與 `army.KindOf(c.Node) == army.FieldNode`（軍費與士氣）。原版 `sub_14548` 比三個欄位（座標兩格 ＋ `+0x0E`），`sub_12600` 比 `+0x0E ≥ 800h`。**「從 X 出發、目標也是 X」在退卻與回首都補兵時是常態**，所以這個誤判不是邊角 | `CONTEXT.md` §6、`docs/spec/177` §1.5、`docs/spec/178` |
| 2026-09-10 | 第六處：攻城的守軍名單 `pickDefender(..., d.Node == node)`。`sub_14C72` 收的是**同一格**的名單（攻城時就是據點座標）。拿 `Node` 比會把「從這座城出發、正走在路上」的軍團算成守軍——而**三個測試一直靠這個誤判通過**（兩支軍團其實錯身而過，攻城時憑 `Node` 憑空找到守軍） | `docs/spec/82`、`CONTEXT.md` §6 |

**防線**：`remind` — `tools/ida.sh` 與 `tools/check.sh` 都會印。

### 屬性 byte 的兩個半位元組是「背景︰前景」

<a id="attribute-nibble-order"></a>**什麼時候想起**：處理原版的顏色屬性 byte

**要做的**：高半位元組是**背景**、低半位元組是前景，不是相反。⚠ 抄反了在別處未必看得出來——只有背景剛好等於前景那一次才會整個消失。

| 日期 | 犯在哪 | 收據 |
|---|---|---|
| 2026-09-06 | `docs/re/52` §4 的日期欄顏色 | `CONTEXT.md` §6 |
| 2026-09-07 | 軍師命名視窗的選字表顏色——**同一種錯誤第二次** | `CONTEXT.md` §6 |

**防線**：`remind` — `tools/ida.sh` 會印。

### 規則沒有觸發時機，等於不存在

<a id="rule-without-trigger"></a>**什麼時候想起**：寫下一條新規則或新教訓的那一刻

**要做的**：同時給出**什麼時候會想起它**與**什麼會檢查它**。會復發的斷言要規則與檢查兩者都有，只有其中一個都不夠。

| 日期 | 犯在哪 | 收據 |
|---|---|---|
| 2026-09-03 | git 身分規則早就存在，但觸發點寫成「第一個 commit 之前」，接手既有 repo 時一次都沒成立——131 個 commit 裡 58 個掛公司信箱 | `CLAUDE.md` §10 |
| 2026-08 | 「密碼頁不擋 oracle」在四份文件裡復發，包括解出來的同一天新寫的筆記 | `CLAUDE.md` §4.0 |
| 2026-09-10 | `docs/playtest/119` §36.5 寫了「對齊要看據點游標，不能看拍數」並列出正確拍數（200／702／1,203／1,604／1,905），**下一輪重生 remake 檢查點還是照檔名跑了 700／1200／1600／1900**。症狀是四個檢查點各有 2–5 座據點的上昇值與防災值差 1，看起來像規則錯，其實只是取樣點差 2–5 拍。改成工具 `tools/parity_ck.sh` 之後不接受手打拍數 | `docs/playtest/119` §37.4 |

**防線**：`tool` — `tools/index.py` 擋「密碼頁受阻」那個字樣的復發；`tools/parity_ck.sh` 把「拍數由游標反推」從一句警告變成唯一的入口。這一份 JSON 本身也是同一條規則的產物——每條教訓都必須說出 `trigger` 與 `guard`。

### grep／pgrep 會匹配到查詢自己

<a id="query-matches-itself"></a>**什麼時候想起**：用 grep 或 pgrep 判斷「某個東西還在不在」

**要做的**：先確認那個 pattern 不會中到**查詢自己**：等待腳本的指令行、條目自己的 body、掃描工具自己的原始碼。

| 日期 | 犯在哪 | 收據 |
|---|---|---|
| 2026-09-10 | `while pgrep -f "apps/wolong"` 匹配到等待腳本自己的指令行，條件永遠成立——多等了二十分鐘等一個早就結束的程序 | 本輪 |
| 2026-09-10 | `worklist.py` 的 verify 會掃到 `worklist.json` 自己（條目的 body 幾乎一定含 pattern 的字樣），每一條都會永遠說「仍未完成」——寫工具時就擋掉了 | `tools/worklist.py` |

**防線**：`test` — `tools/worklist.py --selftest` 有一條「不掃 worklist.json 自己」的正反對照。⚠ pgrep 那一半還沒有防線。

### 宣告一個欄位「remake 不必建模」等於為它關掉所有檢查

<a id="not-modelled-turns-off-checks"></a>**什麼時候想起**：對拍時要把某個原版欄位標成「畫面用」「導出值」「remake 走自己那一套」而放進「不必比」那一格

**要做的**：先 grep `docs/` 問那個符號已經有沒有答案，再決定。符號查對了不代表語意查對了——**錯的是符號後面那句話**，而那句話一旦寫成「畫面用」，之後每一次逐欄比對都會合法地跳過它，於是它再也不會開口。要免除一個欄位，理由要跟斷言同級：寫明出處與推論等級。

| 日期 | 犯在哪 | 收據 |
|---|---|---|
| 2026-09-10 | 全域 `+0x30` 被記成「`cs:word_10D20`，大地圖捲動原點，畫面用」。它其實是**事件佇列的派發游標**，`docs/re/15`／`docs/re/07` 早就寫清楚了。沒還原它 ⇒ 整個月的宣戰被重播 ⇒ 四個勢力重設侵攻目標、多編四支救援軍團，拍 1,200 的據點表差到 172 座 | `CONTEXT.md` §6、`docs/spec/176` |

**防線**：`remind` — `tools/dosgolem.sh` 與 `tools/ida.sh` 開頭會印。真正的防線要等逐欄比對工具能列出「被免除的欄位＋理由」才做得起來。

### 過濾器自己有洞：清除端的立即值是補數

<a id="filter-has-a-hole"></a>**什麼時候想起**：寫掃描條件去問「有沒有人做某件事」，而答案是 0 處

**要做的**：**把條件對「反面的寫法」也套一次。** 位元的設定端立即值含那個位元，清除端的立即值是**補數**（`and …, 0FEh` 清位元 0，而 `0FEh & 01h == 0`）——只篩「含」的一側，清除端全部落空，而輸出看起來完全正常。工具要**按角色分組**並在某一組是 0 處時明講，不要讓「這一類我沒在找」與「這一類真的沒有」印成同一個樣子。

| 日期 | 犯在哪 | 收據 |
|---|---|---|
| 2026-09-10 | `parity_pace_diff.py` 的切拍規則 `PRE` 少列 `140F9`（求援冷卻擲骰在錨點 `sub_14194` 之前 43 道指令），報表因此說「原版拍 5654 求援、remake 拍 5655 求援」——**remake 才是對的** | `docs/playtest/119` §47.1 |
| 2026-09-10 | `tools/ida_bitflag_users.py` 照「立即值含指定位元」篩，於是 `docs/spec/173` 斷言軍團 `+0x00` 位元 0「沒有清除端，所以是一次性的」。清除端就在 `sub_147BB` 的 `00014869`，語意整個相反 | `CONTEXT.md` §6、`docs/spec/177` §1.3 |

**防線**：`tool` — `tools/ida_bitflag_users.py` 現在按角色分組（設／清／翻轉／測／遮罩保留），空的那一組會印出「一條都沒有——下結論前先確認掃描本身有正對照」。

### 測試可能正是靠那個 bug 才綠的

<a id="tests-green-on-the-bug"></a>**什麼時候想起**：修好一個規則錯之後，本來綠的測試紅了

**要做的**：**先問「這個測試原本是怎麼通過的」，不要先假設自己改壞了。** 端對端測試搭的場面常常沒有被驗證過——它只驗「有沒有發生」，不驗「該不該發生」。修好判準之後場面不成立了，那代表原本的綠燈是假的。修法是**把場面改成真的成立**（並在測試裡寫明為什麼），不是把判準改回去。

| 日期 | 犯在哪 | 收據 |
|---|---|---|
| 2026-09-10 | 攻城守軍改用座標判準之後，三個端對端測試同時紅。查下去發現它們讓兩個君主互相攻打對方首都，而兩支其實**錯身而過**——攻城時憑 `Corps.Node`（行軍中留著出發據點）憑空找到守軍才打起來。改成「玩家留守、AI 來攻」之後場面才真的成立 | `internal/state/state_test.go`、`internal/state/tactical_resolve_test.go` |

**防線**：`rule` — 沒有機器訊號——這一條要在「測試紅了」的當下想起來。⚠ verify **不能綁測試檔**：`lessons.py` 與 `worklist.py` 都只掃產品程式碼（`rulebook/61` 的硬規則），綁在 `_test.go` 的 pattern 永遠找不到，而那看起來就像「防線不見了」。改綁 `CONTEXT.md` 的推翻紀錄。

### 倒推出來的數字是假說，不是觀測

<a id="derived-number-as-observation"></a>**什麼時候想起**：手上沒有某個值，於是拿一條式子從別的值反解出來，然後拿它去解釋現象

**要做的**：**先問這個數字有幾個獨立來源。** 反解用的式子本身可能就是錯的那一個——那時反解出來的值會「完美地」解釋現象，因為它就是為了解釋而生的。真正的觀測要能直接讀到（原版的記憶體、log 的暫存器、程式碼的常數）。拿不到就標成假說，並**先去找能直接讀的那一個**。

| 日期 | 犯在哪 | 收據 |
|---|---|---|
| 2026-09-10 | 拍 3,415 的調兵分歧。從 `want = 威脅量 + 2 − 佔用數` 反解出「原版的佔用數是 2」，據此花一整輪建模佔用圖、寫規格、追五個維護點。實際上那條式子用錯了支（`want` 恆為 1），而原版的佔用圖 peek 出來與 remake **逐格相同** | `docs/spec/184`、`docs/playtest/119` §43.1 |

**防線**：`tool` — `tools/orig_occupancy.py` 把原版的佔用圖直接 peek 出來逐格比——那一類「只能倒推」的狀態，能 peek 就 peek。dosgolem 的 `-watch` log 也帶暫存器，`sub_14155` 的 `DX` 就是 skip:want。

### 取數次數相同，不代表取數序列相同

<a id="count-matches-sequence-differs"></a>**什麼時候想起**：拿「這一拍取了幾次亂數」當同步的判準，或看到某個逐拍指標全綠就宣告那一段沒問題

**要做的**：**比次數是比一個雜湊，不是比內容。** 呼叫順序換位而總數不變時，兩邊的亂數值會全部餵給不同的判斷，而指標一片綠。下「這一拍一致」的結論之前，先把**兩邊的呼叫來源並排看一次**（`tools/parity_pace_diff.py --tick N`）。

同一個形狀還有第二種：**那個維度根本不取亂數。** 行軍、路徑選擇、旗標維護都不取，所以取數比對對它們結構上是盲的——要抓它們得比狀態表，不是比取數。

| 日期 | 犯在哪 | 收據 |
|---|---|---|
| 2026-09-10 | 5/1 月結那一拍兩邊都是 647 次，逐拍取數比對說「一致」。實際上原版 `sub_15358` 把政略（`sub_12BD9`）排在災害之前，remake 排在之後——647 個值全部錯位，遷都的勢力集合、宣戰落在哪個佇列槽全不同，而第一個看得見的分歧要到 565 拍之後才浮出來 | `docs/spec/185`、`docs/playtest/119` §45 |
| 2026-09-10 | 月結修好之後取數不一致降到 21/5,480，第一個分歧報在拍 4,910；但軍團表在拍 4,900 就差了 15 個 byte——軍團 35 走上另一條路，而**行軍不取亂數** | `docs/spec/185` §4 |
| 2026-09-10 | 拍 5,020 的檢查點看起來是分歧（軍團 `+0x03` 差 1、子刻差 1、軍團游標差一個移動週期），實際是 `steps:` 停在**戰鬥中間**而 remake 跑完整拍。據點游標對得上只代表同一拍，不代表同一個位置 | `docs/playtest/119` §46.6、`tools/parity_ck_orig.sh` 的註解 |

**防線**：`tool` — `tools/parity_pace_diff.py --tick N` 把兩邊的呼叫來源逐筆並排；工具的 docstring 也明寫它只比次數。狀態面則靠 `tools/parity_ck.sh` 的五張表逐 byte 比。

### 「這個值被算出來」不等於「有人會看它」

<a id="flag-set-without-consumer"></a>**什麼時候想起**：追一支函式追到它設了旗標／回了 CF／寫了欄位，就下「所以會發生 X」的結論

**要做的**：**下結論前要找到讀那個值的那一行。** 回傳值可能被整個丟掉，旗標可能只在某些呼叫端被消費，欄位可能只有一半的路徑會看。`tools/ida_callers.py` 查呼叫端、`tools/ida_disp_users.py` 查誰讀那個位移——**兩邊都要，因為「誰設」與「誰看」是不同的問題**（`docs/re/34` §2.05 是同一個形狀：位元 0 的設定端早就記了，消費端沒記，於是它被當成顯示狀態）。

| 日期 | 犯在哪 | 收據 |
|---|---|---|
| 2026-09-10 | `sub_1474A` 士氣 0 回 STC ⇒ 斷言「士氣不足 100 的軍團打贏也會散掉」，還據此判說明書 5.5 與機器碼不符。實際上 `sub_14ADE`（攻城）拿「誰贏」分流，**勝方那一位根本不看**——說明書是對的 | `docs/spec/187`、`docs/re/09` §4.3、`CONTEXT.md` §6 |
| 2026-09-10 | 同一輪先斷言 `sub_14E5C`／`sub_14ED7` 不看 `sub_15130` 的回傳值 ⇒ 「壞滅在自動判定這條路沒有消費端」。實際上看的是它們的**呼叫端** `sub_14A7B`／`sub_14ADE`——只往下追一層不夠 | `docs/spec/187` §1 |
| 2026-09-10 | 軍團 `+0x00` 位元 0 的設定端與清除端都記了，消費端沒記，於是 remake 把它當顯示狀態寫成 `LinkAddr != 0` 的衍生值。它其實決定行軍主迴圈這一拍走 `sub_12708` 還是 `sub_127A2` | `docs/re/34` §2.05 |

**防線**：`tool` — `tools/ida_return_consumers.py` 對每個呼叫端印呼叫之後的 4 條指令，並判「消費 CF／消費 AX／⚠ 沒消費」。⚠ 它只看直線的下幾條，**輸出是候選不是結論**——「看起來沒消費」要再往上追一層（那正是第二次踩坑的地方）。欄位那一側用 `ida_disp_users.py`（誰讀那個位移）。

### 要說「卡住了」之前，先查自己的 docs/

<a id="answer-already-in-own-docs"></a>**什麼時候想起**：準備下「這裡是天花板／解不出來／需要決策」的結論，或準備開一項新工作

**要做的**：**先用關鍵詞查一次自己的文件**（`tools/py.sh tools/askdocs.py <詞>`）。答案常常是自己前幾天寫的，而且**標題就寫著**。

⚠ 把「卡住」寫成決策呈給使用者，比自己多查五分鐘貴得多——那消耗的是別人的注意力，而且會讓一份已經驗過的結論看起來像未解的問題。

| 日期 | 犯在哪 | 收據 |
|---|---|---|
| 2026-09-10 | 同局面對拍撞到「孫乾　大人，主公有事召見。」停住，於是把「玩家事件怎麼處理」寫成三選一的決策呈給使用者。而 `docs/playtest/118` 的**標題**就是「劉備 90 天無人值守：君主召見用右鍵推進，選項用移動次數」，狀態「通過」——連 90 天跑得完、一輪 26 秒、兩次逐 byte 相同都驗過了 | `docs/playtest/118`、`tools/askdocs.py` 的 docstring |

**防線**：`tool` — `tools/py.sh tools/askdocs.py <關鍵詞>` 掃 `docs/` 的標題與狀態行，只印標題／狀態／路徑——一眼看得出「那份說通過了」。`grep -r` 做得到同樣的事，差別在它不會被全文淹沒。⚠ 回空**不代表沒解過**，再查 `docs/INDEX.md` 的斷言總表。

### 夾具自己吃掉一筆，指標不會變紅

<a id="silent-fixture-drop"></a>**什麼時候想起**：改了規則層卻發現「比對結果一模一樣」，或夾具在某個階段之後才做一件會改狀態的事

**要做的**：先問**夾具在哪一刻把那一拍的數字定案**。統計之後才發生的副作用（自動回應、收尾、補寫）會落在兩次快照之間，**兩邊都不算**——比對一片綠，而狀態表在幾十拍之後才浮出來。判準：改了會取亂數的路徑，序列的**總數**要跟著變；沒變就先懷疑夾具，不要先懷疑規則沒生效。

| 日期 | 犯在哪 | 收據 |
|---|---|---|
| 2026-09-10 | `rng_pace.go` 在 `w.Tick(tr)` 之後立刻記錄該拍取數，而自動回應（`ResolveDiplomacy`）排在記錄之後、`prev = tr.seq` 之前，於是 `sub_13902` 的君主採納那一擲兩拍都不算 | `docs/playtest/119` §47 |

**防線**：`remind` — 規則印在 `tools/dosgolem.sh` 的教訓提示（`docs/lessons/parity.txt`）。⚠ 目前只有規則——要變成檢查，得讓 `rng_pace.go` 對「一拍之外發生的取數」直接報錯。

<!-- lessons:end -->

<!-- 缺口：無 -->

<!-- 缺口：無 -->
