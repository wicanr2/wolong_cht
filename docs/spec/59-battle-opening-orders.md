# 59 — 開場的常令：腳本那一側不要先下「攻擊」

**狀態：CONFORMED。** 原版兩側都是**站在陣形上等**，動作由腳本第一批指令發起；
remake 先前一律先下一發「攻擊」，於是**開局第一個 tick 兩側就都衝出去了**。
⭐ 連帶抓到一個真正的規則 bug：**腳本的陣形線查表用了側編號**，
玩家守城時腳本會把自己的線設成玩家那一條（§6）。
兩件都修好之後，小地圖在第 40 步**只差 8 個像素（0.04%）**。

- 日期：2026-08-18
- 出處：`BATTLE.DAT` 的段（[`../formats/07`](../formats/07-battle.md) §6、
  [`../re/11`](../re/11-tactical-battle.md) §3.5）、實機截圖（§3）
- 推論等級：**強證據**（腳本內容 confirmed，開場常令由畫面反推）

## 1. 原版做什麼

AI 那一側由 `BATTLE.DAT` 的一段驅動，段編號 ＝ 武將 `+0x16` × 4 ＋ 戰場類別。
張遼（`+0x16` ＝ 2）在攻城那幾段的開頭是：

```
msg  …            開場對白
wait 15
msg  …
line 0            陣形線
form 1            ⭐ 換成陣形 1
order 全軍 命令0   ⭐ 命令 0 ＝ 走到陣形指定的座標
wait 35
…
```

**第一個動作指令是「走到陣形」不是「攻擊」。** 玩家那一側也一樣——
實機截圖上玩家的兵整齊站在陣形線上沒有動。

## 2. remake 先前怎麼做

`internal/state/tactical.go` 的 `beginTactical` 對**兩側**都先
`b.Order(side, -1, tactical.Attack)`，理由寫在註解裡：
「腳本第一個指令通常是查詢，免得沒有腳本的那一場開局所有人都站著不動」。

代價是：有腳本的那一側在腳本跑到 `order` 之前就已經在衝鋒，
等腳本說「走到陣形」時人已經散開，**再也回不到原版那張圖**。

## 3. 證據：小地圖上的部隊點

拿同一個局面對拍（[`../playtest/40`](../playtest/40-tactical-parity.md) §5），
把小地圖的部隊點還原成戰場座標（`workplace/scratch/minidots.py`）：

| | 原版 | remake（開場 0 步）|
|---|---|---|
| 玩家側（色 10）| X ∈ {3, 5}、Y 7–55 | **完全相同** |
| 腳本側（色 3）| X ∈ {56, 57, 59, 60}、Y 19–44 | X ∈ {58, 60}、Y 7–55 |

腳本側那個 4 欄 × 12 列的形狀在十六個陣形裡是**唯一**的：
陣形 1 的 `dx ∈ {−2,−1,1,2}`、`dy −13..12`（`workplace/scratch/forms.py`）
——與腳本裡的 `form 1` 對上。

## 4. remake 實作

| 項目 | 位置 |
|---|---|
| 開場常令 | `internal/state/tactical.go` `beginTactical`：**玩家側**與**腳本側**都下命令 0（`tactical.Form`）；沒有腳本的**非玩家**側維持「攻擊」|

⚠ **沒有腳本的非玩家側不要一起改。** 那一側代表「規則層自己跑的 AI」，
把它改成站著不動會讓野戰 fixture 永遠打不完（既有的長跑測試會卡住）。

## 5. 驗證

| 方式 | 內容 |
|---|---|
| 對原版 ✅ | 小地圖第 40 步 **0.04%（8 px）**：兩側的部隊點都與原版對上（§3、§6）|
| 單元測試 ✅ | `TestScriptLineUsesRoleNotSideIndex`：玩家守城時腳本側的線要是 58 不是 5，且玩家當攻方時行為不變（正對照）|
| 迴歸 ✅ | `go test ./...` 全綠——野戰的長跑測試沒有因為改開場常令而卡住 |

## 6. ⭐ 連帶抓到的規則 bug：陣形線查表用了側編號

`lineX` 那兩組常數（玩家 5／28／48、腳本 58／36／16）綁的是
**玩家與腳本**，不是攻方守方，也不是 remake 的側編號——
原版的 side 0 永遠是玩家（[`56`](56-battlefield-rotation.md) §1）。
而 remake 的 side 0 是**攻方**，玩家守城時兩者剛好相反。

所以腳本的 `line` 指令要查**角色**那一列：`LineFor(b.Role(side), arg)`。
用側編號查的話，玩家守城時腳本會把自己的陣形線設成 X=5（玩家那一條），
整支軍團往對面走——症狀在小地圖上一眼可見，敵方的部隊點從 X=58 一路掉到 38。

接對之後腳本側走完 `line 0` → `form 1` → `order 全軍 命令0`，
在第 40 步站定，與原版**只差 8 個像素**。

| 步數 | `sb-minimap` |
|---:|---:|
| 0 | 1.60% |
| 24 | 1.09% |
| **40** | **0.04%（NEAR）** |
| 56 | 0.04% |

## 7. 未解

| 項目 | 現況 |
|---|---|
| 玩家側的開場常令 | 畫面上看起來是「站在陣形上」，但原版是哪一個命令碼（`Form`／`Holding`／`Guard`）沒有直接證據。⚠ **三個在開場那一幀畫出來一模一樣**——守陣沒有敵人靠近時也是站在陣形位置上，所以截圖分不出來 |
| ⤷ 試過的一條線索 | 原版 `probe-march/e10.png` 那一刻**攻方已折損 115 點兵力**（兵力條反推，[`../playtest/51`](../playtest/51-siege-deadlock.md) §2），而 remake 的守方站著不還手。把玩家側的開場常令改成守陣試跑（2026-08-27）：攻方只折損 **30 點**，離 115 還差得遠，而且守方仍然全滅。**所以開場常令不是唯一的成因**，先不改；真正的差距在雙方交手的損傷模型（同兵力同士氣 48 對 48，一邊全滅、一邊掉 5%）|
| 腳本節奏與原版的 tick 對應 | 第 40 步對上那一張截圖，但「原版的 40 個 tick 是多久」還沒對過（[`34`](34-speed-steps.md)）|
