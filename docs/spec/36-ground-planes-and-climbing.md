# 36 — 兩個平面的地面圖、導航位元與登城

**狀態：CONFORMED。** 已實作並有單測，實跑量測見
[`../playtest/30`](../playtest/30-ground-planes-implemented.md)。 建表演算法、導航圖、移動規則、登城條件都有機器碼出處，
而且拿三張原版攻城圖對過數字。

- 日期：2026-08-16
- 出處：[`../re/63`](../re/63-ground-plane-map.md)
  （`sub_1BBA6` `0001BBA6`、`sub_1BC39` `0001BC39`、`sub_1BCA6` `0001BCA6`、
  `sub_1BD07` `0001BD07`、`sub_1B1B1` `0001B1B1`、`sub_1B0D3` `0001B0D3`、
  `sub_1AF69` `0001AF69`、尋路 `0001BD46`–`0001BFF0`）
- 推論等級：演算法 **confirmed**；段相鄰關係 **強推論**（[`../re/63`](../re/63-ground-plane-map.md) §1）

## 1. 原版做什麼

戰場不是一張高度圖，是**兩個平面**：低平面（地面）與高平面（城牆頂）。
每一格每個平面存**一個 byte**：

```
bit 0–2  地面層          bit 3  這一格是門（可以上下）
bit 4 ←  bit 5 →  bit 6 ↑  bit 7 ↓     ← 這四個方向走得通
```

### 1.1 建表（每張戰場一次）

```
每格：
  rec = 圖塊屬性[圖塊]                    # 8 byte：[0] 層數、[1..7] 七層的子圖塊
  if 0xD0 ≤ 圖塊 ≤ 0xDF: rec = 圖塊屬性[圖塊 + 0x10]     # 城壁改讀對應的圖塊
  低 = 層 0–3 裡第一個「子圖塊 ≥ 0x70」的層號，找不到 → 無
  if 圖塊 ≥ 0xF0: 高 = 門標記(8)
  else:           高 = 層 4–6 裡第一個「子圖塊 ≥ 0x70」的層號，找不到 → 無

每格與四個鄰格：
  if 中心高 == 門標記 且 鄰格高 ≤ 6: 中心高 = 鄰格高
  低平面：兩邊都有地面且 |Δ| ≤ 1  → 設方向位元
  高平面：兩邊都有地面且 相等或鄰格是門 → 設方向位元

高平面正規化：
  統計有地面的格子，層 ≤ 4 與 > 4 哪邊多 → 目標高度 4 或 5
  高度不同的格子：是門就改成目標高度，不是門就整格清 0
```

### 1.2 尋路

在 **(格, 平面)** 上做 uniform-cost 搜尋：

| 邊 | 條件 | 成本 |
|---|---|---|
| 四方向 | 該方向的位元是 1 | 1 ＋ 佔用成本 |
| 換平面 | bit 3（門）| 兩個平面的地面層差 ＋ 佔用成本 |

**佔用成本**是另一張表（`word_1D2FE`）：有兵站著的格子是 **8**，其餘 0。
寫入端是移動落定的 `sub_1B240`，透過 `word_1D2FA` 加 `0x9000` 的位移
寫進去（舊格 0、新格 8，[`../re/63`](../re/63-ground-plane-map.md) §1）。
⇒ **尋路會繞開有兵的格子**，繞路的代價是 8 格。

### 1.3 移動一步（`sub_1B1B1`）

```
目標格任一平面有單位 → 失敗
g = 地面表[格, 平面]
g == 0 → 失敗
低平面：g 的層比自己高 → 升一層；低 → 降一層；相等 → 通過      # 沒有幅度上限
高平面：層必須相等，且目標圖塊不是未破的門（0xF0–0xF7），且命令 ≠ 6
```

### 1.4 上下（`sub_1AF69` → `sub_1B0D3`／`sub_1B116`）

```
移動順序：先 X、再 Y，兩軸都到位才做 Z
兵種 ≤ 0x12（大將與騎馬）→ 完全不做 Z
往上：目前這一格的圖塊必須 ≥ 0xF0（門），成功則 Z++、進高平面
往下：同樣要 ≥ 0xF0，回到低平面時清掉高平面旗標
```

⭐ **登城只在門那一格發生。城牆本體爬不上去，撞穿一格也開不出通道。**

## 2. remake 做什麼

| 項目 | 做法 |
|---|---|
| 資料 | `internal/assets/battle` 多一支 `TileLayers(n)`，回傳那一組圖塊的 `[256][7]byte` 子圖塊表 |
| 建表 | `Field` 多兩張導航表（方向位元 ＋ 門旗標）與兩張地面層表。**層數另外存**：原版靠「整個 byte 是 0」代表沒有地面，而 remake 的 Z 從 0 起算，擠在同一個 byte 會撞號 |
| Z 的意義 | **兵腳下佔的那一層**，與原版同一個座標系（頂面那一層不擋人，兵站在它上面）|
| 每層擋不擋人 | 照子圖塊重算（`sub_1BB6D` 的 0／≥ 0x70 不擋），不再用「堆疊高度以下全實心」|
| 沒有圖塊層資料時 | `NewField(stack)` 這種合成戰場：地面層 ＝ 堆疊高度、堆到 4 層以上算牆頂、有圖塊值就照 `≥ 0xF0` 認門。**同一套演算法，只是輸入退化** |
| 尋路 | `FindPath` 改成在 (格, 平面) 上搜尋，邊照 §1.2 |
| 移動 | `tryMove` 照 §1.3：先查目標格**頭頂那一層**擋不擋（原版 `es:[bx+1000h]`），再讀所在平面的地面層同步高度 |
| 上下 | `Soldier` 多一個 `OnWall`；X／Y 都到位之後才做 Z，且只在門那一格 |
| 兵種閘 | `CanClimb`（兵種 > 0x12）改成**只擋純 Z 移動**，不擋水平移動 |

### 2.1 這修掉什麼

`docs/playtest/28` 量到 remake 的攻方**一次都沒碰到城壁**。
接上之後同一組測量打掉 480 點耐久、最低的一段只剩 20
（[`../playtest/30`](../playtest/30-ground-planes-implemented.md)）。

⚠ **攻城還是打不下來**，但卡點換成「攻方大多數不前進」——
那是攻擊命令的目標選擇，與地形無關。

### 2.2 會翻掉的既有測試

| 測試 | 為什麼 | 怎麼改 |
|---|---|---|
| `TestCavalryCannotClimb` | 它把「騎馬爬不上牆」釘在**水平移動**上，而原版擋的是**純 Z 移動**（`../re/63` §4）| 改成「城壁那一格誰都走不上去」＋ 新的 `TestClimbOnlyAtGateCells` |
| `TestPathUnreachable` | 它假設「一層高的坎擋得住爬不上去的兵」 | 高度差 ≤ 1 的連通與兵種無關，改成兩種兵都過得去 |
| `TestHorizontalStepAdjustsOneLevelForNonClimber` | 用高度差 2 當「跨一層」 | 改成真正的一層 |

**訂正的證據**：`sub_1AF69` 的兵種閘明文只包住 `sub_1B0D3`／`sub_1B116`
那兩支，而 `sub_1B1B1`（水平）整支沒有兵種判斷。

## 3. 驗證

| 方式 | 內容 |
|---|---|
| 單元測試 | `TestGroundPlanesMatchRawAlgorithm`：拿原版 `BATTLE.MAP`／`BATTLE.MDL` 的戰場 0／5／20，低平面分佈與高平面分佈要等於機器碼演算法算出來的 |
| 單元測試 | `TestHighPlaneIsNormalisedToOneLevel`：牆頂正規化後只有一個高度 |
| 單元測試 | `TestOnlyGateCellsAllowClimb`：門格能上下、城壁格不能 |
| 單元測試 | `TestNavBitsFollowLevelRules`：低平面連通一定 `|Δ| ≤ 1` |
| 單元測試 | `TestSiegeWallBlocksUntilBroken`：尋路穿得過城門那一列（地面層表是打壞後的樣子），但通行層擋著 |
| 單元測試 | `TestClimbOnlyAtGateCells`：門格能上下、城壁格不能、大將與騎馬不做 |
| 實跑 | [`../playtest/30`](../playtest/30-ground-planes-implemented.md)：三張攻城圖各打掉 480 點耐久（之前是 0）|
| 截圖 | 攻城畫面重跑一張，確認換座標系之後兵沒有陷進地形 |

## 4. 未解

| 項目 | 現況 |
|---|---|
| 攻方大多數不前進 | [`../playtest/30`](../playtest/30-ground-planes-implemented.md) §3。目標選擇的問題，與地形無關 |
| 打壞城壁之後不重算地面表 | 原版不重算，而且**不影響結果**：城壁的地面層本來就是拿打壞後的圖塊算的。remake 為了合成戰場仍會重算，在真實資料上是恆等變換 |
| 命令 6 為什麼擋高平面橫移 | 命令碼 6 是什麼沒對過 |
| `sub_1B186`／`sub_1B15D` | 爬升／下降時檢查上下一層的那兩支沒讀，remake 用「目標平面有地面」代替 |
