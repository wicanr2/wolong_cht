# 81 — 災害的實際數值：機率、marker 量、持續與距離衰減

**狀態：CONFORMED（2026-08-25）。整條鏈的數值都有機器碼出處，
remake 已實作；本輪修掉一處距離算錯（min → max）。**

- 日期：2026-08-25
- 出處：`KI.EXE`（DOS/V，SHA-256
  `fffeba985231cda4d636e93d10f598470b1f691d00275e4aa38e285893d43868`）：
  `sub_12286`（火災／暴動產生）、`sub_122DB`（暴風雨產生）、
  `sub_134B1`（事件 12 處理）、`sub_134A6` → `sub_1237E`（事件 11 →
  逐據點 marker）、`sub_14269`（marker 套用，[`../re/07`](../re/07-monthly-settlement.md) §19.1）
- 推論等級：**confirmed**（逐行讀出）

## 1. 火災／暴動（事件 12）

| 項目 | 值 | 出處 |
|---|---|---|
| 產生機率（每據點每月各擲一次）| `24/256 × max(0, 64 − 防災值)/64`（火災）；上昇值存值版（暴動）| `sub_12286` |
| **marker 量** | `rand(0..7) ＋ 4` ＝ **4–11** | `sub_134B1` 的 `and al,7 / add al,4` |
| **延燒時間** | 重排自己一筆延遲 `rand(0..7)＋6` ＝ **6–13** 的清除事件（佇列每 10 tick 取一筆）；到期（Code 高 byte 0）才清 marker | `sub_134B1` 尾段 `sub_1301C(ax=0Ch)` |
| 傷害套用 | marker 掛著期間**每次據點輪到（約每日）套一次**：先扣防災值；不足的差額扣上昇存值、生產力 −`(差額×生產力高byte)>>2`、城兵 −`差額/2` | `sub_14269` |
| 玩家通知 | `cx ＝ 型＋0x46`：火災 #71、暴動 #72 | `sub_134B1` |

## 2. 暴風雨（事件 11／0x0B）

| 項目 | 值 | 出處 |
|---|---|---|
| 產生（每月最多一次）| `1/2 × 3/4`（al < 192 均勻選據點）；⭐ 選中據點 **X < 0xC0 要再過一次 1/2**——暴風雨偏東岸 | `sub_122DB` |
| 持續 | `(rand(0..7)＋8) × 4` ＝ **32–60**（佇列延遲單位）| `sub_122DB` 的 `and al,7/add al,8/shl×2` |
| **強度**（事件 11 每次處理重擲）| `rand(0..15) ＋ 0x18` ＝ **24–39** | `sub_134A6` |
| **逐據點 marker** | 距離 `d ＝ max(|dx|,|dy|)`（**切比雪夫**，對暴風雨框中心）；`d ≤ 20` 才吃，marker ＝ `強度 − d/2`（下限 0）| `sub_1237E` 的 `cmp dx,bx / jnb / mov dx,bx`——**取大不取小** |
| 風暴結束 | 以強度 0 再跑一次 `sub_1237E`（全數清 0），框重設到圖外 | `sub_122DB` 開頭 |

## 3. remake 實作與本輪修正

| 項目 | 位置 |
|---|---|
| 全部量值 | `internal/state/events.go`：`applyQueuedStormMarker`／`applyQueuedDisasterMarker`；套用在 `applyCityDisasterEffect` |
| **修正** | 暴風雨距離先前取了 `min(dx,dy)`，原版是**切比雪夫 `max(dx,dy)`**——min 會把對角遠處的據點也圈進來且衰減不足。改為 max 並補單測 |

## 4. 驗證

| 方式 | 內容 |
|---|---|
| 單元測試 | `TestStormMarkerUsesChebyshevDistance`：對角距離 (12,3) 的據點衰減按 12 算，(3,12) 同；d>20 不吃 |
| 靜態 | 量值逐項對 `sub_12286`／`sub_134B1`／`sub_134A6`／`sub_1237E` 的立即值 |

## 5. 未解

| 項目 | 現況 |
|---|---|
| 事件 11 在風暴期間被重排的節奏 | 產生端一次排 32–60 延遲；期間事件 11 重擲強度的觸發頻率沒逐格讀（推定隨佇列輪到）|
