# 27 — 君主選擇視窗

**狀態：CONFORMED。版面已照原版實作並有契約測試；
「自定」（軍師命名）還沒接（§5）。**

- 日期：2026-08-15
- 出處：[`docs/re/53`](../re/53-lord-select-window.md)（版面、判定）、
  [`docs/formats/08`](../formats/08-sinario-save.md)（勢力記錄欄位）
- 推論等級：**confirmed（靜態）**——版面出自顯示清單，數值座標由 VRAM 位移換算

## 1. 版面

視窗 `sub_1895D(al=1Fh, bx=5, dx=0Ah, cx=0C0Fh)` → **(160, 112, 240, 192)**，
顯示清單場景 8 的原點是 (168, 120)。

### 1.1 靜態層

| 元件 | 座標 | op |
|---|---|---|
| 上半的大底框 | (176, 128) 208×104 | 03 |
| 左下的底框 | (176, 240) 144×48 | 03 |
| 垂直線 | (247, 240) 長 48 | 06 |
| 「自定」「確定」的底 ＋ 立體外框 | (328, 248)／(328, 272) 48×16 | 03 ＋ 04 |
| 「君主」 | (184, 200) | 08 |
| 「軍師」 | (312, 128) | 08 |
| 「首　都」「武將數」「據點數」 | (184, 240／256／272) | 08 |
| 「自定」「確定」 | (336, 248)／(336, 272) | 08 |

### 1.2 動態層

| 值 | 座標 | 位數 | 來源 |
|---|---|---:|---|
| 君主頭像 | (184, 128) | — | 勢力 `Lord` → `General.Portrait` |
| 君主名 | (200, 216) | — | `World.LordName` |
| 軍師頭像 | (312, 168) | — | 勢力 `Advisor`；**`0x7F`（無）就不畫** |
| 軍師名 | (328, 144) | — | 同上 |
| 首都名 | (264, 240) | — | 勢力 `Capital` 的據點名 |
| 武將數 | (272, 256) | 3 | 勢力 `Generals` |
| 據點數 | (272, 272) | 3 | 勢力 `Cities` |

## 2. 互動

| 熱區 | 位置 | 作用 |
|---:|---|---|
| `0x20` | (328, 272) 48×16 | **確定**（下面那顆）|
| `0x21` | (328, 248) 48×16 | **自定**（上面那顆）|

⚠ 熱區編號與畫面上下**顛倒**。

⭐ 按「確定」時，若軍師欄是 `0x7F` 且名字還是空的，原版會**當成按了自定**
——先去命名再說（[`docs/re/53`](../re/53-lord-select-window.md) §3）。

## 3. remake 要做什麼

`cmd/wlgame/lordcard.go`（新增）畫這張卡，啟動殼層的「選君主」那一頁用它。

| 項目 | 作法 |
|---|---|
| 版面 | 照 §1 |
| 換勢力 | remake 差異：**上下鍵**換一個。原版怎麼換在 `sub_11AC3` 裡，未讀 |
| 兩顆按鈕 | 畫出來了，但 remake 這一輪只有「確定」有效 |
| 劇本標題 | 顯示在卡片上方，**從區塊 `+0x40` 讀**（[`25`](25-slot-select-window.md) §1.2）|

⭐ **劇本名先前是硬編的四個字串**（「第一章　呂布歸天」…），
而原版寫的是「第一章・　　「呂布歸天」之卷」——連格式都不一樣。
現在 `game.scenarioTitles` 啟動時從 `SINARIO.DAT` 讀。

## 4. 驗證

| 方式 | 內容 |
|---|---|
| 單元測試 | `TestLordCardLayout`（版面契約，含「自定在確定上面」與數字不穿到按鈕欄）|
| 截圖 | `WOLONG_SHOT_CMD=wlgame tools/shot.sh <out> KEYS=Return,Return,Return` → 曹操／荀彧／許昌／武將 12／據點 14，標題「第一章・　　「呂布歸天」之卷」（**本地產物，`*.png` 不進版控**）|
| 對原版 | **靜態**：座標逐項出自機器碼 |

## 5. 未解

| 項目 | 現況 |
|---|---|
| 「自定」 | 軍師命名（場景 9 的注音輸入）還沒做，這顆按鈕目前無效 |
| 換勢力 | 原版的換法在 `sub_11AC3`，未讀 |
| 頭像尺寸 | 軍師頭像的下緣照原版座標會略微超出那個 208×104 的底框；沒有 oracle 可比，先照機器碼畫 |
