# 154 — 原版的滑鼠游標：14×14 紅箭頭，畫不畫由 `byte_20100` 決定

**狀態：CONFORMED（對拍端）。** 逐像素對拍上反覆出現的 95 px 殘差就是它。
圖樣從兩張不同背景的原版擷取抽出來、逐格相同；
**remake 目前只在對拍時畫**（`-cursor X,Y`），遊玩時維持系統游標——
理由見 §4。

- 日期：2026-09-07
- 出處：[`../re/88`](../re/88-mouse-cursor-visibility.md)（顯示旗標 `byte_20100`、
  繪製本體 `sub_20137`）；圖樣是**逐像素抽出來的**（§1）
- 推論等級：**confirmed**（圖樣兩個背景各抽一次，逐格相同）
- 驗證：[`../playtest/107`](../playtest/107-cursor-parity.md)
- 相關：[`149`](149-march-target-map-picker.md)（大地圖上的 15×15 空心框，另一種游標）

## 1. 圖樣：14×14，色 15 描邊 ＋ 色 10 填色

`W` ＝ 色 15 `(243,243,243)`、`#` ＝ 色 10 `(211,0,0)`、`.` ＝ 不畫：

```
WW............
W#WW..........
.W##WW........
.W####WW......
..W#####WW....
..W#######WW..
...W########WW
...W#######W..
....W#####W...
....W######W..
.....W##W###W.
.....W#W.W###W
......W...W##W
......W....WW.
```

**熱點在左上角 (0, 0)**：`stap:200,112` 之後這一格就是圖樣的第一個 `W`。

⭐ 兩張背景完全不同的原版擷取（清單的米色底 `orig-help7b`、
選字表的米色底 `orig-naming`）抽出來的圖樣**逐格相同**，
所以 `.` 是真的不畫，不是「剛好與背景同色」。

## 2. 什麼時候畫

`byte_20100 ≠ 0`（[`../re/88`](../re/88-mouse-cursor-visibility.md)）。
實測過的十個狀態：

| 狀態 | 旗標 |
|---|--:|
| 大地圖（剛進遊戲、切橫幅）| 0 |
| 指令列的彈出選單開著 | 1 |
| 清單開著、等待點選 | 0 |
| 清單選走後**回上層迴圈**（人事任命）| 1 |
| 清單選走後**留在原地**（武將一覽）| 0 |
| 兩項選單（遊戲結束）開著 | 0 |
| 請求協助的第二步 | 1 |
| 說服場景／判決畫面 | 0 |
| 軍師命名視窗 | 1 |
| 殼層的四槽視窗（LOAD DATA／選劇本）| 1 |

## 3. remake 實作

| 項目 | 位置 |
|---|---|
| 圖樣 | `cmd/wlgame/cursor.go` 的 `arrowCursor`（逐列字串，與 §1 同形）|
| 繪製 | 同檔 `drawMouseCursor`——**只在 `cursorAt` 有值時畫** |
| fixture | `-cursor X,Y`（對拍用；headless 沒有真實滑鼠位置）|
| 差異 | 遊玩時仍是系統游標（§4）|

## 4. ⚠ 為什麼遊玩時不自繪

原版在「清單等待點選」時旗標是 0，也就是**畫面上沒有游標**——
玩家要靠反白列。這在真機上說得通與否，**沒有實機佐證**：
dosgolem 是否完整模擬 `INT 33h AH=0Ch` 設的中斷副程式（滑鼠移動時
由驅動回呼重畫）還沒查，所以「旗標 0 時移動也不重畫」有可能是
oracle 的限制而不是原版行為。

⭐ **「旗標 1 → 有游標」是可信的**（十個狀態逐格對上），
所以對拍端先接這一半：`-cursor` 讓那幾張從 95 px 收到 0 px。
遊玩端要等「旗標 0 到底畫不畫」有實機佐證再動——
現在改成自繪反而可能讓玩家在清單上看不到游標。

## 5. 未解

| 項目 | 現況 |
|---|---|
| 旗標 0 時真機會不會重畫 | §4。要一次真機（或另一個模擬器）的實測才能定案 |
| 大地圖上的那一種 | 15×15 白色空心框（[`149`](149-march-target-map-picker.md)），**什麼時候換成箭頭**沒對過 |
| 遊玩端的自繪 | 等 §4 定案 |
