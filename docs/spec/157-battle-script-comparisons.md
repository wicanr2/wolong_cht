# 157 — 戰術 AI 分支比較方向勘誤

**狀態：CONFORMED。原版指令、單項隔離重播、正式兩條野戰重播及攻城回歸已通過。**

- 日期：2026-09-08
- 範圍：戰術腳本指令 10 的參數 3／4；不改兵力、武將、亂數或委任模式。
- 輸入：dosv `KI.EXE`，SHA-256 `fffeba985231cda4d636e93d10f598470b1f691d00275e4aa38e285893d43868`。
- 工具／位址：IDA Pro 9.4，IDA DOS/V linear；dosgolem `fd746405cf0ef9e535121e215b935b0bf59cd1de`。
- 證據：`workplace/delegated-battle-20260908/ida-script/battle-cause.json`，
  `prototype-script-trace/result.json`、`prototype-branch-only/result.json`。

## 已證實的契約

| 參數 | 原始定位與運算元 | 正確語意 | 舊 remake |
|---|---|---|---|
| 3 | `0001A5B8 cmp cs:byte_1D315, ah`；`0001A5BD jnb loc_1A5CA` | 無號條件值 ≥ 運算元時跳躍，包含相等 | `<` |
| 4 | `0001A5C1 cmp cs:byte_1D315, ah`；`0001A5C6 jbe loc_1A5CA` | 無號條件值 ≤ 運算元時跳躍，包含相等 | `>` |

推論等級：**已證實**，依原始 bytes、IDA 跳表分支與旗標語意。
保留原始名稱與位址；舊 re/11 §3.4 的 `<／>` 是錯誤解讀，追加勘誤而不抹去歷史。
不是有號數比較；曾測試的有號假說不採用。

## 玩家可見的因果驗證

受控呂布／夏侯惇野戰，在戰術畫面後完全不下令。
舊腳本讀到兵力條件 255，遇參數 3、運算元 100，本應跳過退卻；
舊 `<` 不跳而執行全軍退卻，倒數完判呂布輸。
只將兩個比較改成原版 `≥／≤`、其餘正式規則不變的隔離重播，
呂布改為勝方，夏侯惇體力 48 後退卻，1037 拍結束。
此結果證明勝負反轉的直接原因，不證明逐拍戰況完全一致。

## 實作與驗收閘

- 指令 10 的參數 3／4 分別使用無號 `≥／≤`；識別字同步為 `cmpGE／cmpLE`。
- 邊界驗證涵蓋小於、相等、大於，以及 0、255；用原始 opcode 送入腳本，檢查是否誤下退卻。
- 受控野戰不下令與原先同拍攻擊令兩條重播均須重新跑到結算。
- 攻城與既有腳本測試須通過；全逐拍差異仍另列，不以勝方相同取代完整對拍。

本規格不改存檔格式、公開發行、Android 包裝或 Windows／macOS 人工驗收分工。
