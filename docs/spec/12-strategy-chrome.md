# 12 — 主畫面的視窗外框、指令列與右欄

**狀態：CONFORMED。主畫面的四個常駐視窗矩形、指令列版面與縮小地圖／勢力篩選鈕的
位置全部由機器碼定死（[`docs/re/47`](../re/47-main-screen-window-registry.md)），
不再有「從影片量的」版面常數。外框的圖塊來源與貼法也已解出（`ICONGRF` 段 3）。
各視窗內部的靜態排版也解出來了——那是一份顯示清單
（[`docs/re/48`](../re/48-window-display-list.md)），不是估值。
畫面已與原版逐像素對過（§6）。**

- 日期：2026-08-15（對拍結果 2026-08-17）
- 出處：[`docs/re/47`](../re/47-main-screen-window-registry.md)（`sub_18755`／`sub_1E3D7`／`sub_1895D`／`sub_11BE0`）、
  [`docs/re/46`](../re/46-strategy-chrome-cell-layer.md)（框線圖塊）
- 推論等級：**矩形與呼叫鏈 confirmed（靜態）**；原版執行期畫面**逐像素對過**

## 1. 主畫面是「橫幅 ＋ 四個可開關的常駐視窗 ＋ 大地圖」

大地圖鋪滿橫幅以下的整片畫面，四個視窗疊在它上面，各自可以關掉。
開關是橫幅右側的 32×32 格，左鍵開、右鍵關；**開新遊戲時四個都是關的**。
開關本身的規格另見 [`13-main-window-toggles.md`](13-main-window-toggles.md)。

| 元件 | 矩形 | 來源 |
|---|---|---|
| 橫幅 | (0, 0, 640, 32) | `sub_18755` 的熱區 6 |
| 開關 1–5 | (336 + 32n, 0, 32, 32) | `sub_18755` 的迴圈；第 5 格接 `nullsub_1` |
| 命令視窗 | **(0, 32, 432, 32)** | `sub_1614A` → `sub_1895D(cx=021Bh)` |
| 縮小地圖視窗 | **(432, 32, 208, 160)** | `sub_15A3A` → `sub_1895D(cx=0A0Dh)` |
| 自勢力情報視窗 | **(432, 192, 208, 208)** | `sub_15E2D` → `sub_1895D(cx=0D0Dh)` |
| 系統視窗（模態）| (208, 112, 208, 192) | `sub_160CC` → `sub_1895D(cx=0C0Dh)` |
| 大地圖 | **(0, 32, 640, 368) ＝ 40×23 格** | `sub_1D615` 的迴圈；視窗是疊在它上面的層（[`docs/re/47`](../re/47-main-screen-window-registry.md) §3.2）|

右欄三段相加 `32 + 160 + 208 = 400`，**剛好鋪滿畫面高度**——
這是「矩形讀對了」的算術檢查，不是巧合。

## 2. 指令列

```
框    sub_1895D(樣式=0x0B, X=0,    Y=0→32, 尺寸=0x021B)   → (0, 32, 432, 32)
文字  sub_106F5(字串=cs:6181h, X=8, Y=40, 屬性=0x0F01)
熱區  sub_1E3D7(編號=0x0C, X=24, Y=40, 尺寸=0x0230)       → (24, 40, 384, 16)
分派  sub_161CA: 索引 ＝ (x − 24) ÷ 48；高亮寬 40、節距 48、高 16
```

`cs:6181h` 是**一整串**，不是八個標籤：

```
　 進言　人事　財政　編成　軍團　據點　武將　勢力 　
↑全形 ↑半形    ↑ 兩個字之間沒有空格；詞與詞之間才有一個全形空格
```

| 項目 | 值 |
|---|---:|
| 字串起點 X | 8 |
| 開頭空白 | 24（全形 16 ＋ 半形 8）|
| **第 n 個詞的 X** | **32 + 48n** |
| 詞寬 | 32 |
| 節距 | 48 |
| 文字列 Y | 40（高 16）|
| 第 n 格命中範圍 | x ∈ [24 + 48n, 72 + 48n)，y ∈ [40, 56) |

驗算：`8 + 24 + 8×32 + 7×16 + 24 = 424 < 432`，落在框內。

## 3. 縮小地圖視窗的內部

| 元件 | 矩形 | 來源 |
|---|---|---|
| 縮小地圖本身 | (440, 40, 192, 128) | `sub_15A3A` 的熱區 `0x16` |
| 勢力篩選鈕 | (536, 168, 96, 16) | 同上的熱區 `0x17` |

`sub_15C58` 把世界座標換算成畫面座標的式子是 `X = 世界X ÷ 2 + 440`、
`Y = 世界Y ÷ 2 + 40`，與上表的縮小地圖左上角一致，是第二條獨立證據。

篩選鈕只有右半格有熱區，對應說明書 3.4 的
「ウインドウ**右下の青いボタン**を押すことにより、特定勢力の拠点のみ…」。

## 3.1 自勢力情報視窗的內部

視窗內容分兩層：**靜態的那一層是一份顯示清單**（`sub_10337(al=0)`，
[`docs/re/48`](../re/48-window-display-list.md) §3），動態欄位由四支常式畫。

| 元件 | 位置 | 來源 |
|---|---|---|
| 場景範圍 | (440, 200) 192×192 | 顯示清單 op `07` |
| 信賴度量條的槽 | (448, 288) 176×10 | 顯示清單 op `03` |
| 資金／預備兵黑底 | (448, 304) 176×80 | 顯示清單 op `03` |
| 標籤「君主／首都／軍師」| (512, 208 / 224 / 240) | 顯示清單 op `08` |
| 標籤「信賴度」| (448, 272) | 顯示清單 op `08` |
| 標籤「資金」「預備兵」| (456, 312 / 328) | 顯示清單 op `08` |
| 四張 24×16 圖示（天秤／馬／弓／步）| (528, 312 / 328 / 344 / 360) | 顯示清單 op `09` → `ICONGRF` 段 3 `0x1BA0` |
| 君主／首都／軍師 名 | (576, 208 / 224 / 240) | `sub_15EB7` |
| 信賴度量條 | (456, 292)，長度 `(信賴度×100 + 0x9F) ÷ 0xA0` | `sub_15F27` |
| 資金（7 位，右端 616）| (560, 312) | `sub_15F5D` |
| 預備兵 ×3（6 位，右端 616）| (568, 328 / 344 / 360) | `sub_15F7F` |

三個規則層面的細節：**信賴度是量條不是數字**、**資金是 32 位且負數換色**、
**預備兵顯示時乘以 10**。第三項只是顯示換算：`sub_14698` 編成時從預備兵扣掉的
數與寫進軍團槽的是同一個（軍團槽 1 byte、上限 100），所以狀態層的單位是
「點」，一點 10 人。

標籤與動態欄位的座標來自**兩份互相獨立的資料**（顯示清單的記錄、
繪製常式的立即值），而「君主」在 512、君主名在 576 這種配對逐列對得上——
兩邊都沒讀錯。

## 4. 外框怎麼畫

```
上下邊（sub_10C60）：每個 16 px 粗格貼兩塊 si=0x00 的 motif
左右邊（sub_10C77）：si=0x20 柱頭 → 迴圈 si=0x40／0x60 柱身 → si=0x20 柱頭
四個 si 是段 3 的 0x06C0／0x06E0／0x0700／0x0720（docs/formats/03 §5.4）
blit 走 sub_1F9B0 → EGA Set/Reset → VRAM 0A0C8h
```

`sub_1895D` 另外還寫一張以 16×16 格為單位的屬性表（`cs:word_1D84E`），
**與框線像素無關**：它記的是「這一格歸地圖管」與「這一格髒了」，
地圖與視窗的互蓋就是靠它（[`docs/re/47`](../re/47-main-screen-window-registry.md) §3.2）。

## 5. remake 實作

| 項目 | 位置 | 現況 |
|---|---|---|
| 呈現層 | `cmd/wlgame/strategyhud.go` 的 `drawNaturalStrategyHUD` | 四個視窗是可開關的疊層（[`13`](13-main-window-toggles.md)）|
| 外框 | `internal/ui/chrome` | 照原版素材貼段 3 的三塊圖塊，順序與 `sub_10C77` 一致 |
| 版面常數 | 同上檔案的 `const` 區 | **已全部改成 §1–§3 的原版值** |
| 資源圖示 | `library.DOSVResourceIcon` | **用原版素材**（段 3 `0x1BA0` 起四張）|
| 顏色 | `g.paletteInk(索引, fallback)` | 一律查 `GAMEPAL.BRG`，硬寫的 RGBA 只是缺素材時的 fallback（[`54`](54-ui-colours-from-palette.md)）|
| 信賴度量條 | `drawStrategyInfo` | 槽 176×10（顯示清單 op 03）＋ 條高 2 px，長度照原版的 `(信賴度×100 + 0x9F) ÷ 0xA0`（`sub_10AAA`／`sub_15F27`）|
| 縮小地圖的據點標記 | `drawMinimapMarkers` | 4×4 外框 ＋ 中心 2×2，四組顏色照 `sub_15CE0` 的位元組對（[`35`](35-strategy-minimap.md)）|

## 6. 驗證

| 方式 | 證據 |
|---|---|
| 單元測試 | `cmd/wlgame/strategyhud_test.go` 把 §1–§3 的矩形釘成常數契約 |
| 對原版靜態 | 三條算術檢查：右欄三段和 ＝ 400、字串尾端 424 < 432、熱區上下限與 `sub_161CA` 一致 |
| 對原版執行期 ✅ | 開局五區逐像素相同（[`docs/playtest/37`](../playtest/37-main-screen-parity.md)）；三個視窗開著時命令列、自勢力情報、縮小地圖三區逐像素相同（[`38`](../playtest/38-window-parity.md)）；系統選單開著時選單本身 ＋ 四區逐像素相同（[`39`](../playtest/39-system-window-parity.md)）|

## 7. 未解

| 項目 | 現況 |
|---|---|
| 樣式碼的值域 | 只確定 `0`＝擦除、`0x0B`＝命令、`0x0Bh`／`0x10h`／`0x15h`／`0x1Fh` 各自出現在哪個視窗已知，完整值域未列 |
