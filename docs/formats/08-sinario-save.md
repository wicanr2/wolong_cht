# 08 — `SINARIO.DAT` / `SAVE.DAT`：劇本與存檔

**狀態：整體結構 confirmed，武將能力值 confirmed，其餘欄位進行中。**

- 日期：2026-08-07
- 兩檔**大小相同（88,832 B）、結構相同**，`cmp` 差 12,566 byte。
  `SINARIO.DAT` 是四個劇本的初始狀態，`SAVE.DAT` 是玩家存檔。

## 0. 區塊的三段式佈局（confirmed）

`sub_18CAE`（載入）／`sub_18CFF`（存檔）用同一組位移，見 `docs/re/06` §7：

| 檔案位移 | 長度 | 去向 |
|---|---:|---|
| `+0x0000` | 59 | `cs:0CF0h`——**時鐘 ＋ 全域狀態** |
| `+0x0080` | 21,056 | `ds:0D52h` 段——**勢力表 ＋ 據點表 ＋ 武將表** |
| `+0x52C0` | 1,024 | `ds:0D56h` 段 ＝ **事件佇列**（256 筆 × 4 B） |

```
0x80 + 0x5240 + 0x400 = 0x56C0 = 22,208 ✅
```

⚠ **`+0x3B`–`+0x7F` 這 69 byte 不會被載入**（第 ① 塊只讀 59 B，
第 ② 塊從 `+0x80` 開始）。劇本標題字串就落在這個空隙裡（`+0x40`）——
**它是給選單畫面看的，不是遊戲狀態**。

### 事件佇列（`+0x52C0`，256 筆 × 4 B）

出貨的劇本檔裡全零，因為開局沒有待處理事件。結構出自
`sub_12FBF`（入佇列）、`sub_1301C`（入佇列，可用全部 256 格）、
`sub_1304E`（依代碼與參數搜尋）：

| 偏移 | 內容 |
|---|---|
| `+0` | 事件代碼（u16），`0` ＝ 空格 |
| `+2` | 參數（u16），通常是據點編號或武將編號 |

入佇列時 `bl` 指定起始格（`bl × 4`），`0xFF` 表示隨機格，
然後往後線性探測找空格。
`sub_12FBF` 只用前 64 格（`bx < 0x100`），`sub_1301C` 用全部 256 格。

已知的事件代碼：

| 代碼 | 來源 | 意思 |
|---|---|---|
| `0x010C` | `sub_12286` | 火災 |
| `0x020C` | `sub_12286` | 暴動 |
| `0x000B` | `sub_122DB` | 暴風雨 |
| `0x0003` | `sub_12E89` | **停戰提案**（訊息 43「與{3}停戰交涉的結果」直接定名） |
| `0x0004` | `sub_15715` | 內政官要錢 |
| `0x0005` | `sub_1578F` | 外交官要錢 |
| `0x000D` | `sub_157FE` | 資金深度赤字時的事件 |

**事件的處理端還沒解**——那是內政官與災害實際效果的所在。

## 1. 檔案 ＝ 4 個劇本區塊 × 22,208 B

```
4 × 0x56C0 = 0x15B00 = 88,832   ← 完全等於檔案大小
```

| 劇本 | 區塊起點 | 據點表 | 武將表 |
|---|---|---|---|
| 1 呂布歸天 | `0x00000` | `0x008C0` | `0x042C0` |
| 2 赤壁之戰 | `0x056C0` | `0x05F80` | `0x09980` |
| 3 蜀地偏安 | `0x0AD80` | `0x0B640` | `0x0F040` |
| 4 劉禪繼位 | `0x10440` | `0x10D00` | `0x14700` |

區塊內固定佈局（相對區塊起點）：

| 偏移 | 長度 | 內容 |
|---|---:|---|
| `+0x0000` | 8 | **遊戲時鐘**（見 §1.1） |
| `+0x0008` | 51 | 未解的全域狀態（一起載入 `cs:0CF0h`） |
| `+0x003B` | 69 | **不載入**。劇本標題在 `+0x40`（「第一章」「呂布歸天」） |
| `+0x0080` | 2,112 | **勢力表：22 筆 × 64 B**（`docs/re/06` §5）＋ 其後未解 |
| `+0x08C0` | 6,144 | **據點表：192 筆 × 32 B**（段內偏移 `0x840`），欄位見 §1.6 |
| `+0x1EC0` | 7,168 | 未解 |
| `+0x3AC0`…`+0x42C0` | — | 未解 |
| `+0x42C0` | 4,096 | **武將表：127 筆 × 32 B**（第 127 筆是空的終止筆） |
| `+0x52C0` | 1,024 | 全零 |

**192 個據點**與維基說的「城池 192 個」一致 ✅

## 1.1 遊戲時鐘（區塊最前面 8 byte，**全部 confirmed**）

反組譯 `sub_11D8E` 與 `sub_11E17` 直接讀出來的，見 `docs/re/06`。
劇本區塊的前 59 byte 原封不動載入 `cs:0CF0h`，所以檔案偏移就是 ds 偏移。

| 偏移 | 型別 | 內容 |
|---|---|---|
| `+0x00` | u8 | **日** |
| `+0x01` | u8 | **該月天數** |
| `+0x02` | u8 | **子刻**（0–8） |
| `+0x03` | u8 | **時**（1–24） |
| `+0x04` | u16 | **月** |
| `+0x06` | u16 | **年** |

> ⚠ **修正**：本文件先前把 `+0x00` 記成「恆為 1，未解」、`+0x03` 記成「日」。
> 兩個欄位在四個劇本裡都是 `1`（所有劇本都從 1 日開始，且「時」的初值是 1），
> **光看資料分不出來**。反組譯定案為 `+0x00` 是日、`+0x03` 是時。

四個劇本的實際值（**兩版完全相同**）：

| 劇本 | 年 | 月 | 日 | `+0x01` | 該月天數 |
|---|---:|---:|---:|---:|---:|
| 1 呂布歸天 | 196 | 4 | 1 | 30 | 30 ✔ |
| 2 赤壁之戰 | 208 | 9 | 1 | 30 | 30 ✔ |
| 3 蜀地偏安 | 212 | 6 | 1 | 30 | 30 ✔ |
| 4 劉禪繼位 | 225 | 5 | 1 | 31 | 31 ✔ |

### 怎麼定案的

用日文說明書 p.3 的 `NEW GAME` 畫面當已知值反查。
四組年月日裡有三組直接對上，**而 `+0x01` 那一欄是獨立的交叉驗證**——
30/30/30/31 恰好是 4、9、6、5 月的天數。
月份欄位若讀錯，天數就不會吻合。

### ⚠ 說明書的兩個日期與資料不符

| 劇本 | 說明書 | **資料（兩版皆是）** |
|---|---|---|
| 2 赤壁之戰 | 208 年 **10** 月 1 日 | 208 年 **9** 月 1 日 |
| 4 劉禪繼位 | **223** 年 5 月 1 日 | **225** 年 5 月 1 日 |

300 dpi 重掃確認過不是判讀錯誤，說明書截圖上寫的就是 `10月` 與 `223年`。
**以資料為準**（`CLAUDE.md` §5：一手資料贏二手推論）。
`+0x01` 的天數欄位讓資料這一側自洽，說明書那一側沒有交叉證據。

推測是說明書的截圖取自送印前的版本，之後資料被改過——
PC-98 與 DOS/V 兩版**完全一致**，所以不是中文化時動的。

### `SAVE.DAT` 的同一組欄位

出貨的 `SAVE.DAT` 四格年月都是 `0`（日是 1、`+0x01` 是 0），
**是一份沒存過任何進度的空檔**。
這同時反證了這組欄位就是**跑動中的遊戲時鐘**，隨遊戲狀態一起寫入，
不是劇本的靜態 metadata。

## 1.5 勢力表：22 筆 × 64 B（區塊 `+0x80`）

迴圈邊界出自 `sub_15358`（月結）的 `add si,40h` ＋ `cmp cl,16h`，見 `docs/re/06` §5。

| 偏移 | 型別 | 內容 | 等級 |
|---|---|---|---|
| `+0x00` | u8 | 存在旗標，`≥ 0x80` 表示該勢力存在 | **confirmed**（`cmp byte ptr [si],80h`） |
| `+0x01` | u8 | **君主**的武將編號 | **confirmed**（解出來全是君主名） |
| `+0x02` | u8 | **軍師**的武將編號，`0x7F` ＝ 無 | **confirmed**（曹操→荀彧、呂布→陳宮、孫策→周瑜） |
| `+0x03` | u8 | **首都**的據點編號 | 強證據（值域 0–191，22 筆互不相同） |
| `+0x04` | u16 | **預備兵：騎馬** | 強證據 |
| `+0x06` | u16 | **預備兵：弓兵** | 強證據 |
| `+0x08` | u16 | **預備兵：步兵** | 強證據 |
| `+0x16` | u16 | 恆 `FFFF` | 未解 |
| `+0x18` | u8 | **武將數** | **confirmed**（與武將表 `+28` 的分佈完全吻合） |
| `+0x19` | u8 | 恆 `FF` | 未解 |
| `+0x1A` | u16 | **月結時讀完歸零的累計值** | **confirmed**（`sub_15358`） |
| `+0x1C` | u8 | 同上 | **confirmed** |
| `+0x14` | u8 | **軍團數**（編成時 `inc`） | **confirmed**（`sub_16F26`） |
| `+0x19` | u8 | **侵攻／敵對目標勢力編號**，`0xFF` ＝ 無 | **confirmed**（`sub_12E33`／`sub_13526`／`sub_135AB`） |
| `+0x1D` | u8 | **士氣基準值**（四個劇本全是 `200`）。編成時複製進軍團的士氣欄位 | **confirmed**（`sub_16F26`） |
| `+0x20` | **i24** | **資金**（有號 24 位元） | **confirmed**（見下） |
| `+0x23` | u8 | **據點數** | **confirmed**（與據點表 `+1`／`+26` 的分佈完全吻合） |
| `+0x28` | u8 | **君主的好戰等級**，0–15 | **confirmed**（見 `docs/mechanics/70-ai.md` §1.1） |
| `+0x2A` | u8 | 恆 `FF` | 未解 |
| `+0x3E` | u8 | 勢力編號（＝自身索引） | confirmed |
| `+0x3F` | u8 | 與 `+0x02` 相同 | 未解（軍師編號的備份？） |

> ⚠ **修正**：`+0x1D` 先前標成「假說：信賴度」。
> 編成軍團時它被複製進軍團的士氣值欄位（`sub_16F26` 的
> `mov al, [bx+1Dh]` / `mov [si+6], al`），而日文說明書 5.5 的編成畫面
> 截圖上士氣值正好是 **200**。→ 它是**士氣基準值**。
> **信賴度存在哪還沒解。**

### 資金是有號 24 位元，而且「疲弊」的判定就在機器碼裡

兩支程式都用同一種寫法讀資金：

```asm
mov  ax, [si+21h]      ; 取資金的高 16 位（＝資金 >> 8）
cmp  ah, 80h           ; 檢查第 23 位
jb   …                 ; 未設 → 資金 ≥ 0
```

`cmp ah, 80h` 檢的就是 24 位值的符號位。
**這是說明書「疲弊状態とは資金がマイナスの状態」那句話的機器碼證據**
（`docs/mechanics/70-ai.md` §1.4）。

四個劇本的起始資金全部是 1000 的整數倍
（74000、21000、17000、33000…87000），這也反證了 24 位的解讀是對的。

### 交叉驗證怎麼做的

`+0x18` 與 `+0x23` 兩個欄位都是小整數，**光看數字分不出哪個是武將數、
哪個是據點數**。做法是去數另外兩張表裡「屬於各勢力」的筆數：

| 表 | 所屬勢力欄位 | 各勢力筆數 |
|---|---|---|
| 據點表（192 × 32 B） | `+1`、`+26` | 完全等於勢力表 `+0x23` |
| 武將表（127 × 32 B） | `+28` | 完全等於勢力表 `+0x18` |

兩組 22 個數字各自完全吻合，**不是猜的**。
順帶解掉了據點表 `+1`／`+26` 與武將表 `+28`（原本都標「未解」）。

## 1.55 ⭐ 交友度矩陣（區塊 `+0x680`，22 列 × 24 B）

位址算法出自 `sub_13119`：

```asm
mov bx, si / shr bx,1 / shr bx,1      ; 觀察者 idx × 16
mov ax, bx / shr bx, 1 / add bx, ax   ; idx × 8 + idx × 16 = idx × 24
mov ax, di / shl ax,1 / shl ax,1 / mov al, ah / xor ah, ah   ; 對象 jdx
add bx, ax / add bx, 600h
```

```
交友度[觀察者][對象] ＝ 段內 0x600 + 觀察者 × 24 + 對象
```

每列 24 byte，實際只用前 22 欄；第 22–23 欄是填充（值 `0x80`）。

| 內容 | 值 |
|---|---|
| 一般值域 | 20 – 100（低 7 位） |
| 對角線（自己） | **127** |
| 最高位元 | **1 ＝ 和平，0 ＝ 交戰中** |

### 用歷史事實驗過

| 關係 | 值 | 史實 |
|---|---:|---|
| 劉備 ↔ 公孫瓚 | **100** | 同門，公孫瓚曾收留劉備 |
| 孫策 → 劉表 | **20** | 孫堅死於劉表部將黃祖之手 |
| 劉表 → 孫策 | 55 | **不對稱**——交友度是有向的 |
| 多數配對 | 55 | 預設「普通」 |

### ⚠ 最高位元是「和平」不是「交戰」

本專案先前把它記成交戰旗標，**方向反了**。從資料看出來的：

> 四個劇本裡**每一格的最高位元都是 1**，而且**沒有任何勢力有侵攻目標**。
> 若 1 代表交戰，開局就會是所有人互相宣戰。

極性定下來之後，`sub_1578F`（外交官要錢）的基準也對上了：
和平 → 100，交戰 → **125**，**打仗時外交官要更多錢**。

## 1.6 據點表：192 筆 × 32 B（區塊 `+0x08C0`）

欄位大多是從月結程式讀出來的（`docs/re/07` §3、§11、§15）。

| 偏移 | 型別 | 內容 | 等級 |
|---|---|---|---|
| `+0x01` | u8 | **所屬勢力編號（執行期用的那一個）** | **confirmed**（月結 `cmp cl,[di+1]`） |
| `+0x02` | 6 B | **城市名稱**（Big5，如「北京」「涿郡」「武陵」） | **confirmed** |
| `+0x08` | u16 | **X 座標**（值域 4–370，地圖寬 384） | **confirmed** |
| `+0x0A` | u16 | **Y 座標**（值域 9–248，地圖高 256） | **confirmed** |
| `+0x0C` | u16 | **生產力上限**（300–42,800） | **confirmed** |
| `+0x0E` | u16 | **生產力**（142–20,714，**全部 ≤ 上限** ✅） | **confirmed** |
| `+0x10` | u8 | **上昇值 ＋ 100**（實際 −90…+79） | **confirmed** |
| `+0x11` | u8 | **防災值**（開局 192 個據點**全部是 100**） | 強證據 |
| `+0x12` | u8 | **城兵數上限**（0–254） | 強證據 |
| `+0x13` | u8 | **城兵數**（0–177，**全部 ≤ `+0x12`** ✅） | **confirmed**（`sub_14F8A` 拿它合成守城部隊） |
| `+0x19` | u8 | **派駐的內政官**（武將編號，`0xFF` ＝ 無；開局全 `0xFF`） | **confirmed** |
| `+0x00` | u8 | **低 4 位 ＝ 四個方向哪幾個有鄰接**（`shr al,1` ×4 逐位測） | **confirmed**（`sub_12CDF`） |
| `+0x1C`–`+0x1F` | 4 B | **四個鄰接據點的編號**，與 `+0x00` 的四個位元一一對應 | **confirmed**（`sub_12CDF`） |
| `+0x1A` | u8 | **所屬勢力編號（作者填的那一個）** | confirmed |

⚠ `+0x0F` 不是獨立欄位——它是 `+0x0E`（生產力 u16）的高位元組。
月結的成長公式故意讀它當作「生產力 ÷ 256」。

### ⚠ 原版的資料瑕疵：兩個據點的歸屬自相矛盾

`+0x01` 與 `+0x1A` 在 190/192 個據點上一致，但有兩個不一致：

| 劇本 | 據點 | 城市 | `+0x01`（執行期） | `+0x1A`（作者填的） |
|---|---:|---|---|---|
| 3 蜀地偏安 | 171 | **武陵** | 劉璋 | **劉備** |
| 4 劉禪繼位 | 175 | **南昌** | 劉禪 | **孫權** |

**`+0x1A` 那一側才是對的**，三條證據：

1. 只有用 `+0x1A` 去數，各勢力的據點數才等於勢力記錄的 `+0x23`
   （四個劇本全部吻合；用 `+0x01` 算，劇本 3、4 各差一個）。
2. 歷史上武陵屬荊南（劉備）、南昌屬東吳（孫權）。
3. `+0x23` 是作者填進檔案的初值，反映的是作者的意圖。

**但執行期讀的是 `+0x01`**（月結 `sub_153C6` 的 `cmp cl, [di+1]`、
內政官 `sub_15715` 的 `cmp ch, [di+1]`），
所以原版實際會把武陵判給劉璋、南昌判給劉禪。

remake 預設照抄（`internal/state` 的 `City.Owner` 用 `+0x01`），
並保留 `OwnerRecorded` 讓人可以改回作者意圖。
`internal/state` 有一條測試**釘住這兩個不一致**——
如果哪天它們消失了，代表資料被動過。

## 1.7 軍團表：127 筆 × **64 B**（區塊 `+0x22C0`）

出貨的劇本檔裡全零——開局沒有軍團，玩家要自己編成。
欄位出自 `sub_16F26`（編成）與 `sub_16F86`（位置），見 `docs/re/08` §4–§5。

| 偏移 | 內容 |
|---|---|
| `+0x00` | 存在旗標，建立時寫 `0xC0` |
| `+0x01` | **所屬勢力** |
| `+0x02` | 軍團編號 |
| `+0x04` | 兵力（AI 的募兵判斷會累加它） |
| `+0x06` | **士氣值**，建立時從勢力 `+0x1D` 複製（＝ 200） |
| `+0x08` | **朝向**（0／1 ＝ X 減／增，2／3 ＝ Y 減／增，**4 ＝ 靜止**）。`sub_12808` 從「現在座標與下一個路徑點的差」算，**X 有差就只看 X，X 相同才看 Y**；到站時 `sub_12662`／`sub_127A2` 改寫成 4。`sub_14B63` 拿它決定野戰要取樣大地圖上的哪兩格 |
| `+0x0A` | **沿路徑表前進的步進量**（`sub_127F6` 取負再與 `+0x0C` 相加當索引；與 0 和 4 比大小） |
| `+0x0B` | **移動計時器**（每 tick 減 1，歸零走一步） |
| `+0x1E` | **移動間隔** ＝ 速度（純騎馬編成較小） |
| `+0x0E`／`+0x10`／`+0x12` | **目前**：據點編號×8、X、Y |
| `+0x14`／`+0x16`／`+0x18` | **目標**：據點編號×8、X、Y |
| `+0x1A`／`+0x1C` | 佔用圖的 X 與段位址 |
| `+0x20` | 建立時寫首都據點編號 |
| **`+0x28`–`+0x3F`** | **六個部隊槽 × 4 B**（見下） |

**現在位置與目標位置是兩組獨立的三元組**，行軍就是把前者推向後者。

### ⭐ 每 tick 只更新 16 支軍團

`sub_125A3` 不掃全表：從一個游標（`cs:0D18h`）開始只跑 16 筆
（`mov cx, 10h`），`si ≥ 0x1FC0`（127 × 64）繞回 0。
**掃完 127 支要 8 個 tick**——這讓同一個 tick 裡不同軍團的行動有先後，
先被處理的那一支會先移動、先撞上人。

### 六個部隊槽（`+0x28` 起，每槽 4 B）

| 槽內偏移 | 內容 |
|---|---|
| `+1` | **兵力**，一點 ＝ 10 人（滿編 100 ＝ 1,000 人） |
| `+2` | **兵種**：`1` ＝ 騎馬、`2` ＝ 弓兵、`3` ＝ 步兵 |
| `+0`／`+3` | 未解 |

出處是戰力計算 `sub_15285`（`add si, 28h` ＋ 迴圈 6 次 ＋ `add si, 4`）
與城兵合成 `sub_14F8A`（直接寫 `[bx+2] = 3` 表示步兵），見 `docs/re/09` §3.1、§7。

`0x28 + 6 × 4 = 0x40`——**這是軍團記錄 64 B 的第三個獨立證據**。

### 軍團編號 ＝ 武將編號

`sub_1291A` 把軍團記錄位址直接換算成武將記錄位址：

```
(軍團位址 − 0x2240) ÷ 2 + 0x4240 = 武將位址
```

軍團 64 B、武將 32 B，**兩張表同索引平行**。記憶體佈局也吻合：

```
ds:2240h  軍團表  127 × 64 ＝ 8,128
ds:4200h  城兵用的臨時軍團   1 × 64      ← 中間那 64 B 不是填充
ds:4240h  武將表  127 × 32 ＝ 4,064
```

> ⚠ 記錄長度先前記成 32 B，**實際是 64 B**（127 × 64 ＝ 8,128）。
> 證據見 `docs/re/08` §4。表的範圍是區塊 `+0x22C0` 起 8,128 B。

### 單位佔用圖

軍團會在一塊 98,304 B 的區域上標記自己的位置：

```
位址 = word_19872 段 + Y × 24 段 + X       （Y × 384 byte + X）
384 × 256 = 98,304 ✅
```

`docs/re/04` §2 原本把這塊記成「渲染用的畫布」——**尺寸猜對，用途猜錯**，
它是**每格一個 byte 的單位計數圖**。

## 2. ⚠ 武將是 127 人，不是 146

社群資料（維基）寫「武將 146 人」。**一手資料是每個劇本 127 人**
（第 127 筆全零，之後也全零）。

可能的解釋：146 是四個劇本的**聯集**，或包含君主／不可用武將。
**沒查證前不要引用 146。**（`CLAUDE.md` §5：一手資料贏二手推論。）

## 3. 武將記錄（32 byte）

```
80 82 | aa5a a677 b0ea | aa5a a677 b0ea | 00 80 00 | 0a 01 02 | 00 00 | 02 00 | 00 ff 00 ff 13 ff 04 00
```

| 偏移 | 長度 | 內容 | 等級 |
|---|---:|---|---|
| `+0` | 1 | 旗標（7 種值）。**bit 4 ＝ 不事二主**：舊主已滅時被俘會自刎（`sub_129C3` → 訊息 0x43） | 其餘位元未解 |
| **`+1`** | 1 | **頭像編號**（`KAOGRF` 的頁碼） | **confirmed**（見下） |
| `+2` | 6 | **姓名**（Big5，3 全形字） | confirmed |
| `+8` | 6 | **呼び名**（暱稱）；多數武將與 `+2` 相同 | **confirmed** |
| **`+14`**（`0x0E`） | 1 | **攻城適性**（高半位元組 0–10）。**計入評價** | **confirmed**（`sub_152D7` 的 `xlat`，模式 0） |
| **`+15`**（`0x0F`） | 1 | **野戰適性**（同上） | **confirmed**（同上，模式 1） |
| `+16`（`0x10`） | 1 | 同格式，戰略層沒有呼叫點取得到（要模式 2）。**計入評價** | 未解 |
| **`+17`** | 1 | **武力**，值域 1–15 | **confirmed** |
| **`+18`** | 1 | **統率**，值域 1–15 | **confirmed** |
| **`+19`** | 1 | **政治／內政**，值域 1–15 | **confirmed** |
| `+20`（`0x14`） | 1 | 值域 0–7 | 未解 |
| **`+22`**（`0x16`） | 1 | **戰場行動腳本編號**（0–7）。`BATTLE.DAT` 的段編號 ＝ 本值 × 4 ＋ 戰場類別 | **confirmed**（`sub_1CBE5`，見 `docs/re/11` §3.3） |
| `+21`,`+23`,`+26` | 各 1 | 全 0 | 保留？ |
| **`+24`**（`0x18`） | 1 | **倒數計時器**，每月遞減，歸零才行動 | **confirmed**（`sub_1585F`） |
| `+25`,`+27` | 2 | 含 `0xFF` 哨兵 | 未解 |
| **`+26`**（`0x1A`） | 1 | **官員的經費餘額**（＝撥款金額 ÷ 128），`0` ＝ 沒錢／沒在要錢 | **confirmed**（`sub_139E8` 撥款時 `shl ax,1` 只存 `ah`；`sub_15715`／`sub_1578F` 檢查是否為 0） |
| **`+28`**（`0x1C`） | 1 | **所屬勢力編號**，`0xFF` ＝ **在野** | **confirmed** |
| **`+23`**（`0x17`） | 1 | **出陣中**：編成軍團時寫 1（`sub_16F26`）、被俘時寫 4（`sub_129C3`）、釋放時歸零 | **confirmed** |
| **`+29`**（`0x1D`） | 1 | **捕虜狀態**：記著**舊主的勢力編號**，`0xFF` ＝ 非捕虜 | **confirmed**（見下） |
| **`+31`**（`0x1F`） | 1 | **評價**（衍生值，載入與月結時重算，檔案裡是 0） | **confirmed**（`sub_155A6`） |


### `+1` 是頭像編號，不是武將編號

原本只知道「127 筆各不相同」，兩種解讀（唯一鍵／所在據點）都說得通。
用 PC-98 實機的君主確認畫面定案（`docs/playtest/07`）：

| 武將 | 武將編號 | `+1` | 畫面上的頭像 |
|---|---:|---:|---|
| 曹操 | 16 | **50** | `KAOGRF` 第 **50** 頁，同一張圖 |
| 荀彧 | 62 | **121** | `KAOGRF` 第 **121** 頁（紅寶石金冠），同一張圖 |

**拿武將編號當頁碼會畫出別人的臉**——第 16 頁是另一個人。
`KAOGRF` 有 150 張 > 127 人，也與「頭像另外編號」對得上。

### ⚠ 訂正：`+0x1D` 是捕虜狀態，不是派駐狀態

四處用法一致（`docs/re/09` §6）：

| 位置 | 用法 |
|---|---|
| `sub_129C3`（戰敗被擒） | 寫入**舊主的勢力編號**，同時把 `+0x1C` 換成勝方 |
| `sub_150D7`（釋放） | `xchg al(0FFh), [di+1Dh]` 並通知該編號的勢力 → 訊息「被敵軍所擒的{1}大人回來了」 |
| `sub_1585F`（月結） | `!= 0xFF` 就跑歸降判定，成功則清成 `0xFF` → 訊息「俘虜的{1}已歸降我軍了」 |
| `sub_16366`（顯示） | `== 0xFF` 才走一般的能力值顯示 |

開局四個劇本全是 `0xFF`，與「未派駐」的觀察一樣說得通——
**光看初始值分不出來，是用法把它定下來的。**

### ⚠ 訂正：`+0x0E`–`+0x10` 不是兵種適性

索引它們的是**場合**（攻城 0／野戰 1），不是兵種：

```asm
add bx, 424Eh     ; 武將記錄 + 0x0E
xlat              ; al ＝ 模式（0 攻城／1 野戰）
mov cl, 4 / shr al, cl
```

三個欄位裡定案的是前兩個。`+0x10` 要 `al = 2` 才取得到，
而戰略層沒有任何呼叫點傳得進 2——與地形係數表的第 2 列是同一個缺口。

### 評價的公式（`sub_155A6`，confirmed）

```
評價 = (適性₁ + 適性₂ + 適性₃) + 2 × 武術 + 2 × 統率
```

**武術與統率的權重相同、政治不計入。**
這證實了說明書 10.5 的「武術と統率はその合計が同じであれば強さは同じ」。
劇本 1 的最高是呂布與趙雲（66）、諸葛亮（64）、關羽（61）；
最低的四人是孫乾、滿寵、張紘、張昭——全是武 1 統 1 政 12–14 的文官。

已實作成 `internal/rules/general`。

### `+8` ＝ 呼び名（confirmed，2026-08-08）

說明書 5.3 節：

> 武将によっては**呼び名**と言う項目を持っている者が存在します。
> たとえば「諸葛亮」という武将が会話文章中では「孔明」と呼ばれる。
> ただし、**中国で言う「字（あざな）」とは違います**。

掃過四個劇本共 508 筆，`+2` 與 `+8` 不同的只有 **13 筆**：

| 本名 | 呼び名 | 出現 |
|---|---|---|
| 司馬懿 | 仲達 | 四個劇本都有 |
| 諸葛亮 | 孔明 | 四個劇本都有 |
| 龐統 | 鳳雛 | 四個劇本都有 |
| 李暹 | （空白） | 只有劇本 1，疑似資料瑕疵 |

**「鳳雛」是雅號不是字**——這正好印證說明書那句「與中國的字不同」。
仲達與孔明是字，鳳雛不是，遊戲把兩類混在同一個欄位。

所以 `+8` 不是冗餘欄位，是**對話中要用的稱呼**
（`TALK.DAT` 的變數標記之一應該就是插這個，見 `docs/formats/02`）。

### 三個能力值怎麼定案的

不是猜的——**用歷史人物的排名驗**：

| 欄位 | 最高的六人 | 判定 |
|---|---|---|
| `+17` | 馬超 15、呂布 15、張飛 15、關羽 14、許褚 14、祝融 14 | **武力** |
| `+18` | 司馬懿 15、諸葛亮 15、徐庶 14、龐統 14、曹操 13、周瑜 13 | **統率** |
| `+19` | 諸葛亮 15、荀彧 14、荀攸 14、滿寵 14、曹操 13、諸葛瑾 13 | **政治** |

逐人對照：

| 武將 | 武 | 統 | 政 |
|---|---:|---:|---:|
| 呂布 | 15 | 11 | 1 |
| 張飛 | 15 | 8 | 1 |
| 關羽 | 14 | 11 | 8 |
| 典韋 | 13 | 5 | 1 |
| 趙雲 | 13 | 13 | 6 |
| 曹操 | 9 | 13 | 13 |
| 周瑜 | 7 | 13 | 9 |
| 諸葛亮 | 5 | 15 | 15 |
| 荀彧 | 4 | 13 | 14 |

**三個榜單各自都符合三國常識，而且不互相矛盾**——這種一致性不可能是巧合。

值域 1–15 也與社群資料對得上（部落格提到「孫策、太史慈、甘寧武力 12；
周瑜、陸遜統率 13；呂蒙 08(武)10(統)」，全部落在 1–15）。

`+17`（武力）與 `+18`（統率）正是日文說明書第 10.5 節講的
「武術と統率」（`docs/mechanics/70-ai.md` §3.1）——
**委任戰術時 AI 的指揮品質由 `+18` 決定**。

## 4. 據點記錄（32 byte）

```
北京  00 0e | a55f a8ca a140 | 0101 | 0900 | 0852 | ea50 | 6f | 64 | a976 | 0000 | 10 00 00 ff | 0e | 00 03 02 ff ff
```

| 偏移 | 長度 | 內容 | 等級 |
|---|---:|---|---|
| `+0` | 1 | 值域 0–15，14 種。**據點類型？** | 未解 |
| `+1` | 1 | 值域 0–24，23 種，最常見 `24`（62 筆）→ **所屬勢力**，24 疑似「無主」 | 強證據 |
| `+2` | 6 | **名稱**（Big5，**定長 6 byte，不足補全形空格 `A1 40`**） | **confirmed** |
| `+8` | 2 | uint16，值域 4–370 → **地圖 x 座標** | 強證據 |
| `+10` | 2 | uint16，值域 9–248 → **地圖 y 座標** | 強證據 |
| `+12` | 2 | uint16，北京 21000、代 4000、涿郡 4300 | 未解（人口／資金？） |
| `+14` | 2 | uint16，北京 20714、代 3000、涿郡 3142 | 未解 |
| `+16` | 1 | 值域 10–179 | 未解 |
| `+17` | 1 | **全部 192 筆都是 100** | 未解（防災值初值？） |
| `+18` | 2 | uint16 | 未解 |
| `+20`,`+21`,`+23`,`+24` | 各 1 | 全 0 | 保留 |
| `+22` | 1 | 值域 0–226，22 種 | 未解 |
| `+25` | 1 | 全 `0xFF` | 哨兵 |
| `+26` | 1 | **值域與分佈和 `+1` 完全相同** → 同一個值的兩份（當前／初始擁有者？） | 強證據 |
| `+27`–`+31` | 5 | 含 `0xFF` 哨兵 | 未解 |

### 名稱是定長 6 byte，用全形空格補齊

`代` ＝ `a5 4e | a1 40 | a1 40` —— `A1 40` 是 Big5 的全形空格。
**所以名稱最多 3 個全形字，不足補空格，不是 NUL 結尾。**
（`docs/formats/08` 初版把武將名讀成「武安國武安」就是因為當成不定長，讀過頭了。）

### 座標的驗證做到哪裡

**confirmed（2026-08-08）。** 用 PC-98 實機畫面驗完了，
過程與數字見 [`docs/playtest/05`](../playtest/05-viewport-and-city-coords.md)。

先用水域分佈把那張截圖對回大地圖（狀態列 32 px、16 px／格對齊、
左上角是圖塊 (170, 98)，725 格水陸 100% 吻合、八萬個候選中唯一），
再把記錄座標投影到畫面：**視窗內 7 個量得到的據點全部落在城池圖案上**，
每一個都在全畫面 824 格的前 2.7%（中位數 33.2%）。第 8 個被對話框蓋住。

**錨點偏移是 (0, 0)**——座標直接就是城池那一格，不需要修正。
下面「最佳偏移 `dx=+2` 只有 41.7%」那一段是**用圖塊編號掃出來的舊結論，
已被實機畫面推翻**，留著是為了記錄判準太鬆會長什麼樣。

以下為當初的推導：

支持：

- **192/192 全部落在 384×256 範圍內**（x 4–370、y 9–248，有合理邊距）。
  隨便挑兩個 uint16 欄位不會這麼剛好。
- 掃描 `(dx, dy)` 偏移找城池圖塊時，**前十名全部是 `dy = 0`**
  —— y 座標是準的。
- 北京 (257, 9)、代 (218, 11)、涿郡 (246, 15)、中山 (230, 27)
  都是河北一帶的城市，而它們的 y 都很小（地圖北部）。**方位合理。**

不足：

- 座標**不精確落在城池圖案的固定一格上**。最佳偏移 `dx=+2` 也只有 41.7%，
  而且有 12 筆附近 10 格內找不到城池圖塊。
- 合理的解釋是**城池在畫面上是橫跨數格的複合圖案**（實機截圖看得到），
  座標可能是入口、道路連接點、或圖案的某個錨點；也可能城池是疊繪的
  sprite 而不是地形圖塊。**兩種都沒驗。**

**要定案得在遊戲裡看**：讓 PC-98 oracle 跑到大地圖，比對某個據點的
畫面位置與記錄裡的座標。這件事還沒做。

名稱——劇本 1 的前 40 個：

> 北京、代、涿郡、易城、廣武、中山、九原、真定、樂成、南皮、晉陽、黃、沾、
> 樂陵、信都、房子、廮陶、平原、界橋、清河、臨淄、高宛、挺、曲梁、北海、
> 濟南、館陶、西涼、盧、鄴、平陽、壺關、奉高、東莞、東武、東平、黎陽、魯、
> 狹道、濮陽

**其餘欄位還沒分析。** `docs/re/05` 已知在記憶體裡據點記錄也是 32 byte、
基址 `0x840` —— 檔案與記憶體的佈局很可能一致，但**還沒驗**。

## 5. 武將名單隨劇本變化，而且符合史實

| 劇本 | 前八名 |
|---|---|
| 1 呂布歸天 | 袁胤、袁尚、袁紹、袁術、袁譚、王朗、郭汜、韓玄 |
| 2 赤壁之戰 | 王朗、韓玄、金旋、曹叡、曹彰、曹植、曹操、曹丕 |
| 3 蜀地偏安 | 王朗、曹叡、曹彰、曹植、曹操、曹丕、孫桓、孫權 |
| 4 劉禪繼位 | 王朗、曹叡、曹植、曹丕、孫桓、孫權、孫韶、孫瑜 |

袁氏在劇本 2 消失（官渡後）、韓玄金旋在劇本 3 消失、
**曹操與曹彰在劇本 4 消失**（曹操卒於 220 年，劉禪 223 年繼位）。

**這本身就是一條驗證**：名單的變化符合史實，代表劇本區塊的切分是對的。

## 6. 下一步

1. 據點記錄的欄位（配合 `docs/re/05` 的記憶體佈局）。
2. `+0x1EC0`–`+0x42C0` 那 7 KB 未解區。
3. `SINARIO.DAT` 與 `SAVE.DAT` 的 diff 集中在哪些欄位
   —— 那些就是「遊戲過程中會變的」，是存檔寫回的必要範圍。
4. `+1` 那個唯一鍵到底是什麼。

## 4.1 ⭐ 據點的鄰接關係就存在記錄裡（2026-08-08）

`sub_12CDF`（AI 局勢評估，`docs/mechanics/70-ai.md`）：

```asm
mov al, [di]          ; 據點記錄 +0
mov si, di / add si, 1Ch
mov cx, 4             ; ★ 四圈
loop:
  shr al, 1 / jnb .skip        ; ★ 低 4 位逐一測 → 這個方向有沒有鄰接
  mov bh, [si]                 ; ★ +1Ch..+1Fh ＝ 鄰接據點編號
  xor bl,bl / shr bx,1 ×3      ; × 32 → 那個據點的記錄
  mov bh, [bx+841h]            ; 讀它的所屬勢力（據點表 +1）
  …
.skip: inc si / loop
```

**所以「哪個據點連到哪個據點」不必從地圖推——它直接存在據點記錄裡。**
四個方向與 `docs/re/08` §7 的行軍連結表（每個據點四筆、每筆一個方向）
是同一組關係的兩種存法：這裡是**據點 → 鄰接據點**，
那裡是**節點 → 下一個節點 ＋ 路徑點**。
