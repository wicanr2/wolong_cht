# 65 — 電腦勢力的行軍決策鏈：Stage 0–3 與整條軍團生命週期

**狀態：四支 AI handler 逐行讀完，Stage 0–3／8–11 的轉移條件全部 confirmed。
⭐ 目標選擇是「消費勢力層的兩個待辦欄」——失土（`+0x17`）優先於求援（`+0x16`），
沒事做就把軍團解散回收兵力。這也是勢力 `+0x00` 位元 6（資金吃緊）的消費端。
⚠ `sub_143AF` 有一處 `di` 沒設就用（§3.2），比對同族的 `sub_1440F` 可知
它想讀的是據點 `+0x18`，實際讀到的位址少了 `0x840`。
⭐ **回首都的路要穿過別人的地，軍團就轉進「敗走」狀態**（§8，強證據）。**

- 日期：2026-08-17
- 範圍：只驗松崗 DOS/V
- 原始 `KI.EXE` SHA-256：`fffeba985231cda4d636e93d10f598470b1f691d00275e4aa38e285893d43868`
- 分析時的 `KI.EXE.i64` SHA-256：`8a8fd7d528e0498000fd04282300588b637fbcb8aa48deb09242f1f41f532691`
- 工具：**IDAPython** `tools/ida_dump.py`；關鍵指令用 `xxd` 對原始 bytes 覆驗
- 位址空間：IDA DOS/V linear address。`si` ＝ 軍團記錄，
  `ax` ＝ 目標據點記錄位址（`0x840 + 據點編號 × 32`）

[`64`](64-corps-arrival-state-machine.md) 解出分派表與玩家那一半，
[`64`](64-corps-arrival-state-machine.md) §6 把非玩家的四個 entry 留給這一份，
這裡把它們讀完。

## 1. 整條生命週期

AI 的軍團只有一個迴圈，每次**走到目標**時由 `sub_14325` 依 `+0x23` 分派：

```
        ┌─────────────────────────────────────────────┐
        ↓                                             │
   Stage 0 ── 到了，據點威脅沒有具體目標 ──→ Stage 1 ──┤
   （移動）                                （在據點決策）│
        ↑                                    │  │  │   │
        │                                    │  │  └───┴→ Stage 9（首都補兵）
        │                                    │  └──────→ Stage 10（走回首都）
        │                                    └─────────→ Stage 2（挑新目標）
        │                                                      │
        └──────────────── 挑到目標 ←───────────────────────────┘
                                                       挑不到 → Stage 11（解體）

   Stage 9 ──補兵──→ Stage 3 ──六槽都 ≥ 300 人──→ Stage 8（等士氣）──→ Stage 1
                        └──任一槽 < 300 人──→ Stage 11（解體）
   Stage 10 ──走到首都──→ Stage 9
   Stage 11 ──走到首都──→ 解散（[`64`](64-corps-arrival-state-machine.md) §3）
```

**四個出口只有兩種：再出擊，或拆回預備兵。** AI 不會讓殘兵在地圖上晃。

## 2. Stage 0（`sub_1439D`，18 B）：到了就換檔

```asm
bx = ax                      ; 目標據點記錄
call sub_14548 / jnb retn    ; CF=0 ⇒ 還沒走到，什麼都不做
test byte [bx], 40h / jnz retn   ; 據點 +0x00 位元 6 ＝ 威脅有具體目標
mov byte [si+23h], 1         ; 否則 → Stage 1
```

據點 `+0x00` 的位元 7 ＝ 受威脅、位元 6 ＝ 威脅有具體目標
（[`40`](40-garrison-relief-request.md) §2）。**位元 6 還亮著就留在 Stage 0**——
這一格還在被盯上，軍團原地駐守。

## 3. Stage 1（`sub_143AF`，96 B）：站在據點上決定下一步

```asm
cmp word [si+0Eh], 800h / jnb → Stage 0    ; 現在節點 ≥ 256：不是據點，繼續走
cmp word [si+4], 12Ch   / jbe → Stage 10   ; 兵力 ≤ 300 點（3,000 人）→ 回首都補兵
bx = ax
test byte [bx], 40h     / jnz → Stage 0    ; 威脅有具體目標 → 駐守
cmp  byte [bx], 80h     / jb  → 出擊       ; 位元 7 沒亮 ＝ 不受威脅
cmp  byte [di+18h], 2   / jbe → 留守       ; ⚠ 見 §3.2
出擊:
    call sub_1ECE0 / al &= 7 / inc al
    [si+0Bh] = al                          ; 延遲 1–8 tick
    [si+23h] = 2
留守:
    cmp word [si+4], 258h / jnb retn       ; 兵力 ≥ 600 點（滿編）→ 不補
    di = 勢力記錄
    cmp ah, [di+3] / jnz retn              ; 這裡不是首都 → 不補
    [si+23h] = 9                           ; → Stage 9 補兵
```

### 3.1 兩個兵力門檻

| 門檻 | 值 | 行為 |
|---|---:|---|
| `0x12C` | 300 點 ＝ 3,000 人 | **以下**就放下手邊的事回首都補兵（Stage 10）|
| `0x258` | 600 點 ＝ 6,000 人 | **以上**視為滿編，站在首都也不補 |

600 點 ＝ 六槽 × 100（[`49`](49-corps-formation-window.md) §5），
與玩家那一側 `sub_14370` 用的是同一個常數（[`64`](64-corps-arrival-state-machine.md) §2.2）。

### 3.2 ⚠ `cmp byte [di+18h], 2`：`di` 沒設就用

`sub_143AF` 從進入到這一行**沒有寫過 `di`**。唯一會走到這裡的呼叫路徑是
`sub_12662` → `sub_128F4` → `sub_14325`，而 `sub_128F4` 的第一件事是
`di = [si+0Eh] × 4` ＝ **現在節點 × 32**。據點記錄在 `0x840 + 編號 × 32`，
所以 `[di+18h]` 落在**據點記錄前面 `0x840` 個 byte** ——那裡是勢力表
（22 × 64 B）與其後未解的 704 B（[`../formats/08`](../formats/08-sinario-save.md) §1.5）。

同一家族的 `sub_1440F` 開頭就是 `mov di, ax`，之後用 `[di+18h]` 讀
**據點 `+0x18`（停在該格的軍團數）**——兩支的指令編碼一模一樣
（`80 7D 18 02` 與 `80 7D 18 01`，檔案位移 `0x45D3`／`0x461B`）。

⭐ **意圖是據點 `+0x18`，寫出來的是別的位址。** 照意圖讀，這一段是
「受威脅、但這一格已經有超過兩支軍團 → 分一支出去打」，語意通順；
照實際位址讀，分支由不相干的資料決定。

**remake 的處置**：實作意圖版（據點 `+0x18` > 2 才出擊），
在 [`../spec/40`](../spec/40-ai-march-decision.md) 標成 remake 差異。
理由是原版讀到的位址在乾淨重寫裡不存在，沒有「照抄」這個選項。
（對照 [`40`](40-garrison-relief-request.md) §4 的位移不對稱——**那一處照抄不修正**，
因為它讀的是有效欄位，只是算式不對稱。**兩者的差別是「算錯」與「讀錯地方」**。）

## 4. Stage 2（`sub_1440F`，87 B）：挑下一個目標

```asm
di = ax                                    ; 目標據點記錄
test byte [di], 40h / jnz → Stage 1        ; 威脅有具體目標
cmp  byte [di], 80h / jb  → 挑目標
cmp  byte [di+18h], 1 / ja → 挑目標        ; 這一格軍團數 > 1
[si+23h] = 1 / retn                        ; 受威脅且只有這一支 → 回 Stage 1
挑目標:
bx = 勢力記錄
test byte [bx], 40h / jnz → 只看求援       ; 勢力 +0x00 位元 6 ＝ 資金吃緊（§6）
al = 0FFh / xchg al, [bx+17h]              ; 取走「被佔走的據點」並清成 FF
cmp al, 0FFh / jnz → 有目標
只看求援:
al = 0FFh / xchg al, [bx+16h]              ; 取走「最近求援的據點」並清成 FF
cmp al, 0FFh / jz → 沒目標
有目標:
cmp [si+20h], al / jz → 略過改寫
    [si+20h] = al / or byte [si], 2        ; 改目標據點
[si+23h] = 0 / dec byte [di+18h] / retn    ; → Stage 0 出發
沒目標:
cmp byte [di], 80h / jnb retn              ; 現在這一格受威脅 → 留著
[si+23h] = 0Bh / dec byte [di+18h] / retn  ; → 解體
```

### 4.1 ⭐ 勢力層的兩個待辦欄是佇列，取一次就清掉

| 欄位 | 內容 | 誰寫 |
|---|---|---|
| 勢力 `+0x17` | **被別人佔走的據點編號** | `sub_13EFD`（[`44`](44-threat-and-reinforcement-ai.md) §1）|
| 勢力 `+0x16` | **最近一次求援的據點編號** | `sub_140B3`（[`44`](44-threat-and-reinforcement-ai.md) §3）|

`xchg al, [bx+17h]`（`al` 先填 `0xFF`）是**取值 ＋ 清空**一步完成，
所以**同一件待辦只會派出一支軍團**。優先序寫死：先奪回失土，再去救援。

**資金吃緊的勢力（勢力 `+0x00` 位元 6）跳過失土那一欄**，只接求援——
沒錢就不主動反攻，這與 §6 的門檻互相呼應。

### 4.2 `dec byte [di+18h]` 是離場的即時扣減

據點 `+0x18` 是**每 tick 從佔用圖抄回來的快取**
（[`../formats/08`](../formats/08-sinario-save.md) §1.6）。軍團決定離開時
先手動減一，同一個 tick 裡後面才被處理的軍團才看得到「這一格少了一支」。
**`sub_125A3` 每 tick 只處理 16 支軍團**，先後順序本來就會影響決策。

## 5. Stage 3（`sub_14466`，29 B）：補完兵的體檢

```asm
al = 1Eh / cx = 6 / bx = si + 29h
loop: cmp al, [bx] / ja → 解體    ; 30 > 該槽兵力
      bx += 4 / loop
[si+23h] = 8 / retn               ; 六槽都 ≥ 30 → Stage 8 等士氣
解體: [si+23h] = 0Bh
```

`si + 0x29` 起、間隔 4 ＝ **六個部隊槽的 `+1` 兵力欄**
（[`../formats/08`](../formats/08-sinario-save.md) §1.7）。

⭐ **任何一槽不到 30 點（300 人）就整團解散。** 補過兵還湊不齊六槽的軍團，
AI 不留——這是「殘兵回收成預備兵」的機制，也解釋了為什麼原版打久了
AI 的軍團數會上下跳。

## 6. `sub_13E11` 是勢力層的資金體檢，不是軍團欄位

`si` 是**勢力記錄**：從 `cs:word_10D1C` 起、每次 `+0x40`、`0x580` 繞回，
＝ 22 × 64 B（[`../formats/08`](../formats/08-sinario-save.md) §1.5）。
機制本身 [`08`](08-hourly-update.md) §1 早就讀過了，remake 也已經照著實作
（`internal/rules/diplomacy` 的 `CanSustainInvasion`）；
這裡重列是因為位元 6 的**消費端**到這一輪才找到（§4.1）。

```asm
si = cs:word_10D1C / ds = cs:word_10D52
cmp byte [si], 80h / jb → 跳過        ; 勢力不存在
and byte [si], 0BFh                   ; 先清位元 6
ax = [si+23h] × 8 + 18h               ; 據點數 × 8 + 24
cmp ax, [si+21h] / jl → 跳過          ; 有號比較；[si+21h] ＝ 資金 >> 8
mov byte [si+19h], 0FFh               ; ★ 取消侵攻目標
ax >>= 1
cmp ax, [si+21h] / jl → 跳過
or byte [si], 40h                     ; ★ 設位元 6
```

| 條件（`資金 >> 8` 與門檻比）| 效果 |
|---|---|
| `資金>>8 ≤ 據點數×8 + 24` | 勢力 `+0x19` ＝ `0xFF`：**放棄侵攻目標** |
| `資金>>8 ≤ (據點數×8 + 24) ÷ 2` | 勢力 `+0x00` 位元 6：**資金吃緊** |

換算成資金本身：**門檻 ＝ 據點數 × 2,048 ＋ 6,144**。
`+0x21` 是 24 位有號資金的高兩個 byte，不是獨立欄位
（[`../formats/08`](../formats/08-sinario-save.md) §1.5），
所以 `jl`（有號）能正確處理疲弊（負資金）。

⭐ 位元 6 的**消費端就是 §4.1** ——沒錢的勢力不反攻失土。
`and byte [si], 0BFh` 每輪先清，代表它是**每次重算的派生旗標，不是狀態**。

**每「時」只檢查一個勢力**（游標 `cs:word_10D1C`），22 個勢力要 22 個「時」轉一圈。

## 7. remake 對應

| 原版 | remake |
|---|---|
| Stage 0–3／8–11 的轉移 | [`../spec/40`](../spec/40-ai-march-decision.md) |
| 勢力 `+0x16`／`+0x17` 佇列 | 同上 |
| `sub_13E11` 的資金體檢 | 同上 |

## 8. ⭐ 回不了家的軍團會敗走（`sub_147BB` 的 `0x8000` 分支）

`sub_12662` 每次要移動時呼叫 `sub_147BB` 重算下一步（[`64`](64-corps-arrival-state-machine.md) §6），
而它算路是丟給 `loc_1491B`：

```asm
cmp cx, 8000h  / jb → 一般路徑
cmp byte [si+23h], 0Ah / jb → 一般路徑
al = [si+1] / call sub_1291A      ; ★ 俘虜／潰散
```

**兩個條件同時成立才走這條**：回傳值的高位元設起，而且 Stage ≥ 10
（＝ 回首都補兵的路上，或解體的路上）。

### 8.1 高位元 ＝ 這條路要穿過別人的地

`loc_1491B` 是**自我修改碼**——它先把三個參數 patch 進自己的立即值
（`mov cs:word_149B8, bx` 等），所以 IDA 只把後半段標成 `db`。
拿原始 bytes 交叉解碼（`CLAUDE.md` §7 第 28 條的作法）：

```asm
81 FE 00 06     cmp si, 600h            ; 節點 ≥ 192 ＝ 野外，沒有歸屬
73 1A           jnb → 跳過歸屬檢查
D1 E6 D1 E6     shl si,1 ×2             ; 節點 × 4
26 80 BC 41 08  cmp es:[si+841h], <勢力> ; ★ 據點 +0x01 所屬勢力
   12                                    ;   這個立即值由 byte_149D2 patch 進來
74 07           jz → 是自己的，跳過
81 C2 A6 00     add dx, 0A6h            ; ★ 成本 +166
80 CE 80        or  dh, 80h             ; ★ 設高位元
```

⇒ **穿過非己方據點要加 166 的成本，並在回傳值上留下一個記號**。

### 8.2 語意：進入「敗走」狀態，不是當場消失

`sub_1291A` 收到的 `al` 是**軍團自己的勢力**（`mov al, [si+1]`），
而 `sub_1291A` 的分流在「`al` 等於軍團的勢力」時走 `sub_12977`：

```asm
sub_12977:
  佔用圖 −1
  mov byte ptr [si], 8        ; ★ 存在旗標換成 8（< 0x80 ⇒ 不算「活著」）
  mov byte ptr [si+3], 30h    ; ★ 48，疑似倒數
  call sub_14689              ; 勢力的軍團數 −1
  （是玩家或對手時）TALK #1F／#20
```

⚠ **旗標 8 是一個狀態，不是「沒了」**：`sub_125A3` 的每 tick 迴圈對
`< 0x80` 的軍團還會 `test byte [si+2240h], 8`，命中就呼叫 `sub_12A7E`。

### 8.3 `sub_12A7E`：48 個 tick 之後才真的消失

```asm
dec byte [si+2243h] / jnz retn     ; ★ 軍團 +0x03 計時器 −1，沒歸零就等
mov byte [si+2240h], 0            ; ★ 軍團記錄歸零 ＝ 這支軍團不存在了
di = si >> 1                       ; 軍團位移 → 武將位移
mov byte [di+4257h], 0            ; ★ 主將 +0x17 職務 ＝ 無職
bx = 該軍團的勢力 × 64
cmp byte [bx], 80h / jb →          ; 勢力已滅
    mov byte [di+425Ch], 0FFh     ;   → 主將 +0x1C ＝ 0xFF（在野）
（是玩家的軍團時）TALK #23 ＋ #198
```

⭐ **兵員沒有回預備兵池**——`sub_14717` 完全沒被呼叫。
回不了家的軍團，那批兵就是沒了；主將回到無職，可以再被編成。

整條規則：

| 時機 | 發生什麼 |
|---|---|
| 判定的當下 | 佔用圖 −1、勢力軍團數 −1、存在旗標 ＝ 8、`+0x03` ＝ 48、地圖上不再顯示、TALK #1F／#20 |
| 48 個 tick 之後 | 軍團記錄歸零、主將無職（原勢力已滅則改在野）、玩家收到 TALK #23 ＋ #198 |

推論等級：**強證據**。分支與兩個條件、`sub_12977` 與 `sub_12A7E`
都是 IDA 解出來的；§8.1 的成本與高位元那六行是手工解碼原始 bytes 得到的。

### 8.4 `sub_1487B`：找回家路上的下一個己方據點

`sub_1474A` 與 `sub_14DA4` 都用它決定「接下來往哪走」。它算的**不是**
攻擊目標，是**回首都的方向**：

```asm
bx = 勢力記錄 / al = [bx+3]        ; ★ 目標寫死是首都
cmp al, 0FFh / jz → STC            ; 沒有首都 ⇒ 失敗
ax = 首都 × 8
dl = [si+1] / bx = [si+0Eh]        ; 勢力／現在節點×8
cmp bx, 800h / jb → 直接找路        ; 現在在據點
  （在野外時）先從兩個鄰接槽裡挑一個**屬於自己**的當起點
call loc_1491B                     ; 廣度優先找路（§8.1）
cmp ax, ±4 / 相符就 bx += 2         ; 回傳的方向決定讀哪一個鄰接槽
mov bx, [bx+6]                     ; ★ 路上的下一個節點
cmp dl, [bx*4+841h] / jnz → STC    ; ★ 那個節點不是自己的 ⇒ 失敗
CLC
```

⭐ **一次只走一個「自己的」據點。** 兩個歸屬檢查各擋一種情況：
起點的鄰接要是自己的、下一站也要是自己的。

道路表的每一筆有**兩個鄰接槽**（`+6` 與 `+8`），
`loc_1491B` 回傳的方向（`+4`／`−4`）決定讀哪一個——
`sub_147BB` 開頭的 `cx = es:[bx+6]` / `bx = es:[bx+8]` 是同一組欄位。
廣度優先搜尋回 carry 時（`loc_1490C`）拿**首都本身**當下一站，
而且**不再檢查歸屬**。

### 8.4.1 這支不是 Stage 10／11 用的

`sub_1487B` 的呼叫者只有兩個：`sub_1474A`（戰後，[`09`](09-combat.md) §5）
與 `sub_14DA4`（據點失守時把受影響的軍團調頭，`sub_14CF3` 的下游）。

**Stage 10／11 走的是另一條路**：

```asm
000144A9  bh = [si+1] / bx = 勢力 × 64
000144B2  mov bh, [bx+3]          ; 首都
000144B5  cmp [si+20h], bh / jz →
000144BA  mov [si+20h], bh        ; ★ 一次把目標設成首都
000144BD  or byte ptr [si], 2
```

所以 remake 的 `headHomeResupply`／`arriveDisband` 一次 `March(i, 首都)`
**與原版一致**。逐站前進屬於戰後與失守那兩條路——
remake 的對應在 [`../spec/46`](../spec/46-post-battle-retreat.md)。

## 9. 未解

| 項目 | 現況 |
|---|---|
| `loc_1491B` 的完整成本模型 | 只解出「穿過非己方據點 ＋0xA6 並設高位元」（§8.1）。廣度優先搜尋本身的佇列結構與其他成本項沒逐條讀 |
| remake 的對應 | §8 已實作（[`../spec/43`](../spec/43-rout-on-blocked-return.md)）；**§8.4 的「逐站前進」沒有移植**，remake 一次算完整條路 |
| `sub_128F4` 的 STC 分支 | 走到敵方據點時呼叫 `sub_1291A`（俘虜／脫離判定），之後 `di` 不可信。本文件的 `di` 推論只涵蓋一般路徑 |
