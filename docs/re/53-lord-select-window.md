# 53 — 君主選擇視窗（顯示清單場景 8）

**狀態：視窗矩形、靜態層、七個顯示值的座標與來源、兩個熱區的語意
全部解出來了。⭐ 「確定」在軍師還沒命名時會被導去「自定」（§3）。**

- 日期：2026-08-15
- 範圍：只驗松崗 DOS/V
- 原始 `KI.EXE` SHA-256：`fffeba985231cda4d636e93d10f598470b1f691d00275e4aa38e285893d43868`
- 原始 `KI.EXE.i64` SHA-256：`7b7c1aa67c47f99062cfdd4439b3423302808c874f929c3ea6f75ec564034c26`
- 工具：**IDAPython** `tools/ida_dump.py`；靜態層由 `tools/displaylist.py` 解
- 位址空間：IDA DOS/V linear address

## 1. 視窗與靜態層

```asm
sub_18F37:
  al = 1Fh / dx = 0Ah / bx = 5 / cx = 0C0Fh
  sub_1895D                        ; 框 (160, 112) 240×192
  al = 8 / dx = 0A8h / bx = 78h
  sub_10337                        ; 場景 8，原點 (168, 120)
  sub_1E3D7(al=20h, dx=148h, bx=110h, cx=206h)   ; 熱區 0x20 (328, 272) 48×16
  sub_1E3D7(al=21h, dx=148h, bx=0F8h, cx=206h)   ; 熱區 0x21 (328, 248) 48×16
```

| 元件 | 座標 | op |
|---|---|---|
| 上半的大底框 | (176, 128) 208×104 | 03 |
| 左下的底框 | (176, 240) 144×48 | 03 |
| 垂直線 | (247, 240) 長 48 | 06，色 `0x0F` |
| 「自定」的底 | (328, 248) 48×16 | 03 ＋ **04** 外框 |
| 「確定」的底 | (328, 272) 48×16 | 03 ＋ **04** 外框 |
| 「君主」 | (184, 200) | 08，色 `0x0F` |
| 「軍師」 | (312, 128) | 08，色 `0x0F` |
| 「首　都」「武將數」「據點數」 | (184, 240／256／272) | 08，色 `0x0F` |
| 「自定」「確定」 | (336, 248)／(336, 272) | 08，色 `0x00` |

⚠ **熱區編號與畫面上下顛倒**：`0x20` 是下面的「確定」，`0x21` 是上面的「自定」。

## 2. `sub_18EA0`：七個值

`si` 是勢力記錄。

| 值 | 座標 | 位數 | 來源 |
|---|---|---:|---|
| 君主頭像 | (184, 128) | — | 勢力 `+0x01` → 武將 `+0x01` |
| 君主名 | (200, 216) | — | 勢力 `+0x01` |
| 軍師頭像 | (312, 168) | — | 勢力 `+0x02`；**`0xFF` 就不畫** |
| 軍師名 | (328, 144) | — | 勢力 `+0x02` |
| （肖像的 `bx`）| `sub_107D2` 的 `bx` 是 **VRAM 位元組位移** `y*80 + x/8`：君主 `2817h` ⇒ (184,128)、軍師 `34A7h` ⇒ (312,168) | `sub_18EA0` | — |
| 首都名 | (264, 240) | — | 勢力 `+0x03` 的據點名 |
| 武將數 | (272, 256) | 3 | 勢力 `+0x18` |
| 據點數 | (272, 272) | 3 | 勢力 `+0x23` |

四個欄位都在 [`../formats/08`](../formats/08-sinario-save.md) 已 confirmed，
這裡是它們的顯示端。

## 3. ⭐ 沒命名的軍師不能直接「確定」

```asm
sub_1E453 / sub al, 20h
jb → 回去等
ja → 回傳 al                      ; 熱區 0x21（自定）→ al = 1
; al == 0（確定）
cmp byte [si+2], 7Fh / jnz → 回傳  ; 軍師欄不是 0x7F（無）→ 回傳 0
cmp word ds:5222h, 0D0A1h / jnz → 回傳
al = 1                             ; ⭐ 兩者都成立就當成「自定」
```

`0x7F` 是「沒有軍師」（[`../formats/08`](../formats/08-sinario-save.md) 勢力 `+0x02`），
`0xD0A1` 是空字串標記（與四槽視窗判空槽用的是同一個值，
[`52`](52-slot-select-window.md) §3）。

**所以按「確定」但軍師欄是空的、名字也還沒取，就會被導去命名畫面**
（場景 9 的注音輸入）。回傳值 0 ＝ 確定、1 ＝ 自定。

右鍵取消走 `sub_18F7C` 並 `stc`。

## 4. ⭐ `ds:5222h` 是自訂軍師名的緩衝區（2026-08-23）

先前這一欄標「推測，配置端未讀」。用 `tools/ida_disp_users.py` 掃位移 `5222`
掃出六處，六處合起來就把它定住了：

| 位址 | 動作 |
|---|---|
| `sub_1905E` `00019062` | `mov word ptr [si+5222h], 40A1h` ← 初始化 |
| `sub_1906E` `00019075` | 同上 |
| `sub_190C0` `0001911E` | `mov [si+5222h], ax` ← **實際寫入名字** |
| `sub_1915B` `00019168` | `mov si, 5222h` ← 讀 |
| `sub_19223` `0001922D` | `mov si, 5222h` ← 讀 |
| `sub_18F7C` `00018F8A` | `mov bx, 5222h` ← 取消時擦除 |

⭐ `40A1h` 這個初值本身就是證據：little-endian 存成 bytes 是 `A1 40`，
**Big5 的全形空白**。所以那塊是一個以全形空白填初值的字串緩衝區，
與 §3 拿 `0xD0A1` 判「名字還是空的」對得起來。

⚠ **為什麼線性 xref 掃不到它。** `ds:5222h` 是**執行期段內位移**，
不是映像裡的位址——IDA 的 xref 只涵蓋直接位址參考，
`基址暫存器 + 常數位移` 完全不在裡面。第一次用 `XrefsTo(0x15222)` 掃回零筆，
而那個零是**掃描條件錯**不是「沒有人用」（同 `docs/re/30` §6 的坑）。

## 5. 未解

| 項目 | 現況 |
|---|---|
| ~~換勢力~~ | ✅ **解了**：呼叫端 `sub_11AC3` 的 `jb` 是退回上一層的**勢力清單**（`sub_17B3C`），不是在這一支裡換下一個。卡片本身沒有換勢力的熱區（[`73`](73-new-game-faction-list.md) §1–§2）|
| ~~自訂軍師名存不存得進 SAVE.DAT~~ | **存得進**：`ds:5221h`／`5222h` 在 `ds:0D52h` 段裡，區塊 `+0x52A1`／`+0x52A2`（[`../formats/10`](../formats/10-end-s15-namechars.md) §4）。原先的推論：`SAVE.DAT` 的勢力記錄只有 `+0x02`（軍師的武將編號，`0x7F` ＝ 無），[`../formats/08`](../formats/08-sinario-save.md) 沒有任何自訂名字的欄位。`ds:5222h` 在遊戲的資料段裡，**有沒有被寫進存檔、寫在哪一段，未讀** |
| ~~`sub_190C0`~~ | 讀了：16×6 格、格距 20、選字表 `END_S15.DAT`（[`../formats/10`](../formats/10-end-s15-namechars.md)）|
| ~~`sub_18F6D`~~ | `sub_1895D(al=0, dx=0Ch, bx=6, cx=0C0Fh)`——把君主卡那塊矩形清掉 |

<!-- 缺口：無 -->
