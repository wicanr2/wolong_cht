# 26 — 一覽表視窗引擎

**狀態：視窗幾何、五個一覽表家族的描述子、選取迴圈、持久化排序狀態、四個描述子欄位
（兩個 callback ＋ 標題字串 ＋ 欄位數）與**捲軸的四個熱區**全部 confirmed，
五組欄位標題與分隔線已取出。畫列常式 `sub_1748F` 的內部未讀。**

- 日期：2026-08-13
- 範圍：只驗松崗 DOS/V
- 原始 `KI.EXE` SHA-256：`fffeba985231cda4d636e93d10f598470b1f691d00275e4aa38e285893d43868`
- 原始 `KI.EXE.i64` SHA-256：`7b7c1aa67c47f99062cfdd4439b3423302808c874f929c3ea6f75ec564034c26`
- 工具：IDA Pro 9.4，`tools/ida_listwin.idc`、`tools/ida_listcb.idc`
- `KI.EXE` 檔案位移 ＝ 段內偏移 ＋ `0x200`（由五組標題字串各自對上而定）
- 位址空間：IDA DOS/V linear address，segment base `0x10000`

## 1. 一個引擎，五種清單

戰略層的每個指令都要先選東西——選據點、選武將、選勢力、選軍團。
這些一覽表**共用同一個引擎**，差別只在四個 callback 指標。

```
sub_181C0(dx=x, bx=y, cx=尺寸)      開視窗
sub_1820E(ax, bx, si, di, cl=狀態)  跑選取迴圈，回傳 bx＝選中項；CF=1 取消
```

八個呼叫端全部用同一組視窗幾何 `dx=0x18, bx=0x58, cx=0x0B18`。

## 2. `sub_181C0`：視窗幾何

```asm
mov word_181B6, cx
mov ax, 4 / xchg al, cl / shl ax, cl    ; ax = cl_原值 << 4
mov word_181B2, ax                      ; 寬（像素）
xor ax, ax / mov al, ch / shl ax, cl
mov word_181B4, ax                      ; 高（像素）
mov word_181AE, dx / mov word_181B0, bx ; 左上角
sub dx, 8 / sub bx, 8 / shr dx, cl / shr bx, cl
```

`cx` 把尺寸打包成 **16 像素為單位的格數**：`cl` ＝ 寬幾格、`ch` ＝ 高幾格。
一覽表用的 `cx = 0x0B18` 就是 **24 格寬 × 11 格高 ＝ 384 × 176 像素**，
左上角在 `(0x18, 0x58)` ＝ `(24, 88)`。

640×400 的畫面上，這個視窗蓋住 x 24–408、y 88–264。
x 的範圍與指令列（[`22`](22-strategy-command-tree.md) §3，24–408）**完全相同**。

## 3. `sub_1820E`：選取迴圈

```asm
sub sp, 200h / mov bp, sp          ; 512 B ＝ 256 個 word 的結果陣列
mov cs:word_181A8, ax              ; ← ax 是函式指標
mov cs:word_181A6, bx              ; ← bx 是函式指標
mov cs:word_183D3, si
mov cs:word_18320, di / mov cs:word_18591, di
mov cs:byte_181BC, 0
xor ch, ch / shl cx, 1
mov cs:word_181BA, cx              ; 進來的狀態 ×2 ＝ 目前選取列的位移
es=ss / di=bp / cx=100h / ax=FFFFh / rep stosw   ; 陣列填 0xFFFF
call cs:word_181A8                 ; ← 建清單：把項目編號寫進 SS:[bp] 陣列
mov cs:byte_181BD, ah
mov al, 3Dh / call loc_1828F
mov bx, cs:word_181BA / call loc_1857F
call sub_18412                     ; ← 熱區分派迴圈
jb → 取消（CF=1）
mov bl, al / bx = al × 2 / bp += bx
mov bx, [bp+0]                     ; ← 從陣列取出選中項
clc
```

三件事定案：

1. **`ax` 與 `bx` 是函式指標，不是資料。** 呼叫端把自己的 callback 位址傳進來，
   引擎存進 `word_181A8`／`word_181A6` 再間接呼叫。
2. **清單最多 256 項**（512 B ÷ 2），未使用的格子是 `0xFFFF`。
3. **回傳的是陣列裡的值**，不是列號——所以 callback 寫進去的就是呼叫端要的編號。

`sub_18412`（[`22`](22-strategy-command-tree.md) §2 的熱區分派）在這裡當選取迴圈用：
它讀游標下的熱區編號減 `0x3D` 再查 `funcs_18450`，五個 handler 對應
捲動與選取，超過的走 `loc_1857F`。

### 3.1 訂正：`word_181A6`／`word_181A8` 靜態可解

[`21-function-census.md`](21-function-census.md) §7 把
`call cs:word_181A6` 這一組列為「開機時指向 return stub，實際目標由 runtime 填，
要靠動態 trace」。**靜態影像確實是 `07 1F C3 90`（`pop es; pop ds; retn; nop`），
但目標完全靜態可解**——八個呼叫端都在 `sub_1820E` 進入時用立即值傳進來。

教訓寫成規則：**看到「全域函式指標」不要先判定要動態 trace。
先找誰寫它**——寫入端如果是立即值，候選集合就是有限且已知的。

## 4. 五個一覽表家族

八個呼叫端，五組 `(bx, si, di)`。同一組的兩個變體只有 `ax`（建清單的 callback）不同，
也就是**同一種清單的兩種取法**。

**四個描述子不是四個函式**：`ax`／`bx` 是 callback，`si`／`di` 是資料（§4.1）。

| 家族 | `bx` 畫列 | `si` 標題 | `di` 欄位數 | 呼叫端 | `ax` 建清單 | 用在哪 |
|---|---|---|---|---|---|---|
| 軍團 | `0x724D` | `0x70EC` | `0x713D` | `sub_1716D` | `0x71A8` | 軍團指令：位置確認／行軍指示 |
| | | | | `sub_171D3` | `0x7217` | `sub_11E46` |
| 據點 | `0x745F` | `0x7378` | `0x73CE` | `sub_17400` | `0x743B` | 據點一覽、遷都、內政官任免 |
| 武將 | `0x76DC` | `0x7550` | `0x75C8` | `sub_17663` | `0x76A0` | 選武將（任命、編成）|
| | | | | `sub_175FA` | `0x763C` | 指令列 #6 武將：看能力 |
| 勢力 | `0x796C` | `0x77EE` | `0x7875` | `sub_17906` | `0x7944` | 外交、外交官任免 |
| | | | | `sub_178A7` | `0x78E5` | 指令列 #7 勢力：看交友度 |
| 開局選勢力 | `0x7B90` | `0x7AC6` | `0x7B12` | `sub_17B3C` | `0x7B6F` | `sub_11AC3` 新遊戲選玩家勢力 |

### 4.1 `si` 是欄位標題，`di` 是欄位數

`si`／`di` 指的不是程式碼——把它們當函式反組譯出來是亂碼。
以 Big5 解讀才對：`si` 指向欄位標題字串（NUL 結尾，其後緊跟分隔線），
`di` 指向一個 byte，值就是欄位數。

| 家族 | 欄位標題 | `di` | 分隔線 |
|---|---|---:|---|
| 軍團 | `武將名　總兵數　士氣值 現在位置 目標據點` | 5 | `－－－　 ----　　---　　－－－　 －－－` |
| 據點 | `據點名　生產力　上昇率　防災　城兵　內政官` | 6 | `－－－　-----　　----　 ---　 --- 　－－－` |
| 武將 | `武將名　武術 統率 政治　　勢力　　　身分` | 6 | `－－－　 --　 --　 --　 　－－－　　－－－` |
| 勢力 | `勢力名　武將　據點　首都　　外交　　外交官` | 6 | `－－－　--- 　--- 　－－－　－－　　－－－` |
| 開局選勢力 | `勢力名　軍師名　　武將　據點　　首都` | 5 | `－－－　－－－　　 --　　-- 　　－－－` |

**五組的欄位數與標題的欄位個數逐一吻合**，所以 `di` 的語意是 confirmed 不是推測。
`di` 同時被存進 `word_18320` 與 `word_18591`——點欄位名排序要知道有幾欄，
畫表頭也要，兩處各拿一份。

分隔線的 `－`（全形）與 `-`（半形）交錯，正好標出每一欄的字寬：
全形欄放中文名，半形欄放數字。這是 remake 排版可以直接沿用的欄寬定義。

### 4.2 「同一種清單、兩種取法」

與
[`../mechanics/10-strategy.md`](../mechanics/10-strategy.md) §3 的說明對得上：
指令列的「武將」「勢力」是**看**（顯示表上沒列的參數、交友度），
人事與外交的一覽表是**選**。兩者欄位不同，所以建清單的 callback 不同、
畫列的 callback 相同。

## 5. 排序狀態是持久的

每個家族有自己的狀態 byte，進 `sub_1820E` 前讀出、回來後寫回：

| 家族 | 狀態 byte |
|---|---|
| 軍團 | `cs:word_198A8` 低位元組 |
| 據點 | `cs:word_198A8` 高位元組 |
| 武將 | `cs:word_198AA` 低位元組 |
| 勢力 | `cs:word_198AA` 高位元組 |

這正是 [`../mechanics/10-strategy.md`](../mechanics/10-strategy.md) §5 由說明書記下的
「排序狀態以**視窗種類**為單位記住，不必每次重排」——現在有機器碼側的對照，
而且知道它存在哪、只有一個 byte。

`sub_17663`（選武將）進來前先 `xor cl, cl` 再讀，所以**它每次都從第一列開始**；
其餘七個直接沿用上次的位置。

## 6. 解任：`sub_16B4F` 與 `sub_16C2A`

兩支完全同構，只差目標欄位（據點 `+0x19` ／ 勢力 `+0x2A`）：

```asm
mov bh, 0FFh
xchg bh, [si+19h]        ; 欄位 ← 0xFF，bh ← 原本的武將編號
cmp bh, 0FFh / jnz +2
  stc / retn             ; 本來就沒人 → 失敗
xor bl, bl               ; bx = 編號 × 256
shr bx,1 / shr bx,1 / shr bx,1
add bx, 4240h            ; ÷8 → 編號 × 32，加基址 = 武將記錄
mov byte [bx+1Ah], 0     ; 經費餘額 ← 0
mov byte [bx+17h], 0     ; 職務 ← 無
clc
```

`xchg` 一步同時「清空欄位」與「取出原值」，所以**沒有中間狀態**。

**解任會沒收經費餘額。** `+0x1A` 是官員的經費餘額
（[`../formats/08-sinario-save.md`](../formats/08-sinario-save.md) §3，撥款金額 ÷ 128），
解任時直接歸零——先撥款再解任，那筆錢就消失了。
這是可玩性上的實質規則，不是清理程式碼。

## 7. 未解

| 項目 | 現況 |
|---|---|
| `sub_1748F` | 畫一列的實作（193 B），未讀 |
| `word_183D3` | `si` 也存進這裡，讀取端還沒找 |
| `byte_181BD` | ✅ 已解：建清單 callback 以 `ah` 回傳筆數（§5.1）|
| `sub_11D46` | 17 個呼叫點，人事四支離開前都呼叫，未讀 |

## 8. 建清單與畫列的實作

### 8.1 建清單：`ax` callback

據點家族的 `0x743B`（linear `0001743B`，36 B）是最短的一支，全貌：

```asm
push si / push di
mov si, 840h              ; 據點表的段內偏移
xor di, di                ; 結果陣列游標
mov al, cs:byte_10CFF     ; 玩家勢力編號
xor ah, ah                ; 筆數
loop:
  cmp al, [si+1]          ; 據點 +1 ＝ 所屬勢力
  jnz skip
    mov [bp+di], si       ; ← 寫進 SS 陣列的是**記錄位址**，不是編號
    inc ah
    inc di / inc di
skip:
  add si, 20h             ; 下一筆（32 B）
  cmp si, 2040h           ; 0x840 + 192 × 32 ＝ 0x2040
  jnz loop
pop di / pop si / retn
```

三件事同時對上 [`../formats/08-sinario-save.md`](../formats/08-sinario-save.md)：
據點表段內偏移 `0x840`、192 筆 × 32 B、`+1` 是所屬勢力。
`byte_10CFF` 是玩家勢力編號也已記載。**這是獨立驗證，不是新結論。**

`ah` 回傳筆數，`sub_1820E` 存進 `byte_181BD`——這解掉了 §7 原本的未解項。

**陣列裡放的是記錄位址**，所以 `sub_1820E` 回傳的 `bx` 可以直接當指標用。
`sub_16A9B` 拿到後立刻 `cmp byte [bx+19h], 0FFh`（[`25`](25-message-variants-and-personnel.md) §3）就是這個緣故。

### 8.2 畫列：`bx` callback

據點家族的 `0x745F`（48 B）：

```asm
call sub_18607
dx = cs:word_181AE + 18h    ; 視窗左上 x ＋ 24
bx = cs:word_181B0 + 10h    ; 視窗左上 y ＋ 16
si = 目前頁首索引 × 2
ax = 0Ah                    ; 10 列
loop:
  call sub_1748F
  si += 2 / bx += 10h       ; 下一列往下 16 像素
  dec ax / jnz loop
```

**每頁十列，列高 16 像素**，第一列在視窗內 `(+24, +16)`。
視窗高 176 像素，扣掉表頭與分隔線正好放得下十列。

## 9. 身分名稱表

`0x75A4`（file `0x077A4`）是六筆 6 B 的全形字串：

| 值 | 松崗版用字 |
|---:|---|
| 0 | `－－－` |
| 1 | `軍團長` |
| 2 | `內政官` |
| 3 | `外交官` |
| 4 | `俘虜　` |
| 5 | `君主　` |

這是武將一覽最後一欄「身分」的顯示來源，**與武將記錄 `+0x17` 的列舉
（[`25`](25-message-variants-and-personnel.md) §4.1）逐值對上**，
並多出 `5 ＝ 君主`。`+0x17` 在四個劇本的初始值全是 0，君主的身分是
顯示時從勢力表反查的，不是存在武將記錄裡。

> **譯名**：松崗版用「**俘虜**」，不是日文的「捕虜」。
> `docs/formats/08` 等文件沿用日文寫法，**面向玩家的文字一律以松崗版為準**。

## 10. ⭐ 捲軸：四個熱區的矩形與行為

`sub_181C0(cx ＝ 高<<8｜寬 的粗格數, dx ＝ X, bx ＝ Y)` 把視窗參數拆成兩套單位：

```asm
word_181B6 = cx                 ; 尺寸（16 px 粗格）
word_181B2 = 寬 << 4            ; 寬（像素）
word_181B4 = 高 << 4            ; 高（像素）
word_181AE = dx                 ; X（像素）
word_181B0 = bx                 ; Y（像素）
word_181AA = (dx − 8) >> 4      ; X（粗格）
word_181AC = (bx − 8) >> 4      ; Y（粗格）
word_181B8 = Y × 80 + X ÷ 8     ; VRAM 位移
```

**框比內容區四邊各大 8 px**：`loc_1828F` 用 `word_181AA`／`word_181AC − 2`
與 `word_181B6 + 0x101` 呼叫 `sub_1895D`，也就是往左上各退一格、寬高各多一格。
一覽表 `(24, 88, 384, 176)` 的框因此是 `(16, 80, 400, 192)`。

接著登記四個熱區（`al` 從 `0x3D` 進來，每次 `inc` 再登記）：

| 熱區 | 矩形 | handler | 行為 |
|---:|---|---|---|
| `0x3E` | (X+16, Y+16, W−16, H−16) | `sub_18463` | **清單本體**：`(游標Y − Y − 16) ÷ 16 + top` ＝ 第幾筆，超出筆數就不理 |
| `0x3F` | (X, Y+32, 16, H−48) | `sub_184DD` | **捲軸槽**：按住不放時每輪 `sub_18713(游標Y)` 換算成新的 `top` |
| `0x40` | (X, Y+16, 16, 16) | `sub_1851A` | **▲**：`top − 1`，0 就不動 |
| `0x41` | (X, Y+H−16, 16, 16) | `sub_18546` | **▼**：`top + 1`，上限 `筆數 − (高格 − 1)` |
| `0x42`＋ | 欄位標題 | `loc_1857F` | 分派時 `bx ≥ 0x0A` 就減 `0x0A` 走排序（§5）|

三段在垂直方向剛好接續：▲ 佔 `Y+16`–`Y+32`、槽佔到 `Y+H−16`、▼ 到 `Y+H`。

⭐ **「一頁 10 列」在這裡有第二個獨立來源**：`sub_18546` 用
`al = word_181B6 的高 byte − 1`（11 − 1 ＝ 10）當一頁的列數去算 `top` 的上限。

⭐ **清單本體從 `X+16` 起，不是 `X`**——左邊那 16 px 是捲軸。
反白列的常式 `sub_184BC` 也是從 `word_181AE + 0x10` 開始、寬 `word_181B2 − 0x10`，
兩處一致。
