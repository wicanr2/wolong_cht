# 原版素材一律不進版控（CLAUDE.md §10）
卧龙传.zip
*.rar
Garyouden_Manual.pdf
Garyouden_Xtra_Addendum.pdf
workplace/

# IDA 產出
*.i64
*.idb
*.asm
*.id0
*.id1
*.id2
*.nam
*.til

# Go
/dragon
/bin/
/wlgame
/wlscen
/wlsim
/wlview
/wlshot
*.test

# Android：只收原始檔，build 產出與 gomobile 綁定不進版控
android/.gradle/
android/build/
android/app/build/
android/app/libs/
android/local.properties
*.apk
*.aar
*.so
*.dex
*.class
*.jar

# 暫存
__pycache__/
*.pyc
.DS_Store

# 截圖：只有 docs/images/ 底下的 README 展示圖是例外（CLAUDE.md §10）
*.png
*.jpg
!docs/images/*.png

# Go 檢視器的截圖輸出（workplace/ 已整個忽略，這行只是提醒）
workplace/shots/
dist/
dist-all/
# 可散布批次（WOLONG_BUNDLE_DATA=0）：不含原版資產，但仍是建置產物。
dist-public/

# 內嵌進 APK 的原版資料（docs/spec/72 §4）。由 tools/android_build.sh 在
# WOLONG_BUNDLE_DATA=1 時複製進去，建完就清掉。⚠ 這是原版資產，
# 一個 byte 都不准進版控——CLAUDE.md §9 的「不得 commit」那半邊沒有例外。
android/app/src/main/assets/
