# Luna 執行提示詞：完成《臥龍傳》松崗 DOS/V 版功能與畫面收尾

**狀態：可直接交給 Luna 執行；依 2026-08-12 推廣片與目前工作樹整理。**

- 日期：2026-08-12
- 唯一原版基準：松崗繁中 DOS/V 版
- 主要比較片：`dist-all/promo/wolong-remake-dosv-live-comparison.mp4`
- 比較片 SHA-256：`4e886f456ffaa3bc93cf3a97450e9415a4033ed699929efb8ccf6f7eff0ac414`

以下整段可直接作為 Luna 的工作提示詞。

---

你現在接手 `/home/anr2/cht/dragon` 的《臥龍傳》remake 收尾。你的任務不是另做新遊戲、
不是只寫報告，也不是重跑完整長程劇本；請在現有實作上把玩家可見、可操作的松崗 DOS/V
原版流程補齊並 polish，完成程式、短路徑驗收、比較畫面、文件與最後封裝。

## 一、動工前必讀與不可違反事項

1. 完整讀取 `AGENTS.md`、`CONTEXT.md`、`WORKLIST.md`、`VERIFICATION-MATRIX.md`、
   `docs/INDEX.md`。若文件互相衝突，以使用者最新決定與可重播證據為準。
2. 只以松崗繁中 DOS/V 版為 oracle；忽略 PC-98 畫面與輸入差異。
3. 密碼頁明確不重製。DOS/V 原版可用空白、`0000` 或 `1234` 越過密碼頁後取樣；詳見
   `docs/playtest/18-dosv-password-verification.md`。
4. 所有分析、DOSBox-X、IDA Pro、轉檔、建置、測試、Xvfb、錄影與 GUI 自動化只能在
   Docker 內執行。原版資料唯讀掛載；可寫容器指定目前 UID/GID、資源上限與 `--rm`。
5. 不得重建 `HANDOFF.md`。進度、未完成項與證據只更新 `WORKLIST.md`、`CONTEXT.md` 及
   對應的 `docs/` 文件。
6. 不得把原版執行檔、資料、字型、音樂或影片來源包進 release。遊戲執行時仍由玩家用
   `-orig` 指向自備 DOS/V 資料。
7. 保留目前所有使用者修改。先看 `git status --short` 與相關 diff，不可重設或覆蓋整個
   工作樹。
8. 反組譯時以既有 IDA Pro 9.4 `.i64` 為主，採非破壞性註記；保留原函式名、線性位址、
   原始運算元及 raw bytes。每項結論標示「已證實／強推論／假說／未知」及證據來源。

## 二、先建立正確的比較基準

先看下列現有證據，不要從舊待辦文字直接猜差異：

- `dist-all/promo/wolong-remake-dosv-live-comparison.mp4`
- `docs/promo/dosv-live-comparison.md`
- `docs/promo/yt-remake-pixel-review.md`
- `docs/promo/dosv-skeleton-compare.png`
- `docs/promo/yt-remake-natural-side-by-side.png`
- `docs/images/wlgame-dosv-natural-remake-skeleton.png`
- `docs/playtest/17-expert-dosbox-remake.md`

這支推廣片是「同類畫面、不同遊戲狀態」的比較，不是同日期、同輸入、同鏡頭的逐像素
證明。片中的可見差異必須分成三類記錄：

1. **已可直接修正**：幾何、框線、字距、glyph、介面組成、缺少的畫面區塊。
2. **需同狀態重錄才可判定**：地圖內容、日期、兵力、勢力、鏡頭、事件文案與單位位置。
3. **影片無法判定**：規則、機率、事件 producer、音訊與長時間 AI 行為；必須改用程式、
   原版短 fixture 或 IDA 證據。

不得用目前 `97.34%` 全畫面差異率當 renderer 錯誤率；來源有縮放、有損壓縮且狀態不同。
每次宣稱畫面 parity 前，至少保存原版與 remake 各一張 640×400、相同劇本／玩家／日期／
鏡頭／視窗狀態的 raw PNG，再分區比較。若無法固定原版亂數，明確標成「骨架比較」，不可
標成逐像素完成。

## 三、影片與程式已確認的差異

### A. 啟動／新遊戲殼層缺席（已證實）

推廣片 04–09 秒顯示原版 `NEW GAME` 正常入口，但 remake 沒有對應片段。現有
`cmd/wlgame/main.go` 會在命令列解析 `-scenario`、`-player` 後直接載入世界並進入策略畫面，
視窗標題仍是「臥龍傳 戰略畫面原型」。遊戲內已有四槽存檔／讀檔，但沒有玩家可操作的
啟動殼層。

### B. 自然策略骨架接近，細節仍未封口（可見差異）

目前 640×400、32 px banner、32 px 命令列、432×336 地圖、208 px 右欄、minimap 色標列與
共享 8 px 分隔已接近原版。仍應逐區驗證：

- banner 裝飾、日期框、命令字的基線／間距／選取色；
- minimap 的內縮、紅藍標頭、勢力 swatch 與框線接縫；
- 右欄 64×64 肖像、君主／首都／軍師、信賴度、資金／預備兵的基線與欄寬；
- 尚未解出的中央預備兵 raw glyph；不可用看似相近的向量符號冒充原版資產；
- 地圖上的硬體游標、單位／據點選取 glyph、動畫相位與遮罩順序；
- 事件小窗、列表、數值輸入窗的尺寸、內框、每頁行數、消像與背景回復。

不同日期、不同地圖狀態造成的道路、城池與部隊位置差異不得當成缺陷。

### C. 戰術畫面是目前最大差距（已證實）

推廣片 33–40、45–50 秒的 DOS/V 原版有完整戰場、主將肖像與 TALK、右側雙方狀態、戰場
縮圖、陣形／命令區、底部原版按鈕 glyph、單位與戰果過場。40–45 秒的 remake 是
`-open-siege` debug fixture：只顯示局部放大的等角地形、簡化上方文字、城壁小框與文字命令
列，右下大面積為空；它只能證明 renderer 活著，不能代表原版戰術畫面 parity。

目前相關入口在：

- `cmd/wlgame/battle.go`：戰鬥輸入、簡化 banner／按鍵列／結果視窗；
- `internal/ui/isoview/isoview.go`：等角投影、地形、士兵、軍旗、投射物；
- `internal/assets/battle/`：`BATTLE.MAP`／`BATTLE.MDL`／`BATTLE.SCH` 解碼；
- `internal/rules/tactical/`：戰術規則、路徑、傷害與投射物；
- `docs/re/11-tactical-battle.md`、`docs/playtest/09-wlgame-normal-tactical-path.md`。

不要推翻已完成的投影、單位、投射物與規則層。應先重做戰術 640×400 畫面編排，再補畫面
需要而現有模型缺少的狀態。

### D. 事件 10 的玩家功能已完成，原版 writer 仍未知（證據邊界）

玩家不下命令且滑鼠穩定時，據點／軍團／物件／時鐘依序更新；日期流逝、已下令軍團行軍、
每時事件 consumer 與月結俘虜通知均已串接。`internal/state/event10_approx.go` 是可關閉、可測的
remake substitute。原版自然低碼 `0x0A` producer 仍是 unknown；不要為了追它阻擋收尾，也
不要把 substitute 改寫成已證實原版規則。

## 四、依序執行的四個封口關卡

### 關卡 1：補齊玩家啟動殼層

在 `wlgame` 內新增可用鍵盤與滑鼠完成的啟動狀態機，至少包含：

1. 原版風格的 `NEW GAME` 確認；不包含密碼頁。
2. 四個劇本選擇、玩家勢力／君主選擇與確認。
3. 若指定可寫 `-save-file`，可由啟動殼層選四槽讀檔；空槽不可誤讀。
4. 進入策略畫面後才建立／啟用正常世界迴圈與 AI。
5. 保留 `-scenario`、`-player`、`-open-*`、`-shot` 等現有自動化入口，但把它們明確視為
   測試／直接啟動捷徑；一般玩家不傳旗標時必須看到殼層。
6. 將視窗標題中的「原型」移除，改成正式名稱。

驗收：以固定 seed 分別走新遊戲與讀檔兩條短路徑；保存 640×400 啟動畫面、劇本選擇、君主
選擇與進入地圖四個代表幀。測試殼層狀態轉移、取消、空槽與確認，不依賴固定秒數。

### 關卡 2：優先完成原版戰術全畫面

先從 DOS/V 正常流程擷取至少一組攻城與一組野戰；記錄劇本、玩家、雙方軍團、地圖編號、
鏡頭、命令與畫格。依量測結果建立集中式版面常數，禁止在多個 draw function 散落魔術數字。

必須補齊：

1. 原版比例的左側戰場 viewport；相機不得把主要交戰區裁到只剩局部地形。
2. 右側完整戰術面板：雙方名稱／兵力、陣形、戰場縮圖、命令與選取狀態。
3. 主將肖像與戰術 TALK 疊窗；正確分頁、確認鍵、消像與回復戰場。
4. 底部原版圖像按鈕列及硬體游標／選取 glyph；鍵盤操作與滑鼠點擊共用同一狀態。
5. 士兵、主將、軍旗、普通與特殊投射物依原版 draw order 顯示；保留現有 raw frame index
   與運動測試，增加可見運動的連續幀驗收。
6. 城門／城壁耐久、破壞畫格、攻守雙方狀態、暫停／恢復及結果確認。
7. 戰果不要用現代化置中摘要窗取代原版流程。先量測原版戰鬥結束、主將發言、狀態回寫與
   回到策略畫面的先後順序，再用原版版面與節奏呈現。
8. 正常無 `-open-*` 路徑必須可走「行軍遭遇 → 戰鬥指揮 → 戰術 → 戰果 → 回策略」。

若 `BATTLE.SCH` 或其他資產取不到，fail visibly 並保留診斷；正式含自備原版資料的執行路徑
不可悄悄退回色點、生成地形或純文字面板後仍宣稱 parity。

驗收至少包含：

- 一個攻城 fixture、一個野戰 fixture；
- 每個 fixture 各保存初始、下令、投射物飛行、命中／城壁變化、結果五類畫格；
- 規則層確定性測試與呈現層 frame／draw-order 測試分開；
- 一次正常玩家路徑短 smoke，不跑完整戰役；
- 原版／remake 同狀態並排圖與分區差異圖。壓縮雜訊可設容差，但 UI 幾何不得只靠肉眼。

### 關卡 3：完成自然 HUD、視窗與輸入 polish

以同一劇本、同一玩家、196 年 4 月初、同一首都鏡頭重錄 DOS/V 與 remake。依 8 px 格線逐項
修正 `cmd/wlgame/strategyhud.go`、`window.go`、`amountpanel.go`、`talkscene.go`、`messages.go`
及 `internal/ui/chrome/`：

1. 對齊所有框線交會、內縮、色帶、文字基線、半形數字欄寬與右對齊。
2. 只使用 DOS/V 已解碼的外框、按鈕與游標資產；PC-98 不得混入。
3. 正常地圖、所有列表、數值 3×6 格、TALK 選項與戰術畫面都顯示一致的原版硬體游標。
4. 滑鼠命中區必須對準可見 glyph；鍵盤 fallback 改變的是同一個 selection state，不能另畫
   一套高亮。
5. TALK 固定五行頁面、硬換行、寬度、標點與 marker formatter；頁尾確認後先消像、重畫
   背景，再顯示下一層通知。
6. 事件 2–5、6／7、9、10 各用既有短 fixture 抽查代表頁。事件 6 `#72` 缺未知參數時維持
   fail-closed；事件 10 維持已標示的近似 producer。
7. 中央預備兵 glyph 若經 DOS/V 資產／IDA 證據解出才替換幾何 fallback；否則保留「未知」
   記錄，不可畫一個臆測圖案假裝完成。

驗收要對每個固定區域分別計算差異：banner、命令列、地圖、minimap、勢力資訊、modal。
文字內容不同的區域另做遮罩；不得用全畫面單一百分比掩蓋版面錯位。

### 關卡 4：做功能可達性稽核並只補真正缺口

不要因某段沒有出現在 60 秒影片就宣稱缺失。以現有規則與 UI 建立「原版功能 → remake
規則入口 → GUI 入口 → 自動化證據」矩陣，至少覆蓋：

- 新遊戲、四劇本、選君主、四槽存讀檔；
- 人事、內政、外交、編成、軍團、行軍、資金與勸誘；
- 無輸入時鐘、策略 AI、道路行軍、遭遇與委任；
- 野戰、攻城、六種戰術命令、投射物、城壁、勝敗與戰後回寫；
- 事件 2–7、9、10 的玩家可見通知／選擇／結果；
- 勝利、敗北或君主／勢力消滅後的可見流程；
- 所有正式玩家操作的鍵盤與滑鼠路徑。

矩陣已有規則測試但沒有 GUI 入口者，才補 GUI；已有 GUI 但無副作用證據者，補狀態測試；
兩者都有者不要重寫。若音訊尚無可合法發行的實作，將它列為獨立 release gap，不得把推廣片
原創配樂誤當遊戲內音訊，也不得複製原版音源。

## 五、工作節奏與完成定義

每次只完成一個可見切片，遵循：原版短擷取 → 明確比較條件 → 小幅修改 → 單測 → Docker／
Xvfb short smoke → 代表幀 → 更新證據。不要先做大規模 renderer 重寫。

一項工作只有同時滿足下列條件才可勾選：

1. 玩家可從正常 GUI 路徑到達，不只靠 `-open-*` fixture。
2. 規則副作用有確定性測試。
3. 畫面／動畫有 640×400 raw PNG 或連續幀證據。
4. 若宣稱原版 parity，有 DOS/V 同狀態畫面、raw bytes 或 IDA 位址證據。
5. 未知部分仍明確標示，不以合理猜測升格成已證實。
6. 沒有回歸 idle clock、事件 queue、存讀檔與正常戰術往返。

最低驗證命令必須沿用專案 Docker 工具鏈並包含：

```text
go test -p=1 -vet=off ./... -count=1
go vet ./...
tools/talkdat_selftest.py
tools/index.py generate
tools/index.py check
git diff --check
```

完成 GUI 後重錄比較片；新版推廣片必須把原版和 remake 的同功能畫面放在相鄰段落，戰術
不得再只用局部 debug fixture。影片仍要明示不是同狀態的片段，不得暗示逐像素相同。只有上述
四個關卡通過後才重建 `dist-all/` 的 Linux AppImage、Windows、macOS 三平台完整候選包與
SHA-256；Windows／macOS 未在目標系統跑 GUI smoke 時，仍寫成「交叉建置候選」，不可寫成
原生驗收完成。

每輪結束檢查本專案相關容器，清除本輪一次性／停止容器，不要碰其他專案。最後回覆必須列出：

- 實際修正的玩家可見功能；
- 原版／remake 對拍證據；
- 通過的正常 GUI 路徑與測試；
- 仍未知但已隔離的研究項；
- 三平台包與新版推廣片的路徑、大小與 SHA-256；
- Docker 清理狀態。

不要只回覆建議或剩餘清單；請持續實作到上述完成定義達成，或遇到需要使用者新增授權、
缺少原版輸入檔、或三次重試後仍無法排除的同一阻擋，再明確回報。

---

## 本提示詞的判讀界線

推廣片中「remake 戰術畫面只有局部 fixture」與程式中的簡化戰術 HUD 是最大差距；命令列
直入世界所造成的啟動殼層缺口則是另一個已證實的功能缺口。執行順序仍先用較小、邊界清楚的
啟動殼層建立完整玩家入口，再集中處理戰術全畫面。自然策略骨架已接近 DOS/V 原版，後續應
做量測式 polish，不應再次推翻整體版面。影片沒有原版遊戲音訊，所以無法用它判定音效／
配樂 parity；事件 10 原版 producer 也不因畫面比較而獲得新證據。
