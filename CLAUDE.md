# 臥龍傳 remake（NEO･GETEN 1994 / 松崗中文版 1995）

## ⚡ 動手前先讀 CONTEXT.md

`CONTEXT.md` 是全專案的單一入口：現況一覽、文件索引、術語表、oracle 優先序、
**已被推翻的斷言清單**、worklist。對話被壓縮或新 session 接手時先讀它。

**要查「某件事解了沒」先看 [`docs/INDEX.md`](docs/INDEX.md)**——那份是
`tools/index.py generate` 從各文件內文生出來的，含一張斷言總表
（欄位／常數 → 推論等級 → 出處）。**不要重讀整份文件，更不要重推一次。**

這份 `CLAUDE.md` 只放「不論做什麼都要遵守」的目標、原則與硬規則。
**已解出的事實不寫在這裡**——那些在 `docs/`，索引在 `docs/INDEX.md`。
規則裡不准出現不存在的檔案、目錄或工具（寫進去之前先 `ls`）。

**反組譯一律先用 IDA Pro 9.4**（環境與規則見 §4.1），Ghidra 只當交叉驗證。

---

## 1. 專案目標

把 NEO･GETEN《臥龍傳－三國制霸之計》（1994 日文版 / 1995 松崗繁中版, DOS）完整逆向，
在 **Go / Ebiten** 上乾淨重寫成可跨平台執行的引擎，保存松崗繁中版的原文，
並以日文原版做逐句對照校訂。定位是文化資產保存。

四條主線：

1. **資料格式全解**：`.MAP`／`.MDL`／`.SCH`／`.MCH` 地圖族、`*GRF.DAT` 圖庫、
   `.BRG` 調色盤、`OPEN_S*`／`END_S*` 過場、`*BGM.DAT`／`SOUND.DAT` 音訊、
   `SINARIO.DAT`／`SAVE.DAT` 存檔、`TALK.DAT` 訊息表。
2. **規則還原**：`KI.EXE` 是遊戲本體，政略、戰鬥、存檔全在裡面。
   **這一款是即時制**，不是回合制——時間怎麼推進是第一等問題（§3.1）。
3. **文本保存與校訂**：松崗版原文直接以 Big5 存在檔案裡（§3.3），
   不必做字模反查。難的是模板片段與變數插入點。
4. **引擎重寫**：Go + Ebiten，跨平台，不依賴 DOSBox。

### 定位：還原 + 現代化外殼

核心規則一律對齊原版。外殼允許現代化——高解析呈現、視窗縮放、現代操作、存檔相容——
但**每一項改動都要在文件裡標記為 remake 差異**，且不得默默改動遊戲規則。

### 語言範圍（使用者裁定 2026-08-07）

- **繁中保存 ＋ 校訂**：以松崗版原文為母本完整保存，只修明顯的錯字／斷句／溢出。
- **日文原版對照**：以 NEO･GETEN 1994 PC-98 版做逐句對照，
  找出松崗版的漏譯、誤譯與刪節。**素材已到位**（§2.1），
  而且兩版的 `TALK.DAT` 是同一個檔的兩種語言，天然就是對照組——
  這條線不需要另外找素材，只需要解開 `TALK.DAT` 的結構。
- 英文版與簡體版**這一輪不做**。但排版層仍要抽離（§6），
  否則之後想做也做不了。

### 不做的事

- 不散布原版執行檔、資料檔、美術或音樂。公開產出只有引擎程式碼與譯文校訂紀錄。
  原版資料一律 gitignore。
- 不做玩法設計改動。這是保存專案。

---

## 2. `[HARD]` 素材與外部資料

### 2.1 手上有什麼

| 素材 | 內容 | 位置 |
|---|---|---|
| 松崗 DOS/V 繁中版 | **69 檔**（原始 zip 是 72 個），`KI.EXE` 是遊戲本體 | `workplace/orig/dosv/` |
| **PC-98 日文原版** | 五片 `.fdi`，已抽出 69 檔 | `workplace/orig/pc98/` |
| 日文原版說明書 | 38 頁 200 dpi 掃描，無文字層 | 判讀結果在 `docs/reference/01` |
| 追補頁 | 4 頁 | 同上 |

**兩版是同一份原始碼的兩次編譯**，23 個資料檔 byte-for-byte 相同。
這讓「兩版對照」成為一種 oracle（§4.0）。偵查當時看到的完整清單、
每個檔的推測與當時的推論等級，保存在
[`docs/reference/04-first-survey.md`](docs/reference/04-first-survey.md)——
**那是歷史快照，現況一律以 `docs/INDEX.md` 為準。**

**尚未取得**：松崗中文版說明書（中文側最高價值的資料）。
`STDFONT.24`／`ASCFONT.24`／`ASCFONT.15` 不需要取得——原版的中文字型
內建在 `END_S13/S14.DAT`，走 `INT 15h AH=50h`
（[`docs/re/29`](docs/re/29-font-service-int15.md)）。

### 2.2 中文資料要主動上網蒐集

這款遊戲的中文資料散在部落格、論壇與 wiki，沒有一份完整的。
動手解格式、下規則結論之前先撈一輪——查表幾乎零成本，漏查會讓整條推論白走。

1. **抓回來的內容整理進 `docs/reference/`，標明來源網址與抓取日期。**
   不要散在對話裡，下一個 session 讀不到對話。
2. **社群資料的 oracle 位階最低**（§4），只能當「往哪裡找」的提示，不能當結論。
3. **⚠ 修改版／MOD 的數值一律不採信。** 本作有 2017 年的擴充 MOD
   把劇本從 4 個加到 16 個，社群寫的數值不一定出自 1995 原版。
   引用任何數值時要標明是哪一版。
4. **巴哈姆特的頁面對自動抓取會回 403**，要人工開瀏覽器看。
5. 找到與已知事實衝突的說法，記進 `CONTEXT.md` 的「已被推翻的斷言」，
   寫清楚誰推翻誰、憑什麼。

起點索引與首輪查證結果見
[`docs/reference/04-first-survey.md`](docs/reference/04-first-survey.md) §1。

---

## 3. 這一款的三個特性，決定了做法

### 3.1 即時制

維基與說明書都說是即時制，`KI.EXE` 的 UI 詞表裡
**`戰略速度` 與 `戰術速度` 是兩個獨立設定**——大地圖與戰場各有各的時間流速。
**規則層要以 tick 為單位設計，不是回合。**

說明書第 3.5 節寫明「選擇的速度超過機器速度時，設再快也不會更快」，
也就是**原版沒有固定 tick rate，速度上限跟著機器跑**。
照抄會得到一個在現代機器上快到不能玩的遊戲；remake 必須自己定義固定時間基準，
**並標記為 remake 差異**。見 `docs/mechanics/15-realtime.md`。

即時制也讓 oracle 變難：回合制送固定按鍵序列就能重現畫面，即時制不行。
**DOSBox oracle 的第一件事是先解決可重現性**——最慢速度檔、固定 cycles、
靠存檔而不是操作序列定位狀態。沒解決之前，「我跑過了，畫面是這樣」不可信。

### 3.2 玩家是軍師，不是君主

戰略指令不是直接執行，而是向君主提議，君主可能拒絕。
這代表**連自己這一方的君主都有 AI**，比一般 SLG 更需要把決策邏輯解清楚。
`docs/mechanics/70-ai.md` 是機制文件裡優先度最高的一份（§5）。

### 3.3 文字是 Big5 明文，不需要字模反查

松崗版的文字直接以 Big5 存在 `TALK.DAT`、`KI.EXE`、`SINARIO.DAT`、`SAVE.DAT` 裡。
（對比大時代的故事：那款畫面上的中文是字模索引，得從點陣字模逐字反查回來。）

三個陷阱：

1. **不能用 regex 找 `\N` 變數標記。** Big5 與 Shift-JIS 的**第二個 byte 都可能是 `0x5C`**，
   regex 會把中文字切一半。必須逐字掃描。
2. **不能用 `big5`／`shift_jis`，要用 `cp950`／`cp932`。** 前者解不開 `F9D8`（「裏」），
   會讓人誤判成原版錯字。**宣告原版有錯之前，先確認自己的工具是對的。**
3. **Big5 掃描會有大量假陽性。** 圖像與地圖資料裡隨機出現的 byte 對很有機會落在
   Big5 範圍。用常用字白名單 ＋ 連續長度門檻過濾，不要只看命中率。

**松崗版是譯本不是原文**，誤譯、漏譯、為了塞版面而刪節都可能存在。
`防災值`、`上昇值` 這種詞就有日式漢語的痕跡。**判斷「這句怪」之前先找日文原版對照**，
不要憑語感改。兩版 `TALK.DAT` 同名同結構、只有語言不同，對照組是現成的。

還原出來的原文要能重新編碼寫回，**byte-for-byte round-trip 是唯一可信的驗收**。

---

## 4. Oracle 優先序

1. **實機實測**（DOSBox 跑松崗版、Neko Project／np2 跑 PC-98 版；
   固定存檔、固定操作序列、截圖）。
2. **執行檔反組譯**（`KI.EXE` 起手，兩版都已進 IDA，見 §2.1）。
3. **日文原版說明書**（38 頁，已到手，§2）。松崗中文版說明書同級，尚未取得。
4. 社群 wiki、部落格、攻略、實況影片、**2017 MOD**——最低，
   只能當「往哪裡找」的提示，不能當結論（§2 第 3 條）。

### 4.0 `[HARD]` 松崗 DOS/V 的密碼頁**不擋 oracle**

**四格留白，直接按「確定」就進開場。** `0000` 與 `1234` 也一樣，
三組受控實驗的最終截圖雜湊相同（[`docs/playtest/18`](docs/playtest/18-dosv-password-verification.md)，
2026-08-12）。**數字不是必要條件。**

所以「DOS/V 的 oracle 被防拷擋著」是**假的**。要驗 DOS/V 的東西就去跑，
不要改用 PC-98 當替代、也不要把待辦標成受阻。
（選 PC-98 仍然可以，但理由只能是「那一版的腳本比較齊」，不能是「過不去」。）

> **為什麼這一條要寫進 `CLAUDE.md`。** 它在
> `docs/playtest/01`／`13`／`17`／`18` 與 `CONTEXT.md` §0.1 都寫過了，
> **還是在四份文件裡復發**，包括解出來的同一天新寫的筆記。
> 原因是它長得像「已知的專案背景」——寫的時候不會想到要查。
> `tools/index.py` 因此加了一道檢查擋這個字樣；
> **會復發的斷言要同時有規則與檢查，只有其中一個都不夠。**

### 4.05 兩版對照是第五種 oracle

PC-98 日文原版與松崗 DOS/V 版是同一份程式的兩次編譯（§2.1）。
**「兩版相同」與「兩版不同」各自帶資訊**：

- 資料檔兩版相同 → 那是**平台無關的遊戲資料**，格式解一次通吃。
- 資料檔兩版不同 → 差異點就是**語言相關或平台相關**的部分，
  diff 直接指出欄位位置。
- 程式碼兩版相同 → 遊戲邏輯。
- 程式碼只在單邊 → 繪圖／字型／音源等平台適配層。

**⚠ 但不要反過來用。** 「PC-98 是這樣所以 DOS/V 也是」是 §7 第 9 條
明文禁止的跨檔案外推。兩版對照能**指出該去哪裡看**，
不能代替在目標版本上的驗證。

衝突時記錄在 `CONTEXT.md` 的「已被推翻的斷言」區，寫清楚誰推翻誰、憑什麼。

> **⚠ 即時制讓 oracle 1 變難。** 回合制遊戲送固定按鍵序列就能重現畫面；
> 即時制不行——同一串按鍵每次跑到的時間點都不同。
> **DOSBox oracle 的第一件事是先解決可重現性**：
> 用 `戰略速度`／`戰術速度` 的最慢檔、固定 cycles、
> 必要時靠存檔而不是操作序列來定位狀態。
> 這件事沒解決之前，任何「我跑過了，畫面是這樣」都不可信。

### 4.1 反組譯工具：`[HARD]` IDA Pro 優先

**反組譯一律先用 IDA Pro 9.4**，Ghidra 只當交叉驗證。
image 來源 `/home/anr2/ida_94_official/dist`，名稱 `ida-pro-9.4-ver2`，16-bit DOS loader。
**完整環境說明與踩過的坑見 `~/.claude/knowledge-base/retro/ida-pro-9.4.md`。**

一律經 `tools/ida.sh`，不要自己手打 `docker run`——包裝器帶了資源上限與唯讀掛載：

```sh
tools/ida.sh batch  dosv KI.EXE                       # 產 .i64 + .asm
tools/ida.sh script dosv tools/<腳本>.idc KI.EXE.i64   # 對唯讀副本跑腳本
tools/ida.sh raw    dosv idat -A "-S/work/tools/ida_func.idc sub_12E89" KI.EXE.i64
```

**`script` 與 `raw` 的差別很重要**：`idat` 一開啟 `.i64` 就會改寫它，雜湊跟著變，
而筆記要靠雜湊標明「這個結論是在哪份資料庫上驗的」。`script` 先複製再跑，
並在前後各印一次原始檔雜湊；要引用資料庫身分的分析一律用它。

**從 `KI.EXE` 起手**：它是遊戲本體、最大，而且沒有打包（字串明文可讀）。
八個執行檔都不需要先解包——沒有 PKLITE／LZEXE 指紋。

#### 現成的腳本，先找再寫

| 腳本 | 用途 |
|---|---|
| `ida_func.idc` | 單支函式：誰呼叫它、反組譯、它碰的每個資料位址還有誰讀寫 |
| `ida_xref.idc` | 查某個位址的交叉參考 |
| `ida_function_census.idc` | 全函式普查（邊界、指令數、呼叫邊、間接分派、資料參考） |
| `ida_call_trace.idc` | 全庫的「立即值 → 呼叫」軌跡，含 INT／I/O 埠與字串參考 |
| `ida_dispatch_tables.idc` | 攤開間接分派跳表的內容 |
| `ida_hot_helpers.idc` | 被大量未讀函式呼叫的共用常式 |
| `ida_dump_bytes.idc` | 指定範圍的原始 bytes |
| **`ida_scan.py`** | **IDAPython**：全庫掃「對 `[reg+disp]` 寫立即值」（解碼運算元，不比對文字）＋ 指定函式 dump。新腳本照這一支的骨架寫 |
| **`ida_tables.py`** | **IDAPython**：每支函式碰哪幾張表（掃表基址立即值）。**函式層級不是指令層級**——暫存器會跨函式帶著走 |
| **`ida_dump.py`** | **IDAPython**：通用 dumper，要看哪幾支寫在 `workplace/ida/<版本>/census/dump_list.txt`，不必為了換位址改工具 |
| **`ida_disp_users.py`** | **IDAPython**：全庫掃「運算元位移等於指定值」的指令。段內欄位（`[si+858h]`）**沒有交叉參考**，grep 反組譯會漏掉換了基址暫存器的寫法，而漏掉的通常正是寫入端 |

四支 Python 把上面的輸出變成可查的表：

| 工具 | 產出 |
|---|---|
| `re_coverage.py` | 覆蓋地圖：哪些函式從沒被文件提過（[`docs/re/21`](docs/re/21-function-census.md)）|
| `re_classify.py` | 未讀函式的角色證據，會把 TALK 索引翻成中文（[`docs/re/24`](docs/re/24-unread-function-catalogue.md)）|
| `phantom_scan.py` | 掃出文件裡指向不存在的檔案、目錄、Go 識別字與 IDA 符號 |
| `re_open_questions.py` | 缺口總表：各文件「未解」表的集中版（[`docs/re/43`](docs/re/43-open-questions.md)）。`check.sh --strict` 會擋下「提到未解卻沒有未解小節」的新文件 |

**動手讀任何一支未命名函式之前，先查
[`docs/re/24`](docs/re/24-unread-function-catalogue.md)**——
它可能已經有 TALK 訊息層級的證據，讀起來比從零開始快得多。

#### ⭐ 不要 grep `.asm`，要查 `.i64`

`.asm` 是攤平的文字，**沒有交叉參考圖**。想知道「某個全域變數是什麼」，
grep `.asm` 只能從呼叫端的參數順序反推——那是間接證據，會推錯。

四條硬知識：

1. **優先寫 IDAPython，不要寫 IDC。** `tools/ida.sh script` 看到 `.py` 就自動換到
   `ida-pro-9.4-idapython:py312-v1`。**基底 image 跑 IDAPython 是零輸出的靜默失敗**，
   所以找不到那顆 image 時要停下來建它，不要退回基底重試。
   IDC 缺一半內建函式（`get_func_qty()` 就不存在），而**缺函式在 headless 底下
   是靜默中止**——已寫進緩衝的輸出一起消失，exit code 還是 0。
2. **headless 的 `print`／`Message()` 不進 stdout**，腳本一律寫檔。
   不寫檔就等於沒跑，而且 exit code 還是 0。
   **輸出檔裡要放 probe**（函式數、輸入檔 SHA-256）——沒有它就分不出
   「沒找到」與「沒跑到」。容器的 locale 不是 UTF-8，寫檔一律明寫
   `encoding="utf-8"`，否則會在第一個中文字炸掉並留下一份看似截斷的輸出。
3. **讀寫判定用 `XrefType()`**（`dr_O=1` 取位址／`dr_W=2` 寫／`dr_R=3` 讀），
   不要比對助憶碼字串，也不要看 `print_operand(x,0)`。
   `call` 的 cref 也要濾：`fl_CN=16`／`fl_CF=17` 才是呼叫邊，`fl_F=21` 是循序落下。
4. **xref 只涵蓋直接參考。** `ptr = &x` → `es:[di] = v` 這種間接寫入抓不到，
   症狀是「讀 74 處、寫 1 處」——看到寫入數異常少，先看「取位址」那幾筆。
   同理，**「零呼叫者」不等於死碼**，可能是被跳表選中或取址後間接呼叫。

#### 判斷暫存器語意要看被呼叫的那一支

`mov cx, 0Ch` 之後接 `call sub_18853` 是 TALK 訊息索引，接 `call sub_12151` 是座標參數。
**同一個數值在不同呼叫點是不同東西**，看數值像什麼會推錯
（實例見 [`docs/re/22`](docs/re/22-strategy-command-tree.md) §3.3）。

#### 位址反查要併進 dump 流程，不要靠紀律

讀組語時手上拿到的是**位址**（`[di-6221h]`），不是函式名。缺反向索引時，
「已經有答案」會長得跟「沒有答案」一模一樣，於是硬推、推錯。
所以查詢要內建在 dump 工具裡——`ida_func.idc` 一次輸出呼叫者、反組譯與資料 xref，
就是為了讓「誰還在用它」這個最便宜的證據不必另外去找。

**待辦清單與硬規則裡不准出現不存在的工具。** 規則指向一支不存在的腳本，
照做的人會先花時間去找它，比沒寫更糟。寫進規則前先 `ls`。

#### 配套

- **每份筆記都要標「輸入檔 ＋ SHA-256 ＋ IDA 位址」**。位址一律寫 IDA database 的
  linear address；同時引用 Ghidra 位址時要明講是哪一套。
- **函式數不是二進位身分，也不是完成率**：重新分析會讓它變動（732 ↔ 739）。
  引用時連同雜湊與工具版本一起寫。
- `.i64`、`.asm`、解包後的 binary 全部 gitignore。
- **⛔ 不要在 IDA 裡把變數改名。** 命名是破壞性的，而且名字不帶推論等級——
  推錯的名字會被烙進之後每一次 dump，還會讓人不去質疑它。
  註記要非破壞性：原始位址留著，語意是附加的，一定要帶推論等級（§9）。

---

## 5. 遊戲機制文件化 `[HARD]`

**每解出一條遊戲機制，就要按屬性歸檔到 `docs/mechanics/`。**
這不是「有空再補」的附加工作，是與程式碼同等的交付物。

規格檔（`docs/spec/`）寫的是「資料長什麼樣」，反組譯筆記（`docs/re/`）
寫的是「程式碼在哪」。**兩者都不回答「這個遊戲是怎麼運作的」。**

| 檔案 | 屬性 |
|---|---|
| `00-index.md` | 索引與狀態總表 |
| `10-strategy.md` | 大地圖政略：指令清單、內政、月結、稅率、徵兵 |
| `15-realtime.md` | **即時制的時間模型**：tick、戰略速度／戰術速度、暫停點、事件觸發時機 |
| `20-military.md` | 行軍、軍團編組、六個編成位置（主將／前鋒／左翼／右翼／左備／右備） |
| `30-combat.md` | 戰場：45 度視角、兵種相剋、陣型、士氣、地形 |
| `40-economy.md` | 資金、生產力、上昇值、防災值、天災 |
| `50-diplomacy.md` | 交戰、停戰、協同進攻、外交官派遣 |
| `60-personnel.md` | 武將能力值、信賴度、任免、內政官／外交官 |
| **`70-ai.md`** | **電腦勢力的判斷邏輯**——優先度最高 |
| `80-victory.md` | 勝負判定、劇本推進、四個劇本的差異 |

> ⚠ **最常漏掉的一步：解完機制之後只寫了 `docs/re/`。**
> `docs/re/` 回答「程式碼在哪」，`docs/mechanics/` 回答
> 「這個遊戲怎麼運作」，**兩份都要**。實作完、測試完、commit 完
> 都不代表這一步做了。

### `[HARD]` 兩個非做不可的重點

**一、`15-realtime.md` 是這一款的特有難題。**
前兩個 remake 專案（冬之魔、大時代的故事）都是回合制，
現成的經驗在這裡不適用。時間模型解錯，整個引擎都要重寫。
**在寫任何規則層程式碼之前，這份要先 READY。**

**二、`70-ai.md`：電腦怎麼想，看到就記。**
玩家扮演的是軍師不是君主，代表**連自己這一方的君主都可能有 AI**——
這比一般 SLG 更需要把決策邏輯解清楚。反組譯時碰到下列東西當場記，不要之後再回頭補：

- 電腦勢力選指令的優先序、出兵目標怎麼選、兵力配置與派將規則
- 撤退／求和的門檻
- 任何帶亂數的判斷——亂數在哪裡介入、範圍多大
- 難度相關的參數（如果有）
- **君主對軍師建議的採納判斷**（如果存在）

每條機制標推論等級（§9），並寫明是在哪個執行檔的哪個位址驗的，
或是哪一次實機測試看到的。**不准為了讓文件看起來完整而編機制。**

---

## 6. 多語系：繁中母本 + 日文原版對照

- **繁中是母本**，來源是松崗版原文（§3.3），不是重寫的。
- **所有玩家看得到的文字都在語系資料檔，不進 Go 原始碼。**
- **原版的模板片段機制到 remake 就結束了。** 重寫版用具名參數的字串模板
  （`{勢力}的兵馬進攻{據點}了`），不再沿用「片段 + 控制位元組」。
  原版機制仍要完整記錄在 `docs/formats/`（那是保存的一部分），
  但不進 remake 的執行路徑。
- **排版層必須先抽離。** 原版版面是照全形字排的；
  日文對照要並排顯示、之後若要做英文版，字寬與行高全部要重算。
  排版邏輯放 `internal/ui/textdraw`，**不依賴 Ebiten**
  （Ebiten 在 init 期就要求顯示器，混進去在無頭環境下連測都測不了）。
- 校訂以 `translations/corrections.json` 為唯一真相，由 `tools/talkdat.py correct`
  套用、`tools/talkdat_selftest.py` 驗證。三國人名地名有大量既有譯法，
  **以松崗版用字為準**，不要自行「訂正」成通行寫法。

---

## 7. 從前三個 remake 專案帶過來的教訓

冬之魔、大時代的故事、大富翁2 反覆踩過的同一組錯誤，這裡先寫死：

1. **動手挖之前先查手上已有的。** 第一動作是 grep `docs/`、程式碼、攻略、譯名表，
   不是讀組合語言。但 grep 落空 ≠ 不存在。
2. **掃常數，不要掃結果。** 老軟體的數字是公式算出來的，搜不到觀察值是正常的。
3. **不要用 `grep -v` 過濾組合語言。** 為了讓輸出短而濾掉
   `mov di, ax`／`shl di, 1` 這類「看起來是樣板」的行，濾掉的正是**索引計算**，
   會把 `旗標[f(x)] = 1` 讀成 `旗標[0] = 1`。要短就用 `sed -n` 取行段。
4. **寫下一個值之前先 grep 它。** 新結論與既有常數衝突，是最便宜的錯誤偵測。
5. **對照表一律從資料取，不要憑印象編。** 三國題材尤其危險——
   「我知道虎牢關在哪」這種直覺會讓整條推論安靜地錯掉。
   城池編號、武將編號全部從 `SAVE.DAT`／`SINARIO.DAT` 取。
6. **一條規則只留一份實作。** 反組譯筆記重複只是浪費，**程式碼重複會產生行為差異**。
   在 `internal/rules/` 或 `internal/state/` 新增規則函式之前先 grep 既有實作。
7. **訂正比新結論需要更硬的證據。** 推翻舊結論前，先找出當初支持它的證據，
   解釋它為什麼不成立。
8. **先量再猜。** 「這東西被壓縮／加密了」的說法，**先量熵再說**。
9. **不要跨檔案外推。** 這裡有**八個執行檔**，比大時代的五個更容易犯。
   每個結論標清楚是在哪個 binary 上驗的。
10. **別再猜編碼，從程式碼反追。** 猜三次沒中就停手，去讀反組譯。
11. **原版的哨兵值不等於 Go 的零值。** 原版常用 `0xFF` 表示「沒有」，
    Go 的零值是 `0`，而 `0` 通常是合法編號。在**唯一入口**正規化，
    不要指望每個呼叫端都記得。
12. **從零建立的路徑會藏 bug。** 從存檔載入時每個欄位都有效，缺口不顯現。
    開新遊戲是一等驗收路徑，而且要看狀態不能只看畫面。
13. **畫面 bug 測試看不到。** 排版溢出與輸入順序只有實跑 ＋ 軌跡抓得到。
    編譯過 ≠ 視覺驗證。
14. **「AI 什麼都沒做」可能是正確的原版行為，不是實作缺口。**
    驗 AI 行動前先確認場面滿足它的前提，否則驗的是空氣。
15. **「防拷擋住 oracle」要實際撞過才算數。** 大富翁2 是真的卡在密碼畫面；
    **這一款不是**——松崗版的密碼頁空白確認就會過（§4.0）。
    差別只有實測分得出來，而「有防拷」與「過不去」是兩件事：
    **看到密碼畫面就宣告受阻，會把一整條可用的 oracle 判死。**
16. **拓樸一樣不等於路一樣。** 兩個演算法可以產生**完全相同的圖**
    而每條路都不同（行軍：253 條邊全同，109 條路徑長度差 > 2 格）。
    圖層的檢查對這種差異完全無感——**要驗路就得逐條比路**。
17. **統計特徵不是語意。** 「84 格、2×2 成組」推成橋，實際是城池圖形。
    看到一個好看的統計形狀，**先把它周圍印出來看**。
18. **錯誤的理由可以得到正確的結果，然後通過所有檢查。**
    把城池圖形當成橋加進去，連通性真的從 27 群收成 1 群——
    因為那讓路徑穿過城池，而路本來就在城裡交會。**結果對不代表理由對。**
19. **「多找到一條」比「少找到一條」更容易被當成好消息**，所以更難被質疑。
    平面陣列的 `si ± 1` 在列邊界繞行，憑空生出一條邊，我差點寫進文件當發現。
20. **快取會把「跑不起來」偽裝成「通過」。** `go test` 對沒重編的套件直接回
    `(cached)`，所以需要顯示器的 Ebiten 測試在無 X 的環境下**看起來是綠的**，
    直到快取被清才炸。**驗收環境的完整性要靠冷啟動驗，不能靠一次綠。**
21. **沉默的成功比失敗難發現。** `tools/go.sh` 沒把 `GOOS` 傳進 docker，
    三個平台建出三個一模一樣的執行檔，每次都 exit 0。
    **跨環境傳參數之後，第一件事是驗產出真的不一樣**（`file` 一下就知道）。
22. **待辦清單上的樂觀判斷，跟文件裡的斷言一樣需要被驗證。**
    worklist 寫「機制已經齊了，缺的是接起來」——量完發現圖有 108 個連通分量。
23. **函式結尾沒有 `retn` 不是分析失敗，是尾端落空。** 只 dump 一支會漏掉後半段。
24. **規則與待辦裡不准出現不存在的東西。** 指向一支不存在的腳本、目錄或檔案，
    照做的人會先花時間去找它；掛在缺口欄的幽靈待辦永遠不會完成，卻讓每一輪
    都繞著它打轉。`tools/phantom_scan.py` 會擋，但寫進去之前先 `ls` 更便宜。
25. **要判斷一支函式在做什麼，先讓遊戲自己說。** 這一款的 UI 與流程函式都走
    「設常數 → 呼叫共用常式」，而常數多半是 TALK 訊息索引，`TALK.DAT` 又已全解。
    查表拿到的中文，比從呼叫端的參數順序反推可靠得多。
26. **看到「全域函式指標」不要先判定要動態 trace，先找誰寫它。**
    寫入端如果是立即值，候選集合靜態就封得起來。靜態影像裡指向 return stub
    只說明初值是安全預設，不代表目標不可解（`docs/re/26` §3.1）。
27. **判斷暫存器語意要看被呼叫的那一支，不是看數值像什麼。**
    `mov cx, 0Ch` 後面接 `sub_18853` 是 TALK 索引，接 `sub_12151` 是座標參數。
28. **IDA 解出不合理的指令時，先懷疑自我修改碼，不要先懷疑分析壞掉。**
    這份程式碼至少兩處這樣做（`loc_1A065` 的每輪更新、`loc_10701` 把屬性
    patch 進 inline 立即值）。判準是看有沒有 `mov cs:[…], reg` 寫到自己的
    程式碼區段；有的話就拿原始 bytes 交叉解碼（`docs/re/28` §3）。
29. **「某個變數語意未解」在寫下去之前，一定要先 grep `docs/`。**
    這一類錯誤在大時代的故事專案兩天內犯了三次，三次都不是查不到，
    是**沒想到要查**。所以查詢要併進 dump 流程（§4.1），不要靠紀律。
30. **要找一份資料放在哪，先找取用它的呼叫，不要掃檔案內容。**
    「遊戲顯示中文 → 字型一定在畫中文的那支程式裡」讓字型懸案掛了五天，
    而把 `YNFONT.EXE` 整檔畫成點陣圖找字形只是把錯誤前提做得更徹底。
    答案是 `KI.EXE` 的兩條 `INT 15h`：**取用端只有幾行，資料端可以是任何檔案。**
31. **檔名連號不代表同類。** `END_S13/S14` 夾在 `END_S1..S12` 這串過場圖中間，
    被當成「中文版加的結局段」記了五天，實際是中文字型。
    **檔名是二手推論，一手資料是讀它的那段程式碼。**
32. **量測工具會把自己的輸出算成進度。** 覆蓋率靠「符號有沒有出現在 `docs/`」算，
    於是寫一份逐支登記未讀函式的清單，T4 就從三位數掉到 8、T1 衝到 90%——
    中間一支都沒多讀。**「已登記」不是「已讀懂」**，而這種偏差永遠朝
    「看起來更好」的方向。看到指標突然改善，先問改善來自哪裡（`docs/re/21` §3.1）。
33. **工具的預設值錯了，錯誤會等比例散進它產出的每一份文件。**
    `re_classify.py` 把 `sub_18810` 的訊息索引記成 `al`，全庫每個
    `mov al, 93h` 都被翻成同一則不相干的訊息，七個假陽性一路寫進目錄。
    **共用常式的參數語意要標明出處**，改動時回頭重跑用到它的所有輸出。

---

## 8. 里程碑

規格檔放 [`docs/spec/`](docs/spec/00-index.md)，索引與寫法見那一份。
**規格是 `docs/re/`（原版怎麼寫）與 `internal/`（我們寫了什麼）之間的那一層**，
每一份都要有出處、算式、推論等級、remake 實作位置與驗證方式。

三種狀態：`DRAFT`（還在寫，不能照著實作）／`READY`（可以實作）／
`CONFORMED`（已實作**而且**驗過）。**「remake 實作」欄留白就是缺口**——
差距多半不是架構錯，是解出來的規則沒接進去而測試看不出來
（`formAICorps` 的軍團上限就是這樣錯了很久）。

> **這張表只給一句話，現況以 `CONTEXT.md` §3 與 `docs/INDEX.md` 為準。**
> 待辦寫在文件裡不會自己消失——一旦把已完成的項目留在表上，
> 之後每一輪都會繞著它打轉。完成一項就當下改這一格，不要留給下一輪。

| | 里程碑 | 現況 | 內容 |
|---|---|---|---|
| M0 | 環境與偵查 | ✅ **完成** | 兩版素材入庫、兩版 `KI.EXE` 進 IDA、逐檔比對、DOSBox-X docker 化並確認可重現、**密碼頁確認可通過**（§4.0）、說明書判讀完 |
| M1 | 資料格式全解 | ✅ **完成**（`ICONGRF` 段 1 的 UI 語意除外）| `.BRG`、`*GRF.DAT`、`.MAP/.MDL/.SCH/.MCH`、`OPEN_S*`／`END_S*`、`TALK.DAT`、`SINARIO/SAVE.DAT`、音訊。Go 解碼器 ＋ round-trip 測試 |
| M2 | 文本抽取與日中對照 | ✅ **完成** | `TALK.DAT` 變數插入語意全解；兩版對照表產出；Big5 原文抽成語系檔並能 byte-for-byte 寫回 |
| M3 | 執行檔反組譯 | ✅ **靜態分析完成** | 739 支函式**全部**有 `docs/re/` 記錄（T4 ＝ 0）。**這不等於全部讀懂**——各文件的「未解」表是真正的缺口（[`docs/re/21`](docs/re/21-function-census.md)）。**引用覆蓋率要用排除目錄後的數字**（`re/21` §3.1）。兩版對照當交叉驗證（§4.0） |
| M4 | 規則規格 ＋ 機制文件 | 🔵 進行中 | 政略、行軍、戰鬥、經濟、外交、勝負。**同步產出 `docs/mechanics/`（§5）** |
| M5 | Go 引擎（規則層） | 🔵 大致完成 | 純邏輯，不認識畫面。以 tick 驅動。時鐘／月結／內政官／外交／說服／戰術／遷都／沿原版道路的行軍都在 |
| M6 | 呈現層 | 🔵 **可玩，但不是原版畫面** | Ebiten、大地圖 ＋ 45 度戰場。**UI 外框是自繪的**（原版的 `ICONGRF` 段 1 UI 語意未解），戰術版面 PARTIAL（[`docs/playtest/20`](docs/playtest/20-tactical-layout-parity.md)）。**音樂與音效完全沒有實作**——`BGM.DAT` 的聲軌事件編碼未解（[`docs/re/23`](docs/re/23-bgm-resource-format.md)）|
| M7 | 日文原版對照與校訂 | 🔵 校訂可套用 | 60 筆校訂可重跑；畫面抽樣與排版 parity 未完成 |
| M8 | 打包發行 | 🔵 進行中 | 發行閘（`denylist.py` ＋ `release.sh`）已接進 `check.sh`；缺各目標平台實機驗收 |

**里程碑不是嚴格的先後關係。** M1 的格式解析與 M5 的規則層交錯進行是對的：
規則層跑起來才會暴露格式解錯（補兵抄隊長兵種那個 bug 是畫上人物圖形才看見的）。

---

## 9. 硬規則 `[HARD]`

- **建置一律走 docker**（Go、IDA、DOSBox、Python 全部）。不裝到系統環境。
- **Python 一律 docker + uv venv**，不污染系統。
- **手打 `docker run` 一律帶 `--rm --log-opt max-size=10m --log-opt max-file=3`。**
- **原版資產唯讀。** 測試存檔一律寫到 `/tmp` 或明確的測試輸出目錄，
  **不覆蓋原版 `SAVE.DAT`／`SINARIO.DAT`**。
- **存檔寫回策略是「改寫」不是「重建」**：從原始 bytes 出發，只蓋已解欄位，
  未解區域一個 byte 都不動。驗收標準是 byte-for-byte round-trip。
- **不得 commit 或打包任何原版 `.EXE/.COM/.DAT/.MAP/.MDL/.SCH/.MCH/.BRG/.O`**，
  也不得散布倚天字型。發行前跑 deny-list 掃描。
  唯一例外：`docs/images/` 底下的 README 展示截圖（那是渲染結果不是資產本身）。
- **推論標籤要誠實**：confirmed / 強證據 / 假說 / 未知。
  不准為了讓 worklist 看起來完整而編規則。
- **每個結論要標明是在哪個執行檔上驗的**（§7 第 9 條）。
- **不碰共用的 docker 資源。** 只能清理自己這次建立的 container。**禁止**
  `docker image prune`、`docker system prune`、`docker volume prune`、
  `docker builder prune`、`docker rmi`、`docker container prune`。
- **截圖驗收要帶固定亂數種子與固定時間步。** 即時制沒有固定時間步就重跑不出結果。
- **ESC 只取消／退回上一層，F10 才離開**，離開前跳 Y／N 確認並自動存檔；
  存檔失敗就不離開。

---

## 10. 每一輪的工作紀律

### `[HARD]` 提交前跑 `tools/check.sh`

一支跑完 `go vet`、`go test`、`tools/index.py generate`。第三項會擋下：

- 文件缺狀態行或日期
- **狀態行說「未解／受阻」但內文已經有一堆 confirmed** ——
  這是本專案最常犯的錯，`docs/formats/07`、`docs/playtest/04`、
  `CONTEXT.md` 的「進行中／受阻」表都中過
- 連結壞掉、`CONTEXT.md` 提到不存在的文件

### `[HARD]` **動手之前**查這五張表（不是下結論之前）

**「還沒解」與「我不記得解過」在動手那一刻長得一模一樣**，
所以要在動手前分開，不是在收尾時。每一張各回答一個問題：

| 想知道 | 查 |
|---|---|
| 這件事解了沒？ | [`docs/INDEX.md`](docs/INDEX.md) 的斷言總表（欄位／常數 → 推論等級 → 出處）|
| 這支函式有人讀過嗎？ | [`docs/re/21`](docs/re/21-function-census.md) 的覆蓋地圖 |
| 這支函式大概在做什麼？ | [`docs/re/24`](docs/re/24-unread-function-catalogue.md) 的未讀目錄 |
| **還有什麼沒解？** | [`docs/re/43`](docs/re/43-open-questions.md) 的缺口總表 |
| **想了解某個子系統？** | [`docs/re/00-index.md`](docs/re/00-index.md) 的 RE 知識庫入口 |

`grep` 一次幾乎零成本，漏查的代價是重推一次已經有答案的東西——
或更糟，繞著一道**已經不存在的閘**打轉。


1. 動手前讀 `CONTEXT.md` worklist（狀態的單一真相來源）與相關的
   `docs/re/`、`docs/spec/`。
2. 看 `git status --short`。既有改動屬於使用者或當前任務，**不要 reset 或丟棄**。
3. 做完一項：更新 markdown → **清掉被推翻的斷言**（不是加註解，是刪掉並記進推翻清單）
   → 跑測試 → 留視覺／實跑證據 → commit + push → 更新 `CONTEXT.md` 現況。
4. 宣稱「完成」之前，對照 reference 實測。**測試綠不是完成**，測試綠只是沒退步。
5. 簡易、可平行的工作派 subagent；派工時把邊界寫進 prompt。

### 派 subagent 的邊界（主迴圈自己要寫進 prompt）

- 只能清理自己建立的 container，**禁止**任何 docker image／volume／system prune 或 rmi。
- 明列不准改的目錄（其他 repo、`~/.claude/`、`workplace/orig/`）。
- 明列不准做的收尾動作（commit、push、重編、清理）。**沒寫的等於允許**。
- 收到「我順便做了 X」時，X 一律當事故處理：先查影響範圍再談結果。

### ⛔ 有 subagent 在跑時，主迴圈**不要 `git add -A`**

「禁止 agent commit」只管住了 agent，沒管住主迴圈。
subagent 執行期間，主迴圈 commit 一律列出明確路徑，不用 `-A` 也不用 `.`。

---

## 11. 目錄結構

```
docs/re/          反組譯筆記（編號流水，一個發現一份）
docs/formats/     檔案格式規格
docs/spec/        實作規格（標 DRAFT / READY，只有 READY 能動手）
docs/mechanics/   遊戲機制，按屬性分類（§5）。15-realtime.md 與 70-ai.md 是重點
docs/playtest/    實跑驗收紀錄與截圖
docs/reference/   外部資料與說明書判讀（§2），標來源網址與抓取日期
docs/promo/       推廣片製作紀錄
docs/release/     發行驗收紀錄
docs/mobile/      Android 原型紀錄
translations/     glossary.md 譯名表、日文對照紀錄、corrections.json 校訂
internal/assets/  純解碼層，回傳 image.RGBA，不認識 Ebiten
internal/rules/   規則層（combat、tactical、diplomacy、persuasion、strategyai…），
                  不認識畫面，以 tick 驅動
internal/state/   世界狀態、事件佇列、存檔改寫
internal/savepath/ 存檔路徑安全（不依賴 Ebiten）
internal/ui/      Ebiten 呈現層（textdraw、listwin、mobile；排版刻意不依賴 Ebiten）
cmd/              wlgame（遊戲本體）、wlsim（規則層模擬）、wlandroid
mobile/、android/ gomobile 綁定與 Android 專案（只收原始檔）
packaging/        AppImage 與發行說明範本
docker/           自建映像（dosboxx＝PC-98 oracle、dosboxx-bridge＝帶原生除錯器的版本）
tools/            docker 包裝（go.sh、py.sh、ida.sh、dosbox.sh、dosboxx.sh、shot.sh、
                  dosboxx_bridge.sh ＋ dosboxx_probe.py＝動態取樣）、
                  check.sh（提交前的單一入口）、denylist.py ＋ release.sh（發行閘）、
                  index.py（文件索引）、re_coverage.py（RE 覆蓋地圖）、
                  re_open_questions.py（缺口總表）、
                  fdi_extract.py、talkdat.py、ida_*.idc
workplace/orig/dosv/      松崗 DOS/V 繁中版 69 檔（gitignore，唯讀）
workplace/orig/pc98/      PC-98 日文原版 69 檔，由 fdi_extract.py 抽出（gitignore，唯讀）
workplace/orig/pc98_fdi/  PC-98 五片磁片映像（gitignore，唯讀）
workplace/ida/{dosv,pc98}/  IDA database 與 dump（gitignore）
```

`workplace/orig/` 底下三個目錄都已 `chmod a-w`。
**目錄分成 `dosv` 與 `pc98` 是刻意的**——§7 第 9 條禁止跨檔案外推，
路徑本身就標明了這份素材是哪一版，寫筆記時不會混。

`CONTEXT.md` 的骨架：現況一覽表／文件索引／術語表／**已被推翻的斷言**／
Worklist（含「一句話現況」與「下一步」）／已建好的工具清單。

---

# github repo

https://github.com/wicanr2/wolong_cht.git

# 工作目錄

`/home/anr2/cht/dragon`
