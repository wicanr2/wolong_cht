# 臥龍傳 remake（NEO･GETEN 1994 / 松崗中文版 1995）

## ⚡ 動手前先讀 CONTEXT.md

`CONTEXT.md` 是全專案的單一入口：現況一覽、文件索引、術語表、oracle 優先序、
**已被推翻的斷言清單**、worklist。對話被壓縮或新 session 接手時先讀它。

**要查「某件事解了沒」先看 [`docs/INDEX.md`](docs/INDEX.md)**——那份是
`tools/index.py generate` 從各文件內文生出來的，含一張斷言總表
（欄位／常數 → 推論等級 → 出處）。**不要重讀整份文件，更不要重推一次。**

這份 `CLAUDE.md` 只放「不論做什麼都要遵守」的目標、原則與硬規則。

**反組譯一律先用 IDA Pro 9.4**（環境與規則見 §5.1），Ghidra 只當交叉驗證。
**中文資料要主動上網蒐集**，規則見 §2。

---

## 1. 專案目標

把 NEO･GETEN《臥龍傳－三國制霸之計》（1994 日文版 / 1995 松崗繁中版, DOS）完整逆向，
在 **Go / Ebiten** 上乾淨重寫成可跨平台執行的引擎，保存松崗繁中版的原文，
並以日文原版做逐句對照校訂。定位是文化資產保存。

四條主線：

1. **資料格式全解**：`.MAP`／`.MDL`／`.SCH`／`.MCH` 地圖族、`*GRF.DAT` 圖庫、
   `.BRG` 調色盤、`OPEN_S*`／`END_S*` 過場、`*BGM.DAT`／`SOUND.DAT` 音訊、
   `SINARIO.DAT`／`SAVE.DAT` 存檔、`TALK.DAT` 訊息表。
2. **規則還原**：`KI.EXE` 是遊戲本體，政略、戰鬥、存檔全在裡面。
   **這一款是即時制**，不是回合制——時間怎麼推進是第一等問題（§3.4）。
3. **文本保存與校訂**：松崗版原文直接以 Big5 存在檔案裡（§4），
   不必做字模反查。難的是模板片段與變數插入點。
4. **引擎重寫**：Go + Ebiten，跨平台，不依賴 DOSBox。

### 定位：還原 + 現代化外殼

核心規則一律對齊原版。外殼允許現代化——高解析呈現、視窗縮放、現代操作、存檔相容——
但**每一項改動都要在文件裡標記為 remake 差異**，且不得默默改動遊戲規則。

### 語言範圍（使用者裁定 2026-08-07）

- **繁中保存 ＋ 校訂**：以松崗版原文為母本完整保存，只修明顯的錯字／斷句／溢出。
- **日文原版對照**：以 NEO･GETEN 1994 PC-98 版做逐句對照，
  找出松崗版的漏譯、誤譯與刪節。**素材已到位**（§3.11），
  而且兩版的 `TALK.DAT` 是同一個檔的兩種語言，天然就是對照組——
  這條線不需要另外找素材，只需要解開 `TALK.DAT` 的結構。
- 英文版與簡體版**這一輪不做**。但排版層仍要抽離（§7），
  否則之後想做也做不了。

### 不做的事

- 不散布原版執行檔、資料檔、美術或音樂。公開產出只有引擎程式碼與譯文校訂紀錄。
  原版資料一律 gitignore。
- 不做玩法設計改動。這是保存專案。

---

## 2. `[HARD]` 中文資料要主動上網蒐集

**這款遊戲的中文資料散在部落格、論壇與 wiki，沒有一份完整的。
動手解格式、下規則結論之前，先把外部資料撈一輪。**
不是「有空再查」——查表幾乎零成本，漏查會讓整條推論白走。

### 蒐集規則

1. **抓回來的內容整理進 `docs/reference/`，標明來源網址與抓取日期**，
   不要散在對話裡。下一個 session 讀不到對話。
2. **社群資料的 oracle 位階最低**（§5），只能當「往哪裡找」的提示，不能當結論。
3. **⚠ 修改版／MOD 的數值一律不採信。** 本作有 2017 年的擴充 MOD
   把劇本從 4 個加到 16 個，社群寫的數值不一定出自 1995 原版。
   引用任何數值時要標明是哪一版。
4. **巴哈姆特的頁面對自動抓取會回 403**，要人工開瀏覽器看
   （這是大時代的故事專案踩過的）。
5. 找到與已知事實衝突的說法，記進 `CONTEXT.md` 的「已被推翻的斷言」，
   寫清楚誰推翻誰、憑什麼。

### 已查到的（2026-08-07 首輪網路查證）

| 項目 | 內容 | 來源 |
|---|---|---|
| 日文原名 | 臥竜伝－三国制覇の計 | 中文維基 |
| 開發／原發行 | NEO･GETEN（ホクショー） | 中文維基、粵語維基 |
| 日文版發行 | 1994 | 中文維基 |
| 繁中版 | 台灣**松崗**，1995 中文化 | 中文維基、粵語維基 |
| 平台 | PC-9801、DOS | 中文維基 |
| 類型 | **即時制**戰略 | 中文維基、粵語維基 |
| 玩家角色 | **軍師**，不是君主 | 中文維基、部落格 |
| 劇本 | 四個：呂布歸天、赤壁之戰、蜀地偏安、劉禪繼位 | 中文維基 |
| 規模 | 武將 146 人、城池 192 個 | 中文維基 |
| 地圖 | 大中小城池、戰場、關口近 300 處 | 部落格 |
| 戰鬥 | 可手動或委任；手動進 45 度視角細地圖，依兵種與陣型即時對戰，目標是打擊對方士氣 | 部落格 |
| 季節 | 大地圖隨四季變化（北方冬季雪白、南部夏季雷雨） | 部落格 |
| MOD | 2017 擴充版劇本增至 16 個，從黃巾之亂起 | 部落格 |

**⚠ 已知衝突**：部落格寫「1996 年發布」，維基寫日文 1994／中文 1995。
未裁決，先都記著。

**✅ 一手資料已印證一項**：`SINARIO.DAT` 位移 `0x40` 起是 Big5 的
「第一章」「呂布歸天」，與維基列的第一個劇本名完全對上（§3.3）。
這是「一手資料驗二手推論」的正例——其餘每一列都要照這樣驗過才算數。

### 中文資料索引（起點，不是全部）

| 資料 | 網址 |
|---|---|
| 中文維基：臥龍傳 | https://zh.wikipedia.org/zh-tw/卧龙传 |
| 粵語維基：臥龍傳 | https://zh-yue.wikipedia.org/wiki/臥龍傳 |
| 中文維基：中文DOS遊戲列表 | https://zh.wikipedia.org/zh-tw/中文DOS遊戲列表 |
| 部落格：【最愛。DOS遊戲】臥龍傳–三國制霸之計 | https://www.sharpelawtravel.com/?p=9494 |
| 中文 DOS 遊戲資料庫 | https://cdosgame.simagame.me/ |

### ✅ 已取得的一手資料（2026-08-07，使用者提供）

**日文原版說明書已經到手，這是 oracle 位階第三高的東西**（§5），
而且它是完整的機制文件，不是行銷小冊。

| 檔案 | 內容 |
|---|---|
| `Garyouden_Manual.pdf` | 日文原版說明書，**38 頁**，200 dpi 掃描 |
| `Garyouden_Xtra_Addendum.pdf` | 追補頁，4 頁 |

**⚠ 兩份都是純掃描影像，沒有文字層**（`pdftotext` 只抽得到 38 個字元）。
要 OCR 或逐頁人工判讀。**逐頁判讀的投報極高**——單一頁（PDF p.14）就解掉了
音源、畫面模式、存檔槽數與速度上限四個問題（§3.9、§3.4）。

已讀到的目次（PDF p.4，說明書第 5–11 章）：

```
5  戦略パラメータ説明   5.1 自勢力  5.2 財政  5.3 武将  5.4 拠点  5.5 軍団
6  戦術パラメータ説明   6.1 表示情報
7  外交               7.1 外交全般  7.2 外交官の任命  7.3 停戦外交  7.4 協力
8  内政               8.1 内政全般  8.2 内政官の任命
9  災害               9.1 暴風雨  9.2 火災  9.3 暴動  9.4 災害の被害
10 戦略を有利に…      停戦外交／協力外交／内政／予備兵／武術と統率／行軍の費用
11 戦術で勝利するためには… 突撃・攻撃のタイミング／陣形の有利不利／撃破状態／
                        中央突破／追撃／回り込み／おとり作戦／門を内側からあける
```

**第 9 章「災害」印證了 §3.4 從 `防災值` 推出來的天災機制**，
而且細分成暴風雨／火災／暴動三種——這在所有中文攻略裡都沒被提到。

### 還沒查的（第一輪要補）

- **松崗中文版說明書**（紙本掃描）——中文側最高價值的資料，
  可與日文版逐節對照，直接產出譯名表的第一版。
- NEO･GETEN（ホクショー）這家公司的其他作品——`YN*` 這組工具鏈可能共用（§3.11）。
- 巴哈姆特、gamebase、對岸論壇的攻略：武將能力值表、城池表、指令表。
- **即時制的時間單位**：一個「月」在真實時間是多久（日文說明書第 3.5 節已說
  速度上限受機器效能限制，見 §3.4）。
- 2017 MOD 的作者有沒有留下格式筆記——**格式筆記可以參考，數值不可採信**。

---

## 3. 已確認的事實（2026-08-07 首輪偵查）

素材：`卧龙传.zip`，解開 **72 個檔、4,407,367 bytes**。
檔案時間戳全是 2021-12-11，**是重新打包的日期，不是原版日期**。
封裝內含 `.jsdos/`（js-dos 打包），`dosbox.conf` 是 `machine=svga_s3`、`memsize=16`
——**這是打包者選的，不代表原版需要 SVGA**。

### 3.1 執行結構（已驗證）

啟動鏈：`PLAY.BAT` → `START.BAT` → **`YNVSHELL.COM`**（外殼／流程控制器）。
`YNVSHELL.COM` 內含這串 8.3 補空格的檔名表，就是它要依序叫起的程式：

```
YNFONT  .EXE   STR     .EXE   YNSOUND .COM   LOGO    .EXE
D7OPEN  .EXE   KI      .EXE   D7END   .EXE   D7OVER  .EXE
```

| 檔案 | 大小 | 已知 |
|---|---|---|
| **`KI.EXE`** | 67,099 B | **遊戲本體**。引用 16 個資料檔（見下）。**未打包**，字串明文可讀。錯誤訊息：`ERROR: Memory not enougth ! ( at least 560KB )`（原文拼字如此）、`ERROR: Mouse driver not install ?` → **需要滑鼠** |
| `YNFONT.EXE` | 60,888 B | Borland C++ 1991。中文字型驅動（§3.6） |
| `LOGO.EXE` | 57,418 B | Borland C++ 1991。開機 logo |
| `INSTALL.EXE` | 66,710 B | Borland C++ 1991。四片磁片安裝程式：`C:\DRAGON`、`copy %s*.* %s > NUL`、`Please type START to run DRAGON` |
| `STR.EXE` | 916 B | 過場播放器，引用 `END_S1x.DAT` |
| `D7OPEN.EXE` | 4,734 B | **開場播放器**，只引用 `OPENPAL.BRG`、`OPENBGM.DAT`、`OPEN_S1..S6.DAT` |
| `D7END.EXE` | 6,677 B | 結局播放器 |
| `D7OVER.EXE` | 2,747 B | 遊戲結束播放器 |
| `YNSOUND.COM` | 3,463 B | 音效驅動，**假說**：常駐 TSR |
| `SHOW.O` | 57,148 B | 被 `INSTALL.EXE` 與 `LOGO.EXE` 引用。開頭 `3c df 00 00 11 af 01 00 50 00 80 07`。**未解** |

`KI.EXE` 引用的資料檔（字串表原序）：
`GAMEPAL.BRG`、`TALK.DAT`、`ICONGRF.DAT`、`KAOGRF.DAT`、`BATTLE.SCH`、`BATTLE.DAT`、
`SINARIO.DAT`、`SAVE.DAT`、`MMAP.MAP`、`MMAP.MDL`、`MMAP.MCH`、`BATTLE.MAP`、
`BATTLE.MDL`、`SOUND.DAT`、`BGM.DAT`、`KYOGRF.DAT`、`IVENTGRF.DAT`。

> **⭐ 從 `D7OPEN.EXE` 起手解圖像格式最省事。** 它只有 4,734 B，
> 卻是一套完整的「讀調色盤 → 讀圖 → 播 BGM」解碼器。
> `KI.EXE` 的同一套邏輯埋在 67 KB 裡；小樣本先讀通，再回去對大的。
> 但**不要把 `D7OPEN` 的結論直接外推到 `KI`**（§8 第 9 條），
> 每個結論要標明是在哪個 binary 上驗的。

### 3.2 編譯器：三支確定，四支未解

`LOGO.EXE`／`YNFONT.EXE`／`INSTALL.EXE` 都有
`Borland C++ - Copyright 1991 Borland Intl.` banner。
**`KI.EXE`、`STR.EXE`、`D7OPEN/END/OVER.EXE` 沒有這個 banner**，
也沒有 PKLITE／LZEXE 指紋。可能是組語寫的、或編譯選項不同。
**未解，不要憑「同一份專案應該用同一個編譯器」外推。**

### 3.3 `SINARIO.DAT` 與 `SAVE.DAT` 同一結構（強證據）

兩者**大小完全相同：88,832 B**，`cmp -l` 只差 12,566 個 byte。
`SINARIO.DAT` 位移 `0x40` 起是 Big5 的「第一章」「呂布歸天」。

> **推論**：`SINARIO.DAT` 是初始劇本存檔，`SAVE.DAT` 是玩家存檔，同一份 layout。
> 這對 remake 很划算——**兩份檔案 diff 就能定位欄位**，
> 不必先把整個存檔結構讀懂。

`SAVE.DAT` 裡已經看得到 Big5 據點名：
氾水關（`0x1022`）、函谷關（`0x1142`）、虎牢關（`0x11c2`）、五丈原（`0x1222`）、
陽平關（`0x1482`）、白水關（`0x1502`）、葭萌關（`0x15a2`）、濡須口（`0x18e2`）。
間距不固定（`0x80`／`0x60`／`0xa0`／`0x260` 都出現過）——
**不要急著斷定記錄長度**，先把 192 個城池全掃出來再看分佈。

### 3.4 即時制：`KI.EXE` 裡的 Big5 UI 詞已經洩漏了系統輪廓

`KI.EXE` 位移 `0x18xx`–`0x1axx` 是一段 Big5 明文的介面詞表，照原序：

```
君主 首都 軍師 信賴度 資金 預備兵 資金 收入 支出 今月底 次月 稅率 徵兵數
資料儲存 畫面模式 戰略速度 戰術速度 遊戲結束
城兵數 生產力 上昇值 防災值
將軍 君主 首都 總兵力 士氣值
主將 前鋒 左翼 右翼 左備 右備 預備兵數
君主 軍師 武將數 據點數 自定 確定 軍師名 重來 繼續 上一頁 下一頁
```

三條**強證據**（還沒到 confirmed，要在反組譯裡驗）：

1. **`戰略速度` 與 `戰術速度` 是兩個獨立設定** → 大地圖與戰場各有各的時間流速，
   佐證維基說的即時制。**remake 的規則層要以 tick 為單位設計，不是回合。**
2. **部隊編成有六個位置**：主將／前鋒／左翼／右翼／左備／右備，
   外加預備兵數。這與部落格說的「依陣型對戰」對得上。
3. **經濟欄位**：資金、收入、支出、稅率、徵兵數、生產力、上昇值、防災值
   → 有內政循環與月結（`今月底`／`次月`）。

`防災值` 這個詞值得單獨追——**代表有天災事件**，這在攻略裡沒被提到。

#### 日文說明書已 confirmed 的四項（第 3.5 節「システムウインドウ」，PDF p.14）

| 項目 | 原文 | 定案 |
|---|---|---|
| データセーブ | 「4カ所のどこにでもセーブが可能」 | **存檔槽是 4 個** |
| 画面モード | 「アナログカラーと8階調液晶とを切り替えます」 | PC-98 的類比彩色 ↔ **8 階調液晶**（筆電）。`KI.EXE` 的「畫面模式」就是這個 |
| サウンド | 「BGMのストップとFM音源とSSG音源のバランスを変更します」 | **音源是 FM ＋ SSG**（PC-98 的 YM2203／OPN），見 §3.9 |
| 戦略速度 | 「選択されている速度が機械の速度を超えるといくら速い速度を設定してもそれ以上速くなりません」 | **原版沒有固定 tick rate**，速度上限跟著機器跑 |

> **⭐ 最後一項對 remake 是硬約束。** 原版的「即時」是「跑多快算多快」，
> 不是每秒固定 N tick。直接照抄會得到一個在現代機器上快到不能玩的遊戲。
> remake 必須自己定義一個固定時間基準，**並把它明確標記為 remake 差異**，
> 同時記錄原版在什麼機器上大約是什麼速度（`docs/mechanics/15-realtime.md`）。

### 3.5 `TALK.DAT` 訊息表：**已全解，READY**

完整規格見 **[`docs/formats/01-talk-dat.md`](docs/formats/01-talk-dat.md)**。
兩版都 byte-for-byte round-trip 通過（`tools/talkdat.py`）。

```
0x000–0x7FF  1024 筆 uint16 LE 偏移表（[1022] 是哨兵，[1023] 未使用）
0x800–       1,022 則訊息，每則是若干個 NUL 結尾的行
變數插入     ASCII `\1` `\2` `\3` `\4` `\6` `\7` 六種（沒有 \5）
編碼         dosv 用 cp950、pc98 用 cp932
```

**兩版索引一一對應**，同一個索引就是同一則訊息的兩種語言 →
日中對照表已經產出，第一批發現見
[`docs/reference/02-jp-cht-diff.md`](docs/reference/02-jp-cht-diff.md)。

#### 三個踩過的坑，寫死在這裡

1. **不能用 regex 找 `\N`。** Big5 與 Shift-JIS 的**第二個 byte 都可能是 `0x5C`**，
   regex 會把中文字切一半。必須逐字掃描。
2. **不能用 `big5`／`shift_jis`，要用 `cp950`／`cp932`。**
   前者解不開 `F9D8`（「裏」），會讓人誤判成原版錯字。
   **宣告原版有錯之前，先確認自己的工具是對的。**
3. **訊息結尾的空行是資料。** 有的訊息沒有、有的有兩個。
   把它「正規化」掉就寫不回去了。

> **⚠ 已推翻的斷言**：初版寫「訊息是模板片段，中間夾**控制位元組**做變數插入」。
> **機制錯了** —— 訊息內除了 `0x00` 沒有任何其他控制位元組，
> 變數用的是 ASCII 反斜線轉義。當初是拿「Big5 掃描掃出一堆短片段」
> 反推機制，那是間接證據。結論（有變數插入）碰巧對，推導過程是錯的。

### 3.6 中文字型（**已結案**：不用自備，`YNFONT.EXE` 自帶）

> **2026-08-07 結案**（`docs/playtest/01`）：DOS/V 版實跑證明字型內建，
> 玩家不需要倚天中文系統。以下是結案前的推理過程，留作紀錄。

`INSTALL.EXE`、`LOGO.EXE`、`YNFONT.EXE` 三支都引用這三個檔名：

```
STDFONT.24   ASCFONT.24   ASCFONT.15
```

**但這三個檔不在封裝裡。** 這是**倚天中文系統的檔名族**
（`~/cht/etan_font/` 有現成素材，`STDFONT.15`／`STDFONT.24`）。

**PC-98 版的對照讓答案清楚了一半**（§3.11）：

| | PC-98 日文版 | 松崗 DOS/V 版 |
|---|---|---|
| 字型程式 | `YNFONT.COM` **843 B** | `YNFONT.EXE` **60,888 B** |
| 字型資料 | `FONTGRF.DAT` 1,216 B | 無，改引用 `STDFONT.24` 等三檔 |

PC-98 有內建字型 ROM，所以只需要 843 B 的驅動加 1,216 B 的特殊字圖。
**移植到 DOS/V 沒有字型 ROM，才需要外部中文系統**——
`YNFONT.EXE` 大了 72 倍就是在補這一塊。

**✅ 已結案（2026-08-07 DOSBox 實跑，`docs/playtest/01`）**：
那三個檔**不用玩家自備**。它們不在封裝裡，遊戲照樣顯示繁體中文
→ `YNFONT.EXE` 自帶字型，那三個檔名是 fallback 路徑。

> ⚠ **2026-08-08 補充：「YNFONT.EXE 自帶字型」找不到證據。**
> 把整個檔案當 1bpp 點陣圖畫出來沒有任何字形，熵只有 3.05（沒加殼、
> 也不是壓縮資料），而全部 192 個據點名加武將名就要 377 個相異字、
> 遊戲文字更多——60,888 B 放不下。
> **這條結論標回「未解」**，但**不擋 remake**：
> remake 走自己的字型路徑（`internal/assets/cjk`，`-font` 指定），
> 已經實作完成並在畫面上驗過。原版怎麼做到的留待日後釐清。

> 不論答案是哪個，**倚天字型都不隨本專案散布**（§10）。
> remake 走自己的字型路徑，執行時由玩家指向自備的字庫，
> 做法與冬之魔專案的 `-eten` 參數相同。

### 3.7 圖像與調色盤

`.BRG` 調色盤四個，都是 3 byte 一組：

| 檔案 | 大小 | 色數 |
|---|---|---|
| `GAMEPAL.BRG` | 384 | 128 |
| `OPENPAL.BRG` | 288 | 96 |
| `ENDPAL.BRG` | 576 | 192 |
| `OVERPAL.BRG` | 48 | 16 |

觀察到的通道值域是 `0x00`–`0x0f`（4 bit）。
`OVERPAL` 的前 16 byte 與 `GAMEPAL` 相同、`OPENPAL` 的前 16 byte 與 `ENDPAL` 相同
→ **強證據**：這些調色盤共用同一組色序，短的是長的前綴。

**假說**：副檔名 `BRG` 就是通道順序 **B, R, G**。
拿一張已解的圖用兩種順序畫出來，跟 DOSBox 截圖比對就能定案——**先驗再用**。

圖庫四個：

| 檔案 | 大小 | 推測 |
|---|---|---|
| `KAOGRF.DAT` | 307,200 | 頭像（日文「顔」）。**假說**：307,200 ÷ 2,048 = **150 張 64×64 4bpp**，而武將 146 人 + 4 → 對得起來，但**這是算術巧合等級的證據，要驗** |
| `IVENTGRF.DAT` | 76,032 | 事件圖（`IVENT` 是 `EVENT` 的拼法） |
| `KYOGRF.DAT` | 69,120 | 未解 |
| `ICONGRF.DAT` | 47,776 | 圖示 |

### 3.8 地圖族：`.MAP` / `.MDL` / `.SCH` / `.MCH`

```
BATTLE.MAP  877,056    BATTLE.MDL  194,560    BATTLE.SCH  115,200   BATTLE.DAT  8,192
MMAP.MAP     80,716    MMAP.MDL     32,768    MMAP.MCH     43,058
PASS.MAP     32,594    PASS.SCH     14,281
MOUSE.MCH       588    MOUSE.SCH       864
INSTALL.MAP  30,831    INSTALL.SCH  11,041
```

`MMAP` = 大地圖（main map）、`BATTLE` = 戰場、`PASS` = 關隘、`MOUSE` = 游標。
**同一組副檔名跨四個用途重複出現 → 這是四種通用容器格式，不是四個一次性檔案。**
解通一個就解通全部，優先度很高。

> **⚠ `file(1)` 對 `INSTALL.MAP`／`INSTALL.SCH`／`PASS.*`／`MOUSE.*` 報
> 「Atari DEGAS Elite bitmap 640×400」——那是 magic 誤判，不是線索。**
> DEGAS 的 magic 只是開頭一個 0/1/2 的 word，這批檔案剛好也以小數字開頭。
> 別拿它當「640×400」的證據。

### 3.9 音訊

`BGM.DAT` 20,826、`OPENBGM.DAT` 3,424、`ENDBGM.DAT` 2,218、`OVERBGM.DAT` 684、
`SOUND.DAT` 304。加上 `YNSOUND.COM`（3,463 B，疑似 TSR）。

**PC-98 側的音源已由日文說明書定案：FM 音源 ＋ SSG 音源**
（第 3.5 節「サウンド」，§2）。那就是 PC-98 標準的 **YM2203／OPN**——
FM 3 聲 ＋ SSG 3 聲，埠 `0x188`／`0x18A`。

**DOS/V 側未解。** `dosbox.conf` 開了 `sb16` 與 `oplmode=auto`，
但那是打包者的設定，不是原版需求。
候選：AdLib／OPL2（`0x388`／`0x389`）、SB（`0x220` 族）、PC speaker。

> **⚠ 這是「不要跨檔案外推」（§8 第 9 條）的教科書案例。**
> 兩版的 `YNSOUND.COM` 大小不同（3,463 B ↔ 4,553 B），
> 五個 `*BGM.DAT`／`SOUND.DAT` **每一個都大小不同**——
> 音訊是兩版差異最大的一塊。**PC-98 是 OPN 不代表 DOS/V 也是。**
> DOS/V 的音源要自己從 `KI.EXE`／`YNSOUND.COM` 找 I/O 埠寫入來定案。
>
> 但 OPN 與 OPL 同屬 Yamaha FM 家族，**樂曲資料很可能同源**——
> 先比對兩版 `BGM.DAT`（20,790 ↔ 20,826，只差 36 B）的結構，
> 這是最省力的切入點。

### 3.10 PC-98 日文原版：`[HARD]` 這是本專案最重要的一項資產

使用者提供了 **PC-98 版的五片磁片映像**（`.fdi`，各 1,265,664 B
＝ 4,096 B 檔頭 ＋ 1232 KB 2HD 裸映像）。已全部抽檔完成，工具是
`tools/fdi_extract.py`（只用標準函式庫，不裝套件）。

磁片是 **FAT12，1024 byte/sector**，只有開機磁區的 OEM 欄位被換掉：
A／B 片是 `FGDOS1.0`（自製載入器，boot sector 內有
`* FGDOS Ver 1.00 Loading module version 1.2 * Copyright(C)1993`），
C／D 片是 `YN  1.00`，E 片是 `NEC  5.0`（原廠 MS-DOS 5.0 系統片）。

| 片 | 內容 |
|---|---|
| A | 遊戲本體：`KI.EXE`、`MMAP.*`、`KAOGRF`／`ICONGRF`、`SINARIO`／`SAVE`、`TALK.DAT`、`YN*` 工具鏈 |
| B | 開場：`D7OPEN.EXE`、`OPEN_S1..S6.DAT`、`ROGO.EXE`／`ROGO0`／`ROGO1` |
| C | 戰場與音樂：`BATTLE.MAP/MDL`、`BGM.DAT`、`SOUND.DAT`、`IVENTGRF`、`KYOGRF` |
| D | 結局：`D7END.EXE`、`END_S1..S12.DAT`、`HDINST.EXE`（硬碟安裝） |
| E | MS-DOS 5.0 系統片 |

#### ⭐ 23 個檔在兩版之間 byte-for-byte 完全相同

```
BATTLE.MAP  BATTLE.MDL  BATTLE.SCH        ← 戰場三件全套
MMAP.MAP    MMAP.MDL    MMAP.MCH          ← 大地圖三件全套
KAOGRF.DAT  KYOGRF.DAT  ICONGRF.DAT  IVENTGRF.DAT   ← 四個圖庫全部
GAMEPAL.BRG  OPENPAL.BRG  ENDPAL.BRG  OVERPAL.BRG   ← 四個調色盤全部
OPEN_S1..S6.DAT                           ← 整段開場動畫
END_S1.DAT  END_S2.DAT  END_S12.DAT
```

**推論（強證據）**：松崗版是移植不是重寫，資料檔原封不動搬過去。
**這對專案是三個層級的好消息**：

1. **圖像、地圖、調色盤格式解一次通吃兩版。** 而且 PC-98 是 640×400
   的固定規格，比 DOS/V 那邊少一層猜測——**格式反推從 PC-98 側入手更省事**。
2. **哪些素材裡燒了文字，diff 直接告訴你。** `END_S3`–`END_S11` 兩版大小全不同、
   `END_S1`／`S2`／`S12` 完全相同 → **那九張結局過場的圖裡燒了日文，
   中文版重繪過**；開場 `OPEN_S1..S6` 六張全相同 →
   **開場沒有燒字，文字是疊繪上去的**。這是一個免費的「燒字偵測器」，
   省掉逐張目視檢查。
3. **松崗版多了 `END_S13`／`S14`／`S15`**（PC-98 只到 `S12`）→
   中文版動過結局段。**這是 remake 必須查清楚的差異**，不能默默選一邊。

#### 大小相同但內容不同的三個：最好的 diff 對象

`BATTLE.DAT`（8,192）、`SAVE.DAT`（88,832）、`SINARIO.DAT`（88,832）。
**結構相同、內容不同 → 兩版逐 byte diff 就能定位「哪些欄位是字串」**，
不必先把整份結構讀懂。`SINARIO.DAT` 兩版 diff 尤其划算：
同一個劇本、同一份初始狀態，差的只有語言。

#### 只在單邊存在的檔

| 只在 PC-98 | 意義 |
|---|---|
| `FGDOS.SYS`／`STARTUP.SYS`／`CCP.EXE`／`STARTUP.CMD` | 自製 DOS。**防拷的候選位置，M0 要查**（§3.11） |
| `FONTGRF.DAT`（1,216 B）／`YNFONT.COM`（843 B） | 字型（§3.6 已解） |
| `YNMOUSE.COM`／`YNSHELL.COM` | `YN*` 工具鏈；DOS/V 版是 `YNVSHELL.COM` |
| `ROGO.EXE`／`ROGO0.DAT`／`ROGO1.DAT` | 開機 logo（「ロゴ」的羅馬字）；DOS/V 版換成 `LOGO.EXE` |
| `HDINST.EXE` | 硬碟安裝 |

| 只在松崗 DOS/V | 意義 |
|---|---|
| `END_S13/S14/S15.DAT` | 中文版加的結局段 |
| `PASS.MAP`／`PASS.SCH`／`MOUSE.MCH`／`MOUSE.SCH` | **PC-98 沒有**。關隘與游標，移植時新增或改名 |
| `INSTALL.EXE`／`INSTALL.MAP`／`INSTALL.SCH`／`SHOW.O`／`STR.EXE`／`LOGO.EXE`／`YNFONT.EXE` | 移植方（松崗）自己寫的安裝與外殼 |
| `DISK1`–`DISK4`／`LABEL.$$$`／`PLAY.BAT` | 磁片標記 |

> **⚠ `YN` 是開發方的工具鏈前綴**（`YNSHELL`／`YNFONT`／`YNSOUND`／`YNMOUSE`），
> 兩版都有。NEO･GETEN 的其他作品可能共用同一套——查到別的作品時，
> 這組檔名是最好的比對特徵。

#### 反組譯：兩版都已進 IDA

```
workplace/ida/dosv/KI.EXE.i64   松崗繁中版  67,099 B  732 函式
                                sha256 fffeba98…3868
workplace/ida/pc98/KI.EXE.i64   PC-98 日文版 65,823 B  725 函式
                                sha256 061917f9…81e8
```

兩版載入位址都是 `10000h–2041Bh`。函式數只差 7 →
**同一份原始碼的兩次編譯**（強證據）。
**兩版對照本身就是一種 oracle**：某段程式碼在兩版都存在且相同，
代表那是遊戲邏輯；只在單邊存在的，多半是平台相關的繪圖／字型／音源。

### 3.11 未知（誠實標記，不要靜悄悄變成前提）

- `SHOW.O` 是什麼（只在松崗版）。
- `TALK.DAT` 前 2,048 B 的結構。
- `.MAP`／`.MDL`／`.SCH`／`.MCH` 四種格式各自的語意。
- `*GRF.DAT` 的圖像編碼（是否壓縮——**先量熵**）。
- DOS/V 側的音源（PC-98 側已定案為 OPN，見 §3.9）。
- 那三個字型檔要不要玩家自備（§3.6）。
- 松崗版多出來的 `END_S13/S14/S15` 是什麼。
- `PASS.*` 為什麼只在松崗版有。
- ~~有沒有防拷~~ **✅ 有，而且擋住了 DOS/V 的 oracle**（`docs/playtest/01`）：
  「密碼輸入：第 NN 頁」查說明書式，四個中文字，資料在 `PASS.MAP`／`PASS.SCH`，
  執行者是 `YNFONT.EXE`。**但 PC-98 版沒有這一關** ——
  規則與資料結構的 oracle 改跑 PC-98 就繞過去了。
- `PASS.*`／`INSTALL.*` 的壓縮演算法（`docs/formats/04` §3）。
- `MMAP.*`／`BATTLE.*` 的展開常式 `sub_1E48A`。

---

## 4. 文本：這一款的好消息與陷阱

**好消息**：松崗版的文字直接以 Big5 存在 `TALK.DAT`、`KI.EXE`、`SINARIO.DAT`、
`SAVE.DAT` 裡，**不需要字模反查**。
（對比大時代的故事：那款畫面上的中文是字模索引，原文得從點陣字模逐字反查回來。
這一款省掉了整條 M1。）

**三個陷阱**：

1. **模板片段**（§3.5）。片段之間的控制位元組決定插入什麼，
   語意沒解出來之前不要動任何一段文字。
2. **松崗版是譯本，不是原文。** 誤譯、漏譯、為了塞進版面而刪節，都可能存在。
   `防災值`、`上昇值` 這種詞就有日式漢語的痕跡。
   **判斷「這句怪」之前先找日文原版對照**，不要憑語感改。

   > **⭐ 對照組是現成的**：`TALK.DAT` 兩版同名同結構、只有語言不同
   > （PC-98 日文 45,718 B ↔ 松崗中文 34,182 B）。
   > 解開這一個檔的索引結構，日中對照表就自動產生了——
   > 不需要另外做句子對齊。**這是整個 M2 最高投報的一項。**
   >
   > 日文版大了 11 KB 值得注意：Shift-JIS 與 Big5 都是雙位元組，
   > 差額多半來自**中文譯得比較短**或**有內容被刪**。
   > 差在哪裡是 M7 校訂的第一個問題。
3. **Big5 掃描會有大量假陽性。** 圖像與地圖資料裡隨機出現的 byte 對
   有很高機率落在 Big5 範圍——`TALK.DAT` 整檔的「Big5 命中率」是 0.70，
   但真正可讀的只有 `0x800` 之後那一段。
   **用常用字白名單 + 連續長度門檻過濾**，不要只看命中率。

還原出來的原文要能重新編碼寫回，**byte-for-byte round-trip 是唯一可信的驗收**。

---

## 5. Oracle 優先序

1. **實機實測**（DOSBox 跑松崗版、Neko Project／np2 跑 PC-98 版；
   固定存檔、固定操作序列、截圖）。
2. **執行檔反組譯**（`KI.EXE` 起手，兩版都已進 IDA，見 §3.10）。
3. **日文原版說明書**（38 頁，已到手，§2）。松崗中文版說明書同級，尚未取得。
4. 社群 wiki、部落格、攻略、實況影片、**2017 MOD**——最低，
   只能當「往哪裡找」的提示，不能當結論（§2 第 3 條）。

### 5.0 兩版對照是第五種 oracle

PC-98 日文原版與松崗 DOS/V 版是同一份程式的兩次編譯（§3.10）。
**「兩版相同」與「兩版不同」各自帶資訊**：

- 資料檔兩版相同 → 那是**平台無關的遊戲資料**，格式解一次通吃。
- 資料檔兩版不同 → 差異點就是**語言相關或平台相關**的部分，
  diff 直接指出欄位位置。
- 程式碼兩版相同 → 遊戲邏輯。
- 程式碼只在單邊 → 繪圖／字型／音源等平台適配層。

**⚠ 但不要反過來用。** 「PC-98 是這樣所以 DOS/V 也是」是 §8 第 9 條
明文禁止的跨檔案外推。兩版對照能**指出該去哪裡看**，
不能代替在目標版本上的驗證。

衝突時記錄在 `CONTEXT.md` 的「已被推翻的斷言」區，寫清楚誰推翻誰、憑什麼。

> **⚠ 即時制讓 oracle 1 變難。** 回合制遊戲送固定按鍵序列就能重現畫面；
> 即時制不行——同一串按鍵每次跑到的時間點都不同。
> **DOSBox oracle 的第一件事是先解決可重現性**：
> 用 `戰略速度`／`戰術速度` 的最慢檔、固定 cycles、
> 必要時靠存檔而不是操作序列來定位狀態。
> 這件事沒解決之前，任何「我跑過了，畫面是這樣」都不可信。

### 5.1 反組譯工具：`[HARD]` IDA Pro 優先

**反組譯一律先用 IDA Pro 9.4**，Ghidra 只當交叉驗證。

```sh
mkdir -p workplace/ida
cp --reflink=auto workplace/orig/KI.EXE workplace/ida/KI.EXE
sha256sum workplace/ida/KI.EXE

# headless 產 .i64 + .asm
docker run --rm -v "$PWD/workplace/ida:/work" -w /work \
  ida-pro-9.4-ver2 idat -A -B KI.EXE

# IDC 稽核腳本
docker run --rm -v "$PWD/workplace/ida:/work" -v "$PWD/tools:/work/tools" -w /work \
  ida-pro-9.4-ver2 idat -A -S/work/tools/<script>.idc KI.EXE.i64
```

image 來源 **`/home/anr2/ida_94_official/dist`**，image 名稱 **`ida-pro-9.4-ver2`**，
16-bit DOS loader。**完整環境說明與踩過的坑見
`~/.claude/knowledge-base/retro/ida-pro-9.4.md`**（已掛進 `rules/00-rules-index.md`
的觸發表）。

**從 `KI.EXE` 起手**：它是遊戲本體、最大、而且**沒有打包**（字串明文可讀）。
八個執行檔都不需要先解包——沒有 PKLITE／LZEXE 指紋。

#### ⭐ 不要 grep `.asm`，要查 `.i64`

`.asm` 是攤平的文字，**沒有交叉參考圖**。想知道「某個全域變數是什麼」，
grep `.asm` 只能從呼叫端的參數順序反推——那是間接證據，會推錯。
寫一支 `tools/ida_xref.idc` 直接查 IDA 的 xref 圖。

四條硬知識（大時代的故事專案實測）：

1. **IDAPython 在這個 image 跑不起來，要寫 IDC。**
2. **headless 的 `print`／`Message()` 不進 stdout**，腳本一律 `fopen` 寫檔。
   不寫檔就等於沒跑，而且 exit code 還是 0。
3. **讀寫判定用 `XrefType()`**（`dr_W=2`／`dr_R=3`／`dr_O=1`），
   不要比對助憶碼字串，也不要看 `print_operand(x,0)`。
4. **xref 只涵蓋直接參考。** `ptr = &x` → `es:[di] = v` 這種間接寫入抓不到，
   症狀是「讀 74 處、寫 1 處」——看到寫入數異常少，先看「取位址」那幾筆。

配套：

- **每份筆記都要標「輸入檔 + SHA-256 + IDA 位址」**。位址一律寫 IDA database 的
  linear address；同時引用 Ghidra 位址時要明講是哪一套。
- `.i64`、`.asm`、解包後的 binary 全部 gitignore。
- **⛔ 不要在 IDA 裡把變數改名。** 命名是破壞性的，而且名字不帶推論等級——
  推錯的名字會被烙進之後每一次 dump，還會讓人不去質疑它。
  註記要非破壞性：原始位址留著，語意是附加的，一定要帶推論等級（§10）。

#### 位址反查要併進 dump 流程

讀組語時手上拿到的是**位址**（`[di-6221h]`），不是函式名。
大時代的故事專案的教訓是：答案早就寫在自己的筆記裡，缺的是**反向索引**，
於是「已經有答案」變成「以為沒有答案」，只好硬推，推錯兩次。

**這個專案從第一輪就建 `tools/addr.py`**（手維護 `BASES`／`FIELDS`／`GLOBALS` 三張表，
每一列標推論等級）與 `tools/dump_func.py`（dump 時自動把位址翻成語意、
把範圍內每個 `word_*`／`byte_*`／`sub_*` 拿去 grep `docs/**/*.md`）。
**不要用 `awk`／`sed` 直接 dump 組語。**

> 這條方法論在大時代的故事專案仍在驗證期。這裡是第二個案例——
> 有效無效都要回報，別默默當成已驗證的東西用。

---

## 6. 遊戲機制文件化 `[HARD]`

**每解出一條遊戲機制，就要按屬性歸檔到 `docs/mechanics/`。**
這不是「有空再補」的附加工作，是與程式碼同等的交付物。

規格檔（`docs/spec/`）寫的是「資料長什麼樣」，反組譯筆記（`docs/re/`）
寫的是「程式碼在哪」。**兩者都不回答「這個遊戲是怎麼運作的」。**

| 檔案 | 屬性 |
|---|---|
| `00-index.md` | 索引與狀態總表 |
| `10-strategy.md` | 大地圖政略：指令清單、內政、月結、稅率、徵兵 |
| `15-realtime.md` | **即時制的時間模型**：tick、戰略速度／戰術速度、暫停點、事件觸發時機 |
| `20-military.md` | 行軍、軍團編組、六個編成位置（主將／前鋒／左翼／右翼／左備／右備） |
| `30-combat.md` | 戰場：45 度視角、兵種相剋、陣型、士氣、地形 |
| `40-economy.md` | 資金、生產力、上昇值、防災值、天災 |
| `50-diplomacy.md` | 交戰、停戰、協同進攻、外交官派遣 |
| `60-personnel.md` | 武將能力值、信賴度、任免、內政官／外交官 |
| **`70-ai.md`** | **電腦勢力的判斷邏輯**——優先度最高 |
| `80-victory.md` | 勝負判定、劇本推進、四個劇本的差異 |

### `[HARD]` 兩個非做不可的重點

**一、`15-realtime.md` 是這一款的特有難題。**
前兩個 remake 專案（冬之魔、大時代的故事）都是回合制，
現成的經驗在這裡不適用。時間模型解錯，整個引擎都要重寫。
**在寫任何規則層程式碼之前，這份要先 READY。**

**二、`70-ai.md`：電腦怎麼想，看到就記。**
玩家扮演的是軍師不是君主，代表**連自己這一方的君主都可能有 AI**——
這比一般 SLG 更需要把決策邏輯解清楚。反組譯時碰到下列東西當場記，不要之後再回頭補：

- 電腦勢力選指令的優先序、出兵目標怎麼選、兵力配置與派將規則
- 撤退／求和的門檻
- 任何帶亂數的判斷——亂數在哪裡介入、範圍多大
- 難度相關的參數（如果有）
- **君主對軍師建議的採納判斷**（如果存在）

每條機制標推論等級（§10），並寫明是在哪個執行檔的哪個位址驗的，
或是哪一次實機測試看到的。**不准為了讓文件看起來完整而編機制。**

---

## 7. 多語系：繁中母本 + 日文原版對照

- **繁中是母本**，來源是松崗版原文（§4），不是重寫的。
- **所有玩家看得到的文字都在語系資料檔，不進 Go 原始碼。**
- **原版的模板片段機制到 remake 就結束了。** 重寫版用具名參數的字串模板
  （`{勢力}的兵馬進攻{據點}了`），不再沿用「片段 + 控制位元組」。
  原版機制仍要完整記錄在 `docs/formats/`（那是保存的一部分），
  但不進 remake 的執行路徑。
- **排版層必須先抽離。** 原版版面是照全形字排的；
  日文對照要並排顯示、之後若要做英文版，字寬與行高全部要重算。
  排版邏輯放 `internal/ui/textlayout`，**不依賴 Ebiten**
  （Ebiten 在 init 期就要求顯示器，混進去在無頭環境下連測都測不了）。
- 譯名表 `translations/glossary.md` 是唯一真相，品質閘要檢查譯名漂移。
  三國人名地名有大量既有譯法，**以松崗版用字為準**，不要自行「訂正」成通行寫法。

---

## 8. 從前三個 remake 專案帶過來的教訓

冬之魔、大時代的故事、大富翁2 反覆踩過的同一組錯誤，這裡先寫死：

1. **動手挖之前先查手上已有的。** 第一動作是 grep `docs/`、程式碼、攻略、譯名表，
   不是讀組合語言。但 grep 落空 ≠ 不存在。
2. **掃常數，不要掃結果。** 老軟體的數字是公式算出來的，搜不到觀察值是正常的。
3. **不要用 `grep -v` 過濾組合語言。** 為了讓輸出短而濾掉
   `mov di, ax`／`shl di, 1` 這類「看起來是樣板」的行，濾掉的正是**索引計算**，
   會把 `旗標[f(x)] = 1` 讀成 `旗標[0] = 1`。要短就用 `sed -n` 取行段。
4. **寫下一個值之前先 grep 它。** 新結論與既有常數衝突，是最便宜的錯誤偵測。
5. **對照表一律從資料取，不要憑印象編。** 三國題材尤其危險——
   「我知道虎牢關在哪」這種直覺會讓整條推論安靜地錯掉。
   城池編號、武將編號全部從 `SAVE.DAT`／`SINARIO.DAT` 取。
6. **一條規則只留一份實作。** 反組譯筆記重複只是浪費，**程式碼重複會產生行為差異**。
   在 `internal/game/` 新增規則函式之前先 grep 既有實作。
7. **訂正比新結論需要更硬的證據。** 推翻舊結論前，先找出當初支持它的證據，
   解釋它為什麼不成立。
8. **先量再猜。** 「這東西被壓縮／加密了」的說法，**先量熵再說**。
9. **不要跨檔案外推。** 這裡有**八個執行檔**，比大時代的五個更容易犯。
   每個結論標清楚是在哪個 binary 上驗的。
10. **別再猜編碼，從程式碼反追。** 猜三次沒中就停手，去讀反組譯。
11. **原版的哨兵值不等於 Go 的零值。** 原版常用 `0xFF` 表示「沒有」，
    Go 的零值是 `0`，而 `0` 通常是合法編號。在**唯一入口**正規化，
    不要指望每個呼叫端都記得。
12. **從零建立的路徑會藏 bug。** 從存檔載入時每個欄位都有效，缺口不顯現。
    開新遊戲是一等驗收路徑，而且要看狀態不能只看畫面。
13. **畫面 bug 測試看不到。** 排版溢出與輸入順序只有實跑 ＋ 軌跡抓得到。
    編譯過 ≠ 視覺驗證。
14. **「AI 什麼都沒做」可能是正確的原版行為，不是實作缺口。**
    驗 AI 行動前先確認場面滿足它的前提，否則驗的是空氣。
15. **防拷／磁片檢查會擋住 oracle。** 大富翁2 卡在密碼畫面，DOSBox 實測整條線停擺。
    **提早排除，不要等到 M4**（§3.10）。
16. **「某個變數語意未解」在寫下去之前，一定要先 grep `docs/`。**
    這一類錯誤在大時代的故事專案兩天內犯了三次，三次都不是查不到，
    是**沒想到要查**。所以查詢要併進 dump 流程（§5.1），不要靠紀律。

---

## 9. 里程碑

先出 spec，才寫程式。規格檔放 `docs/spec/`，標 `DRAFT` / `READY`。
**只有標 READY 的規格可以動手。**

> ⚠ **這張表的「現況」欄要跟著改。** 舊版把 M0 的四件剩餘工作留在表上，
> 而那四件早就做完了——`docs/playtest/04` 的「四件事被擋住」是同一個形狀，
> **待辦寫在文件裡不會自己消失，然後每一輪都繞著它打轉**。
> 現況以 `CONTEXT.md` §3 與 `docs/INDEX.md` 為準，這裡只給一句話。

| | 里程碑 | 現況 | 內容 |
|---|---|---|---|
| M0 | 環境與偵查 | ✅ **完成** | 兩版素材入庫、兩版 `KI.EXE` 進 IDA、逐檔比對、DOSBox-X docker 化並確認可重現、字型結案、防拷確認、說明書判讀完 |
| M1 | 資料格式全解 | 🔵 大致完成 | `.BRG`、`*GRF.DAT`、`.MAP/.MDL/.SCH/.MCH`、`OPEN_S*`／`END_S*`、`TALK.DAT`、`SINARIO/SAVE.DAT`、音訊。Go 解碼器 + round-trip 測試。**從兩版相同的那 23 個檔入手**（§3.10） |
| M2 | 文本抽取與日中對照 | 🔵 核心完成 | `TALK.DAT` 模板片段與插入點語意全解；兩版 `TALK.DAT` 對照表產出；Big5 原文抽成語系檔並能 byte-for-byte 寫回 |
| M3 | 執行檔反組譯 | 🔵 進行中 | `KI.EXE` 主迴圈、**即時制的時間模型**、事件觸發、存檔讀寫、亂數。兩版對照當交叉驗證（§5.0） |
| M4 | 規則規格 ＋ 機制文件 | 🔵 進行中 | 政略、行軍、戰鬥、經濟、外交、勝負。**同步產出 `docs/mechanics/`（§6）**，含 `15-realtime.md` 與 `70-ai.md` |
| M5 | Go 引擎（規則層） | 🔵 進行中 | 純邏輯，不認識畫面。以 tick 驅動 |
| M6 | 呈現層 | 🔵 進行中 | Ebiten、大地圖 ＋ 45 度戰場、音樂音效 |
| M7 | 日文原版對照與校訂 | ⬜ 未開始 | 逐句對照 → 校訂繁中母本，差異全部記錄。素材已到位（§3.10） |
| M8 | 打包發行 | ⬜ 未開始 | 跨平台建置、資產 deny-list 掃描、實機驗收 |

> 原本寫「M0／M1 沒完成之前不要碰 M5 以後」。**這條已經不適用**——
> M1 的格式解析與 M5 的規則層一直是交錯進行的，而且那樣才對：
> 規則層跑起來才會暴露格式解錯（補兵抄隊長兵種那個 bug 是畫上人物圖形
> 才看見的）。留這句話會讓下一輪誤以為現在的做法違規。

---

## 10. 硬規則 `[HARD]`

- **建置一律走 docker**（Go、IDA、DOSBox、Python 全部）。不裝到系統環境。
- **Python 一律 docker + uv venv**，不污染系統。
- **手打 `docker run` 一律帶 `--rm --log-opt max-size=10m --log-opt max-file=3`。**
- **原版資產唯讀。** 測試存檔一律寫到 `/tmp` 或明確的測試輸出目錄，
  **不覆蓋原版 `SAVE.DAT`／`SINARIO.DAT`**。
- **存檔寫回策略是「改寫」不是「重建」**：從原始 bytes 出發，只蓋已解欄位，
  未解區域一個 byte 都不動。驗收標準是 byte-for-byte round-trip。
- **不得 commit 或打包任何原版 `.EXE/.COM/.DAT/.MAP/.MDL/.SCH/.MCH/.BRG/.O`**，
  也不得散布倚天字型。發行前跑 deny-list 掃描。
  唯一例外：`docs/images/` 底下的 README 展示截圖（那是渲染結果不是資產本身）。
- **推論標籤要誠實**：confirmed / 強證據 / 假說 / 未知。
  不准為了讓 worklist 看起來完整而編規則。
- **每個結論要標明是在哪個執行檔上驗的**（§8 第 9 條）。
- **不碰共用的 docker 資源。** 只能清理自己這次建立的 container。**禁止**
  `docker image prune`、`docker system prune`、`docker volume prune`、
  `docker builder prune`、`docker rmi`、`docker container prune`。
- **截圖驗收要帶固定亂數種子與固定時間步。** 即時制沒有固定時間步就重跑不出結果。
- **ESC 只取消／退回上一層，F10 才離開**，離開前跳 Y／N 確認並自動存檔；
  存檔失敗就不離開。

---

## 11. 每一輪的工作紀律

### `[HARD]` 提交前跑 `tools/check.sh`

一支跑完 `go vet`、`go test`、`tools/index.py generate`。第三項會擋下：

- 文件缺狀態行或日期
- **狀態行說「未解／受阻」但內文已經有一堆 confirmed** ——
  這是本專案最常犯的錯，`docs/formats/07`、`docs/playtest/04`、
  `CONTEXT.md` 的「進行中／受阻」表都中過
- 連結壞掉、`CONTEXT.md` 提到不存在的文件

### `[HARD]` **動手之前**先查 `docs/INDEX.md`（不是下結論之前）

那份是生成的，含**斷言總表**（欄位／常數 → 推論等級 → 出處）。

> ⚠ 這條原本寫「下結論之前」，**太晚了**。2026-08-08 就是這樣把
> `sub_15695`（早已實作進 `internal/rules/economy`）與 `sub_15715`
> （`docs/re/07` §15 逐行寫著）各重推了一次——
> **「還沒解」與「我不記得解過」在動手那一刻長得一模一樣**，
> 要在動手前分開，不是在收尾時。

`grep` 一次幾乎零成本，而漏查的代價是重推一次已經有答案的東西——
或更糟，繞著一道**已經不存在的閘**打轉（`docs/playtest/04` §6 的四件事，
在那張表寫下的當下就有三件已經解掉了）。


1. 動手前讀 `CONTEXT.md` worklist（狀態的單一真相來源）與相關的
   `docs/re/`、`docs/spec/`。
2. 看 `git status --short`。既有改動屬於使用者或當前任務，**不要 reset 或丟棄**。
3. 做完一項：更新 markdown → **清掉被推翻的斷言**（不是加註解，是刪掉並記進推翻清單）
   → 跑測試 → 留視覺／實跑證據 → commit + push → 更新 `CONTEXT.md` 現況。
4. 宣稱「完成」之前，對照 reference 實測。**測試綠不是完成**，測試綠只是沒退步。
5. 簡易、可平行的工作派 subagent；派工時把邊界寫進 prompt。

### 派 subagent 的邊界（主迴圈自己要寫進 prompt）

- 只能清理自己建立的 container，**禁止**任何 docker image／volume／system prune 或 rmi。
- 明列不准改的目錄（其他 repo、`~/.claude/`、`workplace/orig/`）。
- 明列不准做的收尾動作（commit、push、重編、清理）。**沒寫的等於允許**。
- 收到「我順便做了 X」時，X 一律當事故處理：先查影響範圍再談結果。

### ⛔ 有 subagent 在跑時，主迴圈**不要 `git add -A`**

「禁止 agent commit」只管住了 agent，沒管住主迴圈。
subagent 執行期間，主迴圈 commit 一律列出明確路徑，不用 `-A` 也不用 `.`。

---

## 12. 目錄結構

```
docs/re/          反組譯筆記（編號流水，一個發現一份）
docs/formats/     檔案格式規格
docs/spec/        實作規格（標 DRAFT / READY，只有 READY 能動手）
docs/mechanics/   遊戲機制，按屬性分類（§6）。15-realtime.md 與 70-ai.md 是重點
docs/playtest/    實跑驗收紀錄與截圖
docs/reference/   外部中文資料整理（§2），標來源網址與抓取日期
translations/     glossary.md 譯名表、日文對照紀錄、校訂流程說明
internal/assets/  純解碼層，回傳 image.RGBA，不認識 Ebiten
internal/game/    規則層，不認識畫面，以 tick 驅動
internal/ui/      Ebiten 呈現層（textlayout 與 layout 刻意不依賴 Ebiten）
cmd/              可執行程式（遊戲本體、抽字工具、解碼驗證工具）
tools/            docker 包裝腳本（go.sh、ida.sh、dosbox.sh）、fdi_extract.py、addr.py、dump_func.py、IDC 腳本
workplace/orig/dosv/      松崗 DOS/V 繁中版 69 檔（gitignore，唯讀）
workplace/orig/pc98/      PC-98 日文原版 69 檔，由 fdi_extract.py 抽出（gitignore，唯讀）
workplace/orig/pc98_fdi/  PC-98 五片磁片映像（gitignore，唯讀）
workplace/ida/{dosv,pc98}/  IDA database 與 dump（gitignore）
```

`workplace/orig/` 底下三個目錄都已 `chmod a-w`。
**目錄分成 `dosv` 與 `pc98` 是刻意的**——§8 第 9 條禁止跨檔案外推，
路徑本身就標明了這份素材是哪一版，寫筆記時不會混。

`CONTEXT.md` 的骨架：現況一覽表／文件索引／術語表／**已被推翻的斷言**／
Worklist（含「一句話現況」與「下一步」）／已建好的工具清單。

---

## 原始素材

repo 根目錄的四份原始檔**都不進版控**，第一輪已解開搬進
`workplace/orig/` 並 `chmod a-w`：

| 檔案 | 內容 | 已解開到 |
|---|---|---|
| `卧龙传.zip` | 松崗 DOS/V 繁中版，72 檔 | `workplace/orig/dosv/` |
| `【PC98】臥竜伝 三国制覇の計.rar` | **PC-98 日文原版**，5 片 `.fdi` 磁片映像 | `workplace/orig/pc98_fdi/` → 抽檔到 `workplace/orig/pc98/` |
| `Garyouden_Manual.pdf` | **日文原版說明書 38 頁**，200 dpi 掃描，無文字層 | 判讀結果進 `docs/reference/` |
| `Garyouden_Xtra_Addendum.pdf` | 追補頁 4 頁 | 同上 |

- **尚未取得**：松崗中文版說明書、`STDFONT.24`／`ASCFONT.24`／`ASCFONT.15`（§3.6）。

# github repo

https://github.com/wicanr2/wolong_cht.git

# 工作目錄

`/home/anr2/cht/dragon`
